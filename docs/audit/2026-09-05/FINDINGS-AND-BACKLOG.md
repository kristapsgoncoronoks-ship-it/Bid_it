# System audit 2026-09-05 — findings register, debate and master backlog

Second full audit of InvoiceIQ (the first, 2026-08-16, lives beside this file
and closed its bounded backlog by WO-42). Nine specialist investigations ran
read-only against the tree at `d8a92ec`; every HIGH/CRITICAL finding below was
then re-verified by the Lead Developer by reading the cited code before it
entered this register. Certainty is the Lead's after that check, not the
specialist's.

Severity: CRITICAL / HIGH / MEDIUM / LOW. Certainty: CONFIRMED / LIKELY /
POSSIBLE / UNKNOWN. Decision: ACCEPT / MODIFY / REJECT / DEFER (with the reason
in the debate section). Status uses the backlog vocabulary.

## 1. Findings register

### Security (SEC)

| ID | Title | Sev | Certainty | Evidence | Decision |
|---|---|---|---|---|---|
| SEC-001 | SSO/SCIM users are created with a bcrypt hash of a **known literal** (`"!sso-no-password"`, `"!scim-no-password"`); `/auth/login` verifies the submitted password against it like any other → the literal signs in as any IdP-provisioned user | CRITICAL | CONFIRMED (read `oidc.py:46,355`, `scim.py:41,144`, `auth.py:132`, `security.py:28-33`) | pre-auth account takeover for every SSO/SCIM tenant | ACCEPT — P0 |
| SEC-002 / ARCH-001 | `--forwarded-allow-ips '*'` makes uvicorn write the client-supplied leftmost `X-Forwarded-For` into `scope["client"]`; the app's own `_client_ip` (with `trusted_proxy_count=0`) then trusts it → per-IP auth rate limit is mintable, audit `ip` forgeable | HIGH | CONFIRMED (uvicorn `proxy_headers.py:176-177`; `Dockerfile:55`; both prod composes) | password spraying across accounts; forged audit attribution | ACCEPT — P1 |
| SEC-003 | SPA origin serves no CSP while the 24h bearer token lives in `localStorage` | MEDIUM | CONFIRMED | XSS → 24h credential exfiltration | ACCEPT — P1 (config) |
| SEC-004 | nginx `location /assets/` declares its own `add_header`, discarding inherited security headers | MEDIUM | CONFIRMED (nginx semantics) | JS/CSS served without nosniff/HSTS | ACCEPT — P1 (config) |
| SEC-005 | Portal/calendar capability tokens sit in the URL path and are written to access logs; portal tokens never expire | MEDIUM | CONFIRMED | credential in logs | MODIFY — P2 redact in logs now; expiry column is an owner decision on duration |
| SEC-006 | SSO delivers the session token in a URL fragment | LOW | LIKELY | browser history exposure | DEFER — P3 with ARCH-012 |
| SEC-007 | 8-char passwords, no MFA, no authenticated change-password; `LoginRequest.password` unbounded | LOW-MED | CONFIRMED | below finance-system bar | MODIFY — P2 bound login length now; policy/MFA is an owner decision |
| SEC-008 / OPS-010 | Default KEK derives from `SECRET_KEY`; rotating it (the documented "logs everyone out") destroys every sealed secret; production validator does not require BYOK | LOW | CONFIRMED | irrecoverable SSO secrets on a routine rotation | MODIFY — P1 docs + startup warning; refusing `local` would break the live deployment |
| SEC-009 | SSRF guard fails open on DNS failure; rebinding window between check and connect | LOW | LIKELY | narrow SSRF | DEFER — P3 → **DONE in reference batch R1 (2026-09-07, PAT-028)**: the webhook POST goes through `app/core/outbound_http.PinnedPublicAsyncHTTPTransport` — resolve right before connecting (off the loop), refuse the whole destination if any answer is non-public (incl. 100.64/10, 64:ff9b::/96, multicast), pin the socket to one vetted IP, keep Host + SNI, no redirects; a rebound answer is a terminal `blocked:` delivery. The fail-open on NXDOMAIN at registration stays by design (a ConnectError at delivery retries) |
| SEC-012 (new, 2026-09-08) | The bearer-token gate checked that the `jti` names a LIVE session but never that the session belongs to the token's `sub`: whoever holds the signing key (or a verify-only fallback — HS256 verification IS signing) could forge `{sub: victim, jti: <their own live session>}` and be served as the victim, with the audit trail naming the victim; "revoke sessions" would not help because the attacker's session is legitimately live. Found by the R3 review panel's probe (reference integration, Adversarial lens B-1), reproduced on the live app (`GET /auth/me` answered as the victim) | MEDIUM (needs the key; then full impersonation) | CONFIRMED (probe) | cross-tenant impersonation given a key leak | ACCEPT — P1 — **DONE in reference R3**: `_authenticate` refuses a session whose `user_id` is not the token's subject; `test_token_subject_must_own_the_session` is the probe as a red test |
| SEC-010 | Audit chain has no external anchor | LOW | CONFIRMED (design limit) | tamper-evident, not tamper-proof | DEFER — P4 |
| SEC-011 | Dependency CVE status unassessed; no `pip-audit` in CI | UNKNOWN | UNKNOWN | — | ACCEPT — P2 CI step |

| SEC-013 (new, 2026-09-08) | The nightly **PII history scan** (`pii-history.yml`, the WO-6 quarantine's authoritative full sweep of every blob on every ref) has been RED on 36 of its 37 scheduled runs since its first night, 2026-07-29 (the one green: #25, 2026-08-27), and nobody noticed — a control that fails every night and is never read is not a control. Run #37 (2026-09-08, on fe66349): structural VAT-id / IBAN hits in historic blobs of `customer_db.py`, `supplier_db.py`, `adjust.py`, `supplier_specs.py` (paths no longer reachable from the local branch tips — history on other refs), 6,724 blobs scanned, deny-list EMPTY (the Fleet Fuel identifier list is still pending the owner's decommission archive). The scan itself is right to fail: the workflow's own rule is "if verifiably synthetic, allowlist with a justification and a NAMED verifier; never disable" — and the verifier is a person, not this session | HIGH (a red PII control, unread for six weeks) | CONFIRMED (workflow run list #1–#37; run #37 log) | **Owner decision — DECISIONS §24**: (a) name the verifier and classify the flagged historic values (synthetic → `scripts/pii_allowlist.json` entries; real → history rewrite + credential/PII incident handling per the harvest protocol); (b) provide the deny-list from the decommission archive. Engineering half, this batch: the run is now READ — the dashboard's build line carries it, and the runbook names it |
### Backend (BE)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| BE-001 | `POST /jobs/{id}/retry` requeues a RUNNING job (clears `locked_by`) → same job executes twice concurrently; not restricted to user-enqueueable kinds (`everypay.charge_mit` included) | HIGH | CONFIRMED (`jobs.py:257-267`, `routes/jobs.py:68-72`) | ACCEPT — P1 |
| BE-002 | `reclaim_stale` ignores `max_attempts` → a job that kills its worker is reclaimed forever (hot crash-loop) | HIGH | CONFIRMED (`jobs.py:244-254`) | ACCEPT — P1 |
| BE-003 | `jobs.enqueue` IntegrityError handler calls `db.rollback()`, discarding the caller's uncommitted business work when `commit=False` | HIGH | CONFIRMED (`jobs.py:106-121`) | ACCEPT — P1 |
| BE-004 | Idempotency unique index is unconditional while the pre-check is live-only → re-enqueue after success/dead returns the old terminal job as `201` | HIGH | CONFIRMED | MODIFY — P2: `retry` (BE-001 fixed) is the operator path; route reports dedup instead of 201 |
| BE-005 / DB-003 | `charge_renewal` flushes the dedupe row, charges the card, then commits → crash/timeout after the charge repeats it next run | HIGH | CONFIRMED (`billing.py:236-253`) | ACCEPT — P1 |
| BE-006 / ARCH-004 | Stripe SDK calls are synchronous inside `async def` — block the event loop (80s default timeout) | HIGH | CONFIRMED | ACCEPT — P1 |
| BE-007 / ARCH-003 | ClamAV scan is blocking untimed socket I/O on the request path, 11 call sites | HIGH | CONFIRMED | ACCEPT — P1 |
| BE-008 | bcrypt hash/verify inline in async auth routes | MED-HIGH | CONFIRMED | ACCEPT — P2 — **DONE earlier than the register recorded**: `ac313de` moved every hash/verify (`auth.py` register/login/change-password, `team.py` join) onto `run_in_threadpool`; verified 2026-09-06, no inline call remains outside the offline seed |
| BE-009 | Inbound email has no message-level idempotency; provider retry duplicates the message | MED-HIGH | CONFIRMED | ACCEPT — P2 |
| BE-010 | Export-once counter mutated by `GET` routes | MEDIUM | CONFIRMED | ACCEPT — P2 (breaking verb change; SPA + e2e follow) |
| BE-011 / PERF-005 | `GET /payment-runs` N+1 ×2 per run, unbounded, bare list | MEDIUM | CONFIRMED | ACCEPT — P2 — DONE ac718b9 (one grouped count; `limit`/`offset`) |
| BE-012 | `GET /invoices/captures/review` 2× N+1 per page | MEDIUM | CONFIRMED | ACCEPT — P2 — DONE ac718b9 (two grouped reads per page) |
| BE-013 | Org switcher N+1 | LOW-MED | CONFIRMED | ACCEPT — P3 |
| CONC-001 (new, 2026-09-06) | First concurrency datapoint (CI #543, 8 in flight, scale 300): `dashboard` 12.5×, `ap_aging` 10.1×, `cash_position` 10.4× the serial p95 — more than serialising would cost; `invoice_list`/`explore`/`reliability` 2–5× (overlapping, as expected). Datapoint 1 (CI #544, artefacts removed, zero errors) reproduces it: dashboard 11.4×, ap_aging 12.0×, cash_position 11.7×, transport_reliability 11.2×, with conc p50 at 5–6× and p95 at 11–12× — the last requests of each batch wait for a resource smaller than eight; single-statement reads (`invoice_list` 5.5×, `explore` 4.6×) show no such tail. First suspect: the connection pool (default 5 + 10 overflow) held across multi-statement aggregate reads. Second suspect since PERF-016 (2026-09-06): a gen-2 garbage-collection pass is stop-the-world for every request in flight on the worker, and those four reads are the ones that allocate most — datapoint 2 (CI #547, the first with the startup heap frozen) answered: `ap_aging` 6.99×, `cash_position` 7.18×, `transport_reliability` 7.40× — below 8, no longer contended — and `dashboard` 10.36×, still contended; datapoint 3 (CI #548) repeated it: dashboard 11.54×, the other three 6.82–7.61×. Three of the four rows were the collector (PERF-016); the finding now names ONE endpoint, the dashboard (five statements per request on one held connection), and the pool measurement applies to it. The write path is fine (same-org 4.95×/5.05×, across-orgs 6.57×/7.92×, no errors) | MEDIUM | CONFIRMED — and concluded (P2 batch 4): the pool measured as a variable on the dashboard (`DB_POOL_SIZE` 2 / 8 / 20, no overflow, local Postgres 16): 4.80× / 10.15× / 10.35× at **22.9 / 20.3 / 22.1 req/s** — throughput independent of the pool, the same 340–570 ms concurrent p95 band at every size; pool starvation refuted, the cause is the endpoint's per-request Python work on one worker's loop (five hydrated statements reduced in Python, interleaved with seven others). Datapoint 4 (CI #551) agreed before the measurement: one endpoint | CLOSED as a finding (`CONCURRENCY-2026-09-06.md`, pool section); no pool setting changed (none of the three sizes beat the default on any endpoint); the remedy is on the endpoint → PERF-018 |
| CONC-002 (new, 2026-09-06) | `rate_limit_per_min` = 300 per token/IP (fixed 60 s window): one API token cannot sustain more than 5 requests/second — an integration replaying a day's invoices will meet it (datapoint 0 tripped it at ~340 requests/min) | LOW | CONFIRMED (`app/core/config.py:309`, CI #543 log: 429 × 4) | DEFER — owner policy number (DECISIONS): raise per plan, or per-token burst allowance |
| BE-014 | `issuer.lock()` fallback commits mid-transaction | MEDIUM (latent) | CONFIRMED (`issuer.py:165` → `get_or_create` → `db.commit()` with the numbering row lock held) | ACCEPT — P2 — **DONE (P2 batch 3)**: the first issuer is created and flushed inside the caller's transaction, then re-selected FOR UPDATE; test patches `commit` to raise |
| BE-015 | SMTP failures are recorded and never retried | MEDIUM | CONFIRMED | DEFER — P3 (email job kind; design) |
| BE-016 | `audit.record` swallows every exception incl. flush errors that poison the session | MEDIUM | LIKELY | ACCEPT — P2 |
| BE-017 | Worker swallows queue-health failures with bare `pass` | LOW | CONFIRMED | ACCEPT — P3 (trivial, ride along) |
| BE-018 | Two API contracts coexist (bare list vs envelope; `{detail}` vs `{detail,code}`) | MEDIUM | CONFIRMED | DEFER — P3 (large, non-breaking path needed) |
| BE-019 | Blobs written before the owning row commits; no orphan reaper | LOW-MED | CONFIRMED | DEFER — P3 |
| BE-020 | Uploads buffered whole in memory; `/email/inbound` decodes every attachment before writing any | MEDIUM | CONFIRMED | MODIFY — P2 bound attachment count/total for inbound; 15 MB cap already bounds uploads — **DONE (P2 batch 3)**: `InboundEmailIn.attachments` ≤ 20, each `content_base64` ≤ the upload cap in base64 (20,971,520 chars at 15 MB), both in the schema (422 before any decode); the Mailgun route refuses a count above 20 as a classified malformed delivery and skips an oversize part by its declared size without reading it; the body as a whole is nginx's 20 MB |
| BE-021 (new, 2026-09-07) | A CSV/JSON line whose `amount` cell is present but not a number ("n/a", "TBC", a stray unit) was parsed as 0.00 with no warning while its provenance still read "extracted" — the line was saved at no cost (measured: `Gravel,10,5.00,n/a` → 0). Fixed in P2 batch 5: the parser falls back to quantity × unit price (the fill an empty cell gets) and appends a per-line warning naming the cell; the provenance keeps the original text beside the figure | MEDIUM | CONFIRMED (parser probe) | ACCEPT — **DONE (P2 batch 5)**, seeded red |

| BE-022 (new, 2026-09-08) | `POST /billing/checkout` (`routes/billing.py` `start_checkout`) has no guard on an existing subscription — only price/purchasable checks — so a second Checkout for a customer who already holds `stripe_subscription_id` creates a second subscription; until R4 the SPA's disabled button was the only double-subscribe guard and it expired silently at 90 s | MEDIUM | LIKELY (R4 panel, Product U-2 — code path read, provider behaviour not executed) | ACCEPT — P2 — **DONE (reference R6)**: `start_checkout` answers 409 for a subscription provider when the org holds a non-canceled `stripe_subscription_id` (before the provider is reached; EveryPay's stored card is not a subscription object and still starts a hosted payment); the Plan & billing page shows "Change to <plan> via Manage billing" for a subscriber and opens the Customer Portal — the Portal's plan-switch setting is the owner's (DECISIONS §25); found on the way: BE-023 |
| BE-023 (new, 2026-09-08) | `PUT /billing/plan` (`change_plan`) let a workspace that holds a live Stripe subscription set the FREE plan in-app: entitlements dropped at once while the provider kept charging until someone cancelled in the Portal, and the next renewal webhook (`_apply_to_org`, plan from the subscription) silently put the paid plan back. The mirror image of BE-022 — payment outrunning entitlements. Found by the R6 batch while placing the BE-022 guard; CONFIRMED by both R6 panels | MEDIUM (money: a customer paying for a plan they no longer have, then reverted without a word) | CONFIRMED (code path read by three lenses; not executed against Stripe) | ACCEPT — P2 — **DONE (R6), a business-behaviour change made under the autonomy rule and flagged**: `change_plan` answers 409 for a Stripe subscriber choosing the free plan ("cancel it through Manage billing; the free plan applies when the provider confirms"); the page's Free card for a subscriber reads "Cancel subscription via Manage billing" and opens the Portal, whose `canceled` webhook applies the free plan for real. Not a policy: the in-app path could never cancel at the provider. The owner's residual choices (Portal cancel timing, plan-switch setting) — `DECISIONS-NEEDED.md` §25 |
| BE-024 (new, 2026-09-08) | `jobs.run_once` has no per-job deadline: a handler that awaits forever (a provider SDK call without a timeout, a lost `await`) holds the worker indefinitely while the lease heartbeat keeps its lease alive by design (STIR-P1-01) — so neither `reclaim_stale` nor the new liveness probe (R6) sees it; only the OCR path has a budget (STIR-P2-01). Found by the R6 panels while checking the probe's claims | MEDIUM (one hung handler removes a worker from the fleet silently; `/health/queue` pages only once the backlog ages past 15 min) | CONFIRMED (code read; the probe's own negative test demonstrates the boundary) | ACCEPT — P2: a per-kind deadline (`asyncio.wait_for` around the handler with a kind-specific budget, default generous; a timed-out job fails with a retry) — separate row; the probe's documentation states the limit until then |

### Database & data integrity (DB)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| DB-001 | `billing_payments.amount_eur` is `Float` — the only float money column | HIGH | CONFIRMED | ACCEPT — P1 (migration to `Numeric(14,2)`) |
| DB-002 | Issued-number uniqueness is per org while numbering is per issuer with an identical default prefix → second issuer's first invoice collides and burns a number | HIGH | CONFIRMED (`issued_invoice.py:52`, `issuer.py:74-75`, `issued_service.py:22`) | MODIFY — P1: refuse a duplicate prefix per org at the service + assign a distinct default; constraint migration deferred (P2) with pre-flight |
| DB-003 | = BE-005 | | | |
| DB-004 | Bank-statement duplicate guard is check-then-insert; no unique on `(org_id, sha256)` | HIGH | CONFIRMED | ACCEPT — P1 |
| DB-005 | No per-vendor uniqueness on supplier invoice number | MED-HIGH | CONFIRMED | REJECT hard constraint (legitimate reuse exists); advisory `duplicates` service is the control — P3 tighten advisory |
| DB-006 | Append-only ledgers enforced by convention only | MEDIUM | CONFIRMED | DEFER — P3 (Postgres-only REVOKE/trigger; needs role model) |
| DB-007 | Org delete cascades into `audit_events` and `archived_invoices` | MEDIUM (latent) | CONFIRMED | ACCEPT — P2 (RESTRICT on those two) — **DONE (P2 batch 3)**: models + migration `c4d6e8f0a2b4` (Postgres by constraint name; SQLite by batch-mode naming convention so `alembic check` reads RESTRICT); no code path hard-deletes an organisation, so nothing changes today |
| DB-008 | `invoices.vendor_id` CASCADE | MEDIUM (latent) | CONFIRMED | ACCEPT — P2 (RESTRICT) — **DONE (P2 batch 3)**, same migration; vendors retire through the recycle bin, never a hard delete |
| DB-009 | `users` delete cascades into expense reports | MEDIUM (latent) | CONFIRMED | ACCEPT — P2 (RESTRICT) — **DONE (P2 batch 3)** on `expense_reports.employee_id` and `expense_transactions.employee_id`; erasure (`privacy.erase`) pseudonymises the user row and is unaffected |
| DB-010 | Nine parent links lack the composite tenant-safe FK | MEDIUM | CONFIRMED | DEFER — P3 (needs `UNIQUE(org_id,id)` targets first) |
| DB-011 | 59 state columns unconstrained `String` | MEDIUM | CONFIRMED | ACCEPT — P2 (CHECKs on the six financial ones, with pre-flight) — **DONE (P2 batch 3)**: `payment_runs.status`, `reimbursement_batches.status`, `expense_reports.status`, `vat_refund_claims.status`, `issued_invoices.lifecycle`, `billing_payments.state`; the sets are the constants the code writes (`RUN_STATUSES`, `BATCH_STATUSES`, `EXPENSE_STATUSES`, `CLAIM_STATUSES`, the new `LIFECYCLES` and `PAYMENT_STATES`), a test pins the models' CHECK text to the migration's sets, and the migration refuses on any value outside a set (one exact normalisation: the demo seed's `lifecycle='paid'` → `issued`, which is how the resolver already read it; the seed is corrected) |
| DB-012 | Model↔migration parity test compares tables+columns only | MEDIUM | CONFIRMED | ACCEPT — P1 (test-only; may surface drift) |
| DB-013 | `amount_paid` caches have no reconciliation check | MEDIUM | CONFIRMED (the AR side WAS checked by `integrity.verify_ledger`; the AP side — `invoices.amount_paid` against `supplier_payments` — was not) | ACCEPT — P2 (integrity job/report) — **DONE (P2 batch 3)**: `verify_ledger` now checks every supplier invoice's cache against one grouped ledger sum (finding kind `supplier_invoice_ledger`); reachable through the existing `/integrity/ledger/verify` route and `integrity.verify_ledger` job |
| DB-014 | Vendor IBANs unnormalised, no cross-vendor collision surface | MEDIUM | LIKELY | ACCEPT — P2 |
| DB-015 | Org-blind single-column indexes on `invoices`/`expense_items` | LOW-MED | LIKELY | DEFER — P3 (measure with EXPLAIN first) |
| DB-016 | FX-rate scale inconsistent (8 vs 6 dp) | LOW | CONFIRMED | DEFER — P4 |
| DB-017 | `issued_invoices.issuer_id` FK is single-column despite a composite target built for it | LOW-MED | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 3)**: `fk_issued_invoices_issuer` is `(org_id, issuer_id) → issuer_profiles(org_id, id)`; on Postgres `ON DELETE SET NULL (issuer_id)` (see DB-018); pre-flight refuses if any invoice references another workspace's issuer (0 by construction); pg-only test proves a cross-tenant reference is refused and an issuer delete nulls only `issuer_id` |
| DB-018 (new, 2026-09-06) | Every composite `(org_id, project_id) → projects(org_id, id) ON DELETE SET NULL` (on `invoices`, `issued_invoices`, `expense_items`) nulls BOTH columns on a project delete, so the delete fails on `org_id NOT NULL` instead of detaching the documents the FK's own comment promises to keep — measured on Postgres 16: `null value in column "org_id" of relation "invoices"` | LOW-MED (latent: no project-delete route exists) | CONFIRMED (reproduced in a rolled-back transaction, 2026-09-06) | ACCEPT — **DONE (P2 batch 3)**: the three links are recreated `ON DELETE SET NULL (project_id)` (Postgres ≥ 15 column list; production runs 16); SQLite has no such form and no delete path, so its model FK stays plain; pg-only test deletes a project and asserts `project_id` NULL with `org_id` intact |

| DB-020 (new, 2026-09-08) | `invoices.vendor_id` is a plain FK to `vendors.id` (`invoice.py`), unlike the composite `(org_id, *_id)` FKs on cost centre / department / project, so a cross-org vendor reference is structurally possible; `uq_vendors_org_id` exists, so the composite form is available. R4's inbox read hides such a name via its ON-clause org predicate (HEAD returned it on the unguarded path) | LOW | POSSIBLE (R4 panel, Security S-3) | ACCEPT — P3, data-integrity group: composite FK in a migration that pre-flights for violators |

### Architecture (ARCH)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| ARCH-002 | k8s reference config cannot boot (no `INBOUND_EMAIL_SECRET`) and defaults to per-pod local storage on a read-only rootfs | HIGH | CONFIRMED | ACCEPT — P2 (config/docs; storage validator MODIFIED — the live VPS uses local on a shared volume, so refuse-on-local would break production) |
| ARCH-005 | Business logic + transaction boundaries in four god-controllers (5,235 lines, 70 services fan-in) | HIGH | CONFIRMED | DEFER — recorded debt; incremental extraction per touched flow, never a rewrite |
| ARCH-006 | CPU-heavy work inline in requests (OCR, ingest, PDF) | MEDIUM | CONFIRMED | DEFER — P3 (job kinds; SCALING.md names it) |
| ARCH-007 | SMTP inside an open request transaction | MEDIUM | CONFIRMED | DEFER — P3 with BE-015 |
| ARCH-008 | Bounded-context seam enforced one way only | MEDIUM | CONFIRMED | ACCEPT — P2 (mirror test with explicit allowlist) |
| ARCH-009 / PERF-012 | Per-process rate limiter | MEDIUM | CONFIRMED (documented, ADR-0015) | DEFER — P3 after SEC-002 |
| ARCH-010 | Audit advisory lock held to commit | MEDIUM | LIKELY | DEFER — P3 (measure) |
| ARCH-011 / QA-003 / FE-017 | Hand-maintained `types.ts` mirror; no contract gate | MEDIUM | CONFIRMED | ACCEPT — P1 OpenAPI drift gate (ERD pattern); P3 codegen |
| ARCH-012 | 24h non-refreshable token in localStorage | MEDIUM | CONFIRMED | DEFER — P3 (design; server-side revocation exists) |
| ARCH-013 / PERF-011 | Daily scheduler O(tenants×kinds) round-trips, on every worker | MEDIUM | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 4)**: `enqueue_daily` takes a transaction-scoped Postgres advisory lock (`pg_try_advisory_xact_lock`, other workers return 0 without reading a tenant; SQLite always proceeds), builds the wanted (org, kind) pairs, reads today's already-queued pairs in ONE statement, inserts only the missing ones and commits once; a repeat sweep costs the same statements for one tenant as for two, no INSERT; pg-only lock test in the CI postgres list |
| ARCH-014 / PERF-008 | Blocking `urllib` in `fx.refresh_from_ecb` reachable from a route | MEDIUM | CONFIRMED | ACCEPT — P1 (ride with the blocking-I/O batch) |
| ARCH-015 | Upload quota check-then-act | LOW | LIKELY | DEFER — P3 |

### Performance (PERF)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| PERF-001 | Reconciliation candidates load whole cash tables into Python | HIGH | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 4)**: the four candidate selects carry `amount BETWEEN magnitude − 0.02 AND magnitude + 0.02` (Numeric(14,2) columns, so the SQL window is exactly the Python one); the window is unchanged (0.02 in, 0.03 out — tested) and the receipts read is asserted to contain BETWEEN |
| PERF-002 | Dashboard materialises every open payable (twice) for five scalars | HIGH | CONFIRMED | ACCEPT — P2 (after PERF-004 measures it) |
| PERF-003 | Receivables report walks all AR history on every dashboard load | HIGH | CONFIRMED | ACCEPT — P2 |
| PERF-004 | Perf harness never sets `workflow_state` and seeds no `IssuedInvoice` → the three ratios above measured empty sets; the growth gate is blind to them | HIGH | CONFIRMED (grep: neither symbol in `perf_harness.py`) | ACCEPT — P1 |
| PERF-006 | Project P&L summary O(projects×invoices) ×3 queries | HIGH | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 4)**: `pnl_summary` reads the tenant in a fixed number of statements — grouped issued/expense/cost-entry sums, one bulk invoice-cost pass (`_invoice_costs_bulk`: five statements for every allocated invoice, same precedence rule, same `_split_shares`), one bulk accepted-offer read — composed by the `_compose` the detail route shares; statement count equal at 3 and 9 projects, every figure equal to the per-project detail incl. a frozen project's adjustments (tested; two seeded violations red) |
| PERF-007 | openpyxl/reportlab on the loop in analytics export | MEDIUM | CONFIRMED | ACCEPT — P1 (blocking-I/O batch) |
| PERF-009 | Integrity sweeps re-hash every document on the loop in one request | MED-HIGH | CONFIRMED | ACCEPT — P2 (202 + job) — **DONE (P2 batch 4)**: the three integrity routes count their references first (`integrity.reference_count`, indexed counts); at or below `INTEGRITY_SYNC_LIMIT` (default 500) they verify in the request as before, above it they queue the matching job with a per-day key and answer 202 `IntegrityQueuedOut` (`created: false` on a repeat click); the SPA's Data-integrity card handles 202 (toast + notice pointing at Background jobs); OpenAPI snapshot regenerated |
| PERF-010 | ~80 unbounded bare-list endpoints | MEDIUM | CONFIRMED | DEFER — P3 (house rule + top three) |
| PERF-013 | Local storage default; byte-oriented storage protocol | MEDIUM | CONFIRMED | MODIFY — see ARCH-002 |
| PERF-014 | SPA renders unbounded lists | MEDIUM | CONFIRMED | DEFER — P3 with PERF-010 |
| PERF-015 | `pool_timeout=30` hides saturation | LOW-MED | LIKELY | ACCEPT — P3 |
| PERF-016 (new, 2026-09-06) | A gen-2 garbage collection over the imported app (335,660 tracked objects) costs 131–146 ms and fires on any read that hydrates a few thousand ORM rows (about every third call at 2,400 rows) — a stop-the-world stall per worker, proportional to the process heap, not to the data. Surfaced as the R15 gate reading 7.47× then 12.24× on ONE commit (CI #544/#545; `transport_reliability` itself grows 1.85×). `docs/perf/GC-PAUSE-2026-09-06.md` | HIGH (blocked the queue-tail deploy; a ~140 ms tail on every large read in production) | CONFIRMED (reproduced without HTTP, 11.83×; fixed and re-measured, 2.87×) | ACCEPT — P1 — **DONE**: `gc.freeze()` at the end of `lifespan` (`app/core/gc_tuning.py`), the harness measures the same configuration, p50 ratio printed beside the gated p95 |
| PERF-017 (new, 2026-09-06) | After the freeze a full pass still walks the 160–190k long-lived objects the process builds after boot (compiled-statement caches, validators, registries): 40–56 ms, about every third request at 2,400 rows — the residual tail in the frozen shape runs (large p95 ≈ 100 ms vs p50 ≈ 65). Two remedies, neither a startup line: column `select`s for the whole-window reads (a fifth of the objects, no instance↔state cycles; equivalence proven per read as PERF-002/003 did), or a higher gen-2 threshold (memory cost to measure first) | MEDIUM | CONFIRMED (logged shape run) | ACCEPT — P2 — **DONE (P2 batch 4)**: remedy 2 measured (three shape runs per setting, `GC-PAUSE-2026-09-06.md`): gen-2 threshold 10 → 100 takes the whole-window read's large p95 from 105–109 ms to 69–74 ms and its p95 growth ratio (2.47–2.81) down onto its p50 ratio (1.85–1.98), for +1–3 MB of peak RSS on 180; `gc_gen2_threshold` defaults to 100 (ADR-A33), applied with the freeze, `GC_GEN2_THRESHOLD=10` restores the interpreter default; the harness prints thresholds and peak RSS. Remedy 1 stays open for the dashboard as PERF-018 |
| PERF-018 (new, 2026-09-06) | The dashboard read is CPU-bound on the worker's loop: five statements per request hydrated to ORM rows and reduced in Python, so with eight in flight the last request waits for the other seven's Python work — 9.4–11.5× the serial p95 at ~21 req/s whatever the pool size (CONC-001's measurement). Remedy: column `select`s / SQL aggregates for the remaining reductions (the PERF-002 pattern, equivalence proven per figure), or a short-lived per-tenant cache of the dashboard figure; a second uvicorn worker spreads it but does not shrink it | MEDIUM | CONFIRMED (three pool sizes, three threshold runs — the ratio never moved) | ACCEPT — P2 — **OPEN, cause found (reference R4, 2026-09-08)**: the AP-inbox pushdown (PERF-DUCK-001) is done and correct but did not move the dashboard (9.16× → 10.60×, `docs/perf/TENANT-GUARD-2026-09-08.md` §1 — the harness seeds no approval steps, so that read reduced nothing either way); the profile attributes the per-request CPU to **PERF-019**, the tenant guard's ~100 loader-criteria options on every SELECT. **PERF-019 is the absolute cost, not this ratio**: with the guard attaching nothing the dashboard still reads 12.5× CONTENDED because the ratio is a shape property (twenty statements per request on one loop, eight in flight); this row's own remedy is fewer round trips per request (batch the fifteen section aggregates) or the cached figure, and its KPI is serial p95 + req/s, never `x` — **after R5 the dashboard reads 31.1 req/s at conc p50 187 ms (was 15.1 / 451), still `x` ≈ 11 by shape**. Still owed: the harness seeds no approval steps, so PERF-DUCK-001's own effect is unmeasured — the dataset that shows it is two pending steps per in-approval invoice (`submitted_by` NULL) under `--shape` (own row); no index is missing at realistic scale — `invoices (org_id, workflow_state, submitted_at DESC)` becomes worth measuring only once an org holds tens of thousands of open unassigned steps. The concurrency mode stays informational |
| PERF-019 (new, 2026-09-08) | The ORM tenant guard (`app/core/tenant.py::_apply_tenant_scope`) attaches one `with_loader_criteria` option per registered tenant model (103, plus 5 soft-delete criteria) to EVERY SELECT, so every statement — a `count(*)` on 300 rows included — carries a cache key with 104–125 bound values (the rendered SQL binds 8–13) and SQLAlchemy regenerates that key over all 108 options on every execute: the profile puts `_gen_cache_key` + option coercion at ~40 % of function time (250,720 key calls over 40 requests; profiler-weighted — the request costs 115 ms under cProfile against 63 without). **The number is the bound:** with the guard attaching no options (in-process measurement only — tenant AND soft-delete criteria off; RLS still scoping every row), every scenario's serial p95 falls 40–50 % and throughput rises 1.7–2.3× (dashboard 16.8 → 31.4 req/s, cash position 30.8 → 69.4, invoice list 60.9 → 116.2) — roughly half of every request in the product. It is the cost CONC-001/PERF-018 attributed to the dashboard's own reductions; the dashboard's ratio does not move (shape), so **acceptance is absolute serial p95 / req/s at N=8, never `x`**. Also learned from the captured guarded SQL: layer 2 reaches the FROM list, joins and aliases but NOT a correlated subquery — inside an EXISTS only the statement's own org predicate and RLS scope rows | MEDIUM | CONFIRMED (`docs/perf/TENANT-GUARD-2026-09-08.md` §2–3: cProfile + the bound measurement, same machine, same seed; the R4 panel's Performance and Security lenses) | ACCEPT — P2, **R5, its own batch with the Security lens** (the guard is isolation layer 2, ADR-0004): one criteria option on a shared tenant base (+ a separate one for `User`'s membership-existence predicate), or reachable-entity criteria. What must hold: `include_aliases=True`; lazy relationship loads stay covered (the guard returns early on `is_relationship_load` and relies on propagation from a parent that covers EVERY model); eager-load ON clauses; no tenant baked into any cache (two orgs, same statement, one process → different rows; `_compiled_cache` not growing per tenant); the registry read per call so `test_tenancy_parity.py`'s self-test can still fail; subqueries documented as uncovered. Proof: `test_tenancy_parity.py` (all three), `test_cross_tenant_isolation.py` (read/update/export/download), `test_rls.py` (raw query, users visibility) on Postgres, `test_recycle_bin_joins.py`, `test_tenant_registration.py`, `test_dashboard_statement_shape_is_bounded` tightened from 110 to a handful of options per SELECT (108 today), and a seeded cross-tenant leak. Never a silent change — **DONE (reference R5, 2026-09-08)**: one `with_loader_criteria` on the declarative `Base` per registry (tenant + soft-delete), per-class decision by attribute, tenant id as a typed bind parameter closed over; `tests/test_tenant_guard_mechanism.py` (two tenants share one compiled statement and never rows, aliases, membership-based `User` visibility with a member of two workspaces, cross-org lazy load returns None, coverage == registry, no-context unscoped), parity 98/98, RLS 5/5 + a Postgres-engine mechanism test, seven seeded violations red on the final tree (incl. the default-argument leak the first attempt actually had, and the aliased-`User` pointer predicate the panel found), shape ratchet ≤ 2 options; throughput ×1.6–2.1 on every harness scenario (`TENANT-GUARD-2026-09-08.md` §4); ADR-0004 addendum |
| PERF-020 (new, 2026-09-08) | The R15 shape gate measured the AP-inbox read on never-analysed tables: the first `--shape` run after the harness began seeding approval steps (R5 P-4) read the dashboard at **8.08×** against its 4.0 ceiling — the planner estimated ONE pending step for the tenant (statistics from when `approval_steps` was empty) and ran the correlated `NOT EXISTS` as a nested-loop anti join over every pending step (1,067 × 1,067 rows, 1.1 M buffer hits, 631 ms of an 822 ms request). `ANALYZE` alone: 84 ms per request, the statement below 2 ms, the gate at 1.94×. Production leaves that state within autovacuum's naptime (50 rows + 10 % trip an analyse) and estimates an unseen tenant from the distinct-count of the seen ones; CI's back-to-back seed-and-measure does not — a coin toss on autovacuum timing, the R15 gate's PERF-016 failure mode again. `docs/perf/SEED-STATISTICS-2026-09-08.md` | MEDIUM (a red gate on every CI run that lost the toss; no production request measured) | CONFIRMED (per-statement timing + EXPLAIN ANALYZE before/after `ANALYZE`) | ACCEPT — P2 — **DONE (R6)**: `_prepare_workspace` analyses the seven seeded tables after seeding (Postgres only), proven by a Postgres-engine test in CI's `postgres` job; the statement is unchanged on purpose (a statistics-independent `MIN(seq)` shape is the change to make only if a production tenant ever shows the nested-loop plan) |
| DB-021 (new, 2026-09-08) | The asyncpg engine sets no `command_timeout` (`core/database.py`): a Postgres that accepts connections and never answers (lock pile-up, network black hole, failover) parks every statement forever — the worker loop included, which the R6 liveness probe then restarts on every replica until the database answers (churn, not loss: in-flight jobs are reclaimed). Found by the R6 Adversarial lens (A6) | LOW–MED | LIKELY (configuration read; not reproduced) | DEFER — P3: a global statement timeout must be measured against the longest legitimate statement first (archive exports, integrity sweeps, the R15 reads at 20k rows) before it can be set; document the restart churn as the probe's honest behaviour meanwhile (done in R6) |
| DB-019 (new, 2026-09-07) | `alembic check` on Postgres reports `remove_fk`/`add_fk` for the three DB-018 column-list constraints (`fk_expense_items_project`, `fk_invoices_project`, `fk_issued_invoices_project`) and `fk_issued_invoices_issuer`: Postgres describes them as `SET NULL (project_id)`, SQLAlchemy's metadata as `SET NULL`, so autogenerate's text comparison never agrees. The repository's own parity gate (`tests/test_migrations.py`, DB-012) compares the real ON DELETE clauses and reads zero drift; CI's `alembic check` step (`ci.yml`, backend job) runs against SQLite, where both sides render `SET NULL`, so the gate is green there and blind to this Postgres-only rendering difference. Found while applying `e7f9a1c3d5b8` (reference R2) | LOW | CONFIRMED (local Postgres 16, `alembic check` output) | none — informational; anyone adopting `alembic check` as a gate must teach it the column-list form first | ACCEPT — P4 (record; no code) |

### DevOps / SRE (OPS)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| OPS-001 | CI auto-deploy runs a host-side `deploy.sh` that skips `vps-deploy.sh`'s preflight/backup/health | CRITICAL | CONFIRMED (repo docs); host content UNKNOWN | MODIFY — P1 docs + CI health assertion; host change is an owner action |
| OPS-002 | Green `deploy` ≠ site up (`up -d --build` exits 0 before health) | CRITICAL | CONFIRMED | ACCEPT — P1 CI post-deploy `/health/ready` assertion |
| OPS-003 | No image retention/pinning; `docker image prune -f` deletes the previous build → no rollback | CRITICAL | CONFIRMED | MODIFY — P2 push SHA-tagged images from CI; compose switch is an owner-coordinated cutover |
| OPS-004 | No scheduled backups anywhere in the repo | CRITICAL | CONFIRMED (repo); host snapshots UNKNOWN | ACCEPT — P1 `scripts/backup.sh` + cron docs; installation is an owner action |
| OPS-005 | `docker-compose.yml`+`prod.yml` publishes Postgres/MinIO with default credentials | CRITICAL | CONFIRMED | ACCEPT — P1 |
| OPS-006 | Base compose sets `ENVIRONMENT=production` without `INBOUND_EMAIL_SECRET` → `make up` crash-loops; `prod.yml` worker likewise | HIGH | CONFIRMED | ACCEPT — P1 |
| OPS-007 | Migrations run in the serving container's command with `restart: unless-stopped` | HIGH | LIKELY | MODIFY — P2 one-shot migrate service (requires compose v2 on host; owner-verified cutover) |
| OPS-008 | Observability built but unconnected; `/health/queue` not proxied; no log rotation | HIGH | CONFIRMED | ACCEPT — P1 nginx location + log rotation; monitoring stack P3 |
| OPS-009 | SSH deploy not atomic; four timeouts on record | MEDIUM | CONFIRMED | ACCEPT — P2 keepalives + health poll |
| OPS-011 | k8s manifests not deployable as committed | MEDIUM | CONFIRMED | ACCEPT — P2 |
| OPS-012 | Floating base-image tags | LOW | CONFIRMED | DEFER — P3 |
| OPS-013 | No GitHub Environment on `deploy` | LOW | CONFIRMED | ACCEPT — P2 (owner configures reviewers) |
| OPS-014 (new, 2026-09-08) | The queue gauges on `/metrics` (`invoiceiq_jobs{status}`, `invoiceiq_jobs_oldest_pending_seconds`, and R6's `invoiceiq_jobs_dead{kind}`) are per PROCESS: `set_queue_metrics` writes the default registry of whichever process ran `queue_health.snapshot` — each uvicorn worker shows its own last `/health/queue` call, and the worker process's per-tick refresh warms a registry nothing scrapes (no multiprocess mode). A recovered kind can read its old count on one API process for as long as no probe hits it. Pre-existing for the two older gauges; found by the R6 Adversarial lens (A8) | LOW | LIKELY (registry model read; not reproduced on a multi-worker deploy) | ACCEPT — P3: refresh the queue gauges inside `/metrics` (one snapshot per scrape) or enable prometheus multiprocess mode; until then route queue alerts off `/health/queue`'s body, which is computed per call (documented in R6) |

### QA / testing (QA)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| QA-001 | Eight Postgres-only concurrency/RLS tests execute in **no** CI job (postgres job lists 3 files) | HIGH | CONFIRMED (`ci.yml:132`) | ACCEPT — P1 + meta-test |
| QA-002 | SEPA `CtrlSum`/`NbOfTxs` have no multi-payee oracle | HIGH | CONFIRMED | ACCEPT — P1 |
| QA-003 | No API contract test SPA↔pydantic | HIGH | CONFIRMED | ACCEPT — P1 (OpenAPI drift gate) |
| QA-004 | Visual regression covers `/design` only | MEDIUM | CONFIRMED | DEFER — P3 |
| QA-005 | Extraction tested for provenance, not arithmetic | MEDIUM | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 5)**: `tests/test_p2_batch5_qa.py` pins the line maths (quantity × unit price, a stated amount winning, a thousands separator), the confirm route's per-line VAT rounding (7.875 → 7.88 summed to 291.38, not 291.37 rounded once on the subtotal) and that a stated e-invoice payable total is a reconcile warning, never the figure; three seeded arithmetic violations red. Found on the way: BE-021 |
| QA-006 | Dunning asserts counts, never the amount demanded | MEDIUM | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 5)**: the reminder body is asserted figure for figure — 750.00 outstanding after a 250.00 part payment, interest 7.40 at 12 % p.a. over 30 days (ACT/365), total now due 757.40; 968.00 after a partial credit note with no interest line; two seeded violations (gross total instead of balance; credit notes ignored) red |
| QA-007 | Cash application: one test; refusal figures untested | MEDIUM | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 5)**: every refusal's quoted figure asserted as the balances move — the receipt's free balance 300.00 → 200.00 → 0.00, the invoice's outstanding 1210.00 → 210.00, back to 1210.00 after a reversal, 605.00 after a partial credit note — plus the credit-note refusal and the cached `amount_paid`; two seeded violations red |
| QA-008 | Perf gate has no write scenario | LOW | CONFIRMED | DEFER — P3 |
| QA-009 | `RELEASE-READINESS.md` is stale (2403 tests, "no CI runners", closed defects marked open) and outside the docs-truth gate | MEDIUM | CONFIRMED | ACCEPT — P1 truth-up |
| QA-010 | CI retries mask flakes; two midnight-boundary assertions | LOW | CONFIRMED | ACCEPT — P3 |
| QA-011 (new, 2026-09-06) | The SQLite suite runs with foreign-key enforcement OFF: `app.core.database` sets `PRAGMA foreign_keys=ON` on the app engine's connections, but `tests/conftest.py` builds its own in-memory engine without that listener, so no test has ever exercised an `ON DELETE` rule or an FK refusal — the batch-3 RESTRICT tests passed vacuously until they switched the pragma on for their own connection | MEDIUM | CONFIRMED (three tests "DID NOT RAISE" with the constraint in place; raised once the pragma was set) | ACCEPT — P2 — **DONE (P2 batch 5)**: `tests/conftest.py` mirrors the app engine's `PRAGMA foreign_keys=ON` listener; the full suite under enforcement read **3124 passed / 4 failed / 17 skipped (54:36)** and every one of the four was a FIXTURE inserting a child of a parent that never existed (a receipt for org `"org"`, a fuel transaction for a made-up entity, an expense report for employee `"e1"`, a receipt-control row for a random entity id) — no product delete path orphaned anything. The four fixtures now create their parents over the product's own routes; the suite runs against the constraints production runs against from here on |
| QA-012 (new, 2026-09-08) | `vat-admin.spec.ts` "countries: each country shows its state…" asserted `getByText('LV', { exact: true })` page-wide while the claimant-documents panel prints the same country code; whether that panel had rendered by the time the assertion ran depended on the runner — CI #578 (R5's deploy run on main) read two elements, a Playwright strict-mode violation on both attempts, where #577 on the same commit read one. The R5 deploy was skipped by it | LOW (a green commit's deploy skipped by a test that measured render timing) | CONFIRMED (job log; the same sha green in #577) | ACCEPT — **DONE (R6)**: the locator is scoped to the "Refunding countries" card; 3× repeat green locally; not re-run — fixed and pushed (the R6 main push carries R5 to production) |

### Frontend / UX (FE)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| FE-001 | No `MutationCache.onError` backstop; 21 mutations fail with no surface at all | HIGH | CONFIRMED | ACCEPT — P1 |
| FE-002 | 401 interceptor hijacks accept-invite/verify/reset flows and drops the server's message; no `?next=` | HIGH | CONFIRMED | ACCEPT — P1 |
| FE-003 | No ErrorBoundary — one render throw whites out the app | HIGH | CONFIRMED | ACCEPT — P1 |
| FE-004 | 23 of 61 query pages render a 4xx as "no data" | HIGH | CONFIRMED | ACCEPT — P2 (ratchet, top three first) |
| FE-005 | Flash-empty / lie-on-error lists | MEDIUM | CONFIRMED | ACCEPT — P2 with FE-004 |
| FE-006 | 83 hand-rolled tables vs `DataTable` | MEDIUM | CONFIRMED | DEFER — P3 ratchet gate |
| FE-007 | 33 tables with no scroll container; 4 clipped by `overflow-hidden` | MEDIUM | CONFIRMED | ACCEPT — P1 for the four clipped |
| FE-008 | 10 `window.confirm`; 13 unconfirmed deletes; `Customers` "archive" label calls DELETE | HIGH | CONFIRMED | ACCEPT — P1 for the unconfirmed deletes and the verb mismatch |
| FE-009 | Errors rendered in success-green, no `role="alert"` | MEDIUM | CONFIRMED | ACCEPT — P2 |
| FE-010 | Success near-silent | MEDIUM | LIKELY | DEFER — P3 |
| FE-011 | ~114 controls without programmatic labels | MEDIUM | LIKELY | ACCEPT — P2 (gate inverse pass) — **DONE (P2 batch 6)**: `scripts/check-controls.mjs` is the inverse of check-labels — every input/select/textarea must be labelled by aria-label, a htmlFor'd id, a wrapping label or a label-bearing wrapper; a placeholder is not a label. It found 91 on the pre-batch tree (the audit estimated ~114); a control whose only label was a placeholder now carries that text as `aria-label` (never where a real label already existed — an aria-label would override it), the rest (table cells, hidden file inputs, filter selects, per-row selects, a read-only link field) were named by hand ("Line 1 description", "Item 2 amount", "Select INV-7"…); the gate carries ZERO debt and is in CI; 2 e2e proofs (getByLabel through the invoice line cells; the invitation link) |
| FE-012 | Only 11 `<form>`s; `required` inert | MEDIUM | CONFIRMED | DEFER — P3 |
| FE-013 | 30 mutation buttons without pending guard | MEDIUM | CONFIRMED | ACCEPT — P2 — **DONE (P2 batch 6)**: 31 buttons (the audit counted 30) fired a mutation with no pending guard; every one now carries `disabled={X.isPending}` (or `loading`), and `scripts/check-pending.mjs` fails the build on a new one; e2e holds a POST open and proves the button is disabled + aria-busy and a second click sends nothing |
| FE-014 | Three button systems | MEDIUM | CONFIRMED | DEFER — P3 |
| FE-015 | 1,500-line pages | MEDIUM | CONFIRMED | DEFER — P3 (only where a bug is fixed) |
| FE-016 | Unbounded lists client-side | MEDIUM | LIKELY | DEFER — P3 with PERF-010 |
| FE-018 | Zero i18n readiness; en-IE/en-GB inconsistency | MEDIUM | CONFIRMED | MODIFY — P2 centralise locale; i18n is an owner decision — **DONE (P2 batch 6)**: `LOCALE = "en-IE"` in `src/lib/format.ts` with `formatNumber/formatDate/formatDateTime/formatTimestamp/formatTime/formatWeekday`; the twelve browser-locale call sites and the en-GB date formatters route through it; `scripts/check-locale.mjs` refuses any page that names a locale or calls toLocale*/Intl.* itself; the i18n decision is written up as DECISIONS-NEEDED §21 with three costed options and a recommendation; e2e proves a rendered timestamp's shape |
| FE-019 | 4xx retried; no request cancellation | LOW | CONFIRMED | ACCEPT — P2 (one-line retry predicate) — **DONE**: the retry predicate has been in `main.tsx` since the P1 set (`retry: (count, err) => count < 1 && isUnexpectedError(err)` — never a 4xx); the register had not been updated. The cancellation half is NOT taken and is recorded as such: react-query already discards a stale result by key, and threading `signal` through ~200 read call sites buys nothing measurable for a LOW row |
| FE-020 | Bundle headroom 9% | LOW | CONFIRMED | no action |
| FE-021 | `/design` fixtures ship to production | LOW | CONFIRMED | ACCEPT — P2 (gate on DEV) — **DONE (P2 batch 6)**: the `/design` showcase's lazy imports and routes sit inside `import.meta.env.DEV`; a production build emits no design chunk (verified: zero `design`/`gallery` assets in dist) and `scripts/check-bundle.mjs` fails on the first fixture organisation's name in any production chunk; the e2e/visual suites run the dev server and keep their surface |

| FE-022 (new, 2026-09-08) | `useFocusTrap` depended on `onEscape` as well as `active`; every `Modal` passes an inline closure, so every re-render — every keystroke into an input whose state the parent holds — re-ran the trap: focus went back to the pre-open element and then to the first focusable (the header's Close button). Reproduced under jsdom and on the live app; pre-existing for every form modal (`Currencies`, `TaxCodes`, `CostObjects`), surfaced by the go-to-page palette where typing is the feature. A second cause on the dev server: the Portal detached its node on StrictMode's simulated unmount, dropping focus to `<body>` | MEDIUM | CONFIRMED (R4 panel Adversarial A-1 / QA Q-1; live probe) | **DONE (R4)** — callback read through a ref, trap depends on `active` alone; `Modal.initialFocusRef`; Portal defers removal by a microtask and cancels it on remount; keyboard-only palette e2e + a form-modal typing e2e as the seeds |

### Product / commercial (PROD)

| ID | Title | Sev | Certainty | Decision |
|---|---|---|---|---|
| PROD-001 | A declined subscription payment sets `org.status=suspended` → every request 401s, including `/billing/*`, so the customer cannot reach the screen to fix the card; no email | HIGH | CONFIRMED (`billing.py:251`, `billing_provider.py:136-141`, `deps.py:116-122`) | MODIFY — P1: billing routes reachable for a suspended org's `BILLING_MANAGE` holder (bug fix); grace/dunning ladder is an **owner decision** |
| PROD-002 | The documented 14-day trial does not exist in code; signups get full features forever | HIGH | CONFIRMED | **OWNER DECISION** (business behaviour) — P1 to ask |
| PROD-003 | Nav gated on the 4-tier ladder while the API gates on the 8-role matrix → dead links for six roles | HIGH | CONFIRMED | ACCEPT — P1 (drive nav off served permissions) |
| PROD-004 | Block-at-cap ships with no warning, no upgrade path, copy says "ask a platform operator" | MEDIUM | CONFIRMED | ACCEPT — P2 |
| PROD-005 | EveryPay customers cannot cancel/change plan/get a receipt | HIGH | CONFIRMED | DEFER — P2 gated on PROD-006 (owner) |
| PROD-006 | Seller-of-record VAT undecided; subscription invoices at 0% | HIGH | CONFIRMED | **OWNER DECISION** (already §2 in DECISIONS-NEEDED) |
| PROD-007 | No DPA/ToS/privacy policy; nothing accepted at signup | HIGH | CONFIRMED | **OWNER DECISION** (legal) |
| PROD-008 | Demo credentials printed on the production login page | MEDIUM | CONFIRMED | ACCEPT — P1 (gate on DEV) |
| PROD-009 | No workspace deletion / whole-tenant export; ex-client archive export unbuilt (= WO-AI) | HIGH | CONFIRMED | ACCEPT — P2 (WO-AI DONE 5f63ca1: the ex-client archive export; workspace deletion / whole-tenant export remain open) |
| PROD-010 | Three role vocabularies; manual documents four roles | MEDIUM | CONFIRMED | ACCEPT — P2 (labels + manual grid) |
| PROD-011 | Onboarding "Invite your team" links to `/settings` (Team is `/team`); any invitation row ticks the step | MEDIUM | CONFIRMED (`onboarding.py:57-58,88`) | ACCEPT — P1 (trivial) |
| PROD-012..020 | Support surface, i18n, pricing/PLANS drift, audit caveats, admin console, invite email, residency, SOC2/SAML, metering | — | CONFIRMED | owner decisions + P2/P3 |

## 2. Cross-agent debate (HIGH/CRITICAL items)

Format: proposal → support → challenge (Adversarial Reviewer) → alternative → decision.

**SEC-001 unusable-password backdoor.** Proposal (Security): store `NULL`/non-hash for SSO users, refuse password login. Support (Backend, Lead): confirmed by reading; the literal is in a public repo. Challenge: the per-account lockout and rate limit? — irrelevant, the first attempt succeeds. Is a data migration needed? — yes: existing rows carry the hash; bcrypt is salted so rows must be found by verifying against the two literals. Alternative: make the column nullable — a wider schema change touching every reader of `hashed_password`. **ACCEPT, MODIFIED**: keep `NOT NULL`, store the non-modular-crypt sentinel `"!"`, refuse it explicitly in `verify_password` (not only via the library's exception), migrate existing rows by verification, add the route-level reproduction test that fails on the old code. P0.

**SEC-002/ARCH-001 forwarded IPs.** Proposal: pin `--forwarded-allow-ips` to the proxy CIDR. Challenge: the compose network's subnet is not stable across hosts, so a CIDR literal in the Dockerfile is wrong for someone. Alternative (Lead): drop `--proxy-headers` entirely and let the app's own `_client_ip` (already correct: rightmost-N) be the single authority, with `TRUSTED_PROXY_COUNT=1` set in every deployment file. Both audit and rate limit already call the same helper. **ACCEPT, MODIFIED.** Structural test: no deployment file may carry `forwarded-allow-ips '*'`.

**PROD-001 lockout on declined payment.** Proposal (Product): grace period + dunning ladder + banner. Challenge: that is a change to fundamental business behaviour (when a non-paying tenant loses access) and the owner has not decided it; the AUTONOMY RULE forbids deciding it internally. But one part is a defect regardless of policy: the person who can fix the card cannot reach the billing screen. **MODIFY**: ship the reachability fix (a suspended org's `BILLING_MANAGE` holder may call `/billing/*`); put the grace policy to the owner with the recommended default.

**DB-002 numbering collision.** Proposal (DB): change the unique constraint to `(org_id, issuer_id, number)` + unique prefixes. Challenge: a constraint swap on the legal-document table is a migration that can fail mid-deploy on the auto-deploy path (OPS-002/007), and the org-level constraint is not wrong — it is stricter. Alternative: make prefixes unique per org at the service layer and give a new issuer a distinct default; keep the org-level constraint as the backstop. **MODIFY**: service-level now (P1); the DB constraint with a pre-flight report is P2.

**DB-005 supplier invoice number uniqueness.** Proposal: partial unique index per vendor. Challenge: suppliers legitimately reuse numbers (per-year series, credit notes sharing the invoice's number, "1" from a sole trader every month); a hard constraint would refuse real documents at intake, and the `duplicates` advisory service exists for exactly this. **REJECT** the constraint; P3 to tighten the advisory.

**OPS-001..004 deploy path.** Proposal (DevOps): point the host's forced command at `vps-deploy.sh`; one-shot migrate service; SHA-tagged images; scheduled backups. Challenge: the host's `/root/deploy.sh` is outside the repo — the repo cannot change it, only document it; the compose `service_completed_successfully` condition needs compose v2 on the host (UNKNOWN); switching `build:` to `image:` is a cutover that must be coordinated with the owner. Alternative: land everything that is repo-side and safe now — CI asserts `/health/ready` after the SSH step (turns the silent-outage mode into a red job), docs carry the corrected `deploy.sh`, `scripts/backup.sh` exists and its cron line is documented — and mark the host-side steps BLOCKED on the owner. **MODIFY** as stated.

**ARCH-005 god-controllers.** Proposal: extract an application layer. Challenge: 5,235 lines of working, tested money code; a layer extraction is exactly the "architectural purity" rewrite RULE 5 forbids; no defect in this audit is caused by the layering itself. **DEFER**: record as debt; extract a use-case function only when a flow is touched for a defect (credit-note proration and expense decision first).

**ARCH-002/PERF-013 refuse `local` storage in production.** Proposal: production validator rejects `storage_backend=local`. Challenge: the live VPS runs `local` on a shared volume by design (documented in the compose file); the validator would take production down on the next deploy. **MODIFY**: fix the k8s config; add a startup warning (not a refusal) when `local` + `WEB_CONCURRENCY>1` without an explicit acknowledgement. P2.

**QA-001 orphaned Postgres tests.** Proposal: run the whole suite under the Postgres URL. Challenge: that doubles CI time for eight tests. Alternative: list the six missing files and add a meta-test asserting every `pg_only` file is named in the workflow. **ACCEPT, MODIFIED.**

**QA-003/ARCH-011 contract gate.** Proposal: OpenAPI codegen for `types.ts`. Challenge: a 2,762-line hand file cannot be swapped in one change without breaking every page; codegen is a project. Alternative: the ERD pattern — check in `openapi.json`, fail on drift — closes "the server changed and nobody noticed" today; codegen follows. **ACCEPT, MODIFIED.**

**FE-001/002/003.** No challenge survived: each is a one-file backstop with no behaviour change for pages that already handle the case. **ACCEPT.**

**PROD-002 trial.** Proposal: build the 14-day clock. Challenge: whether free signups should lose features after 14 days is the owner's pricing decision, and the code currently contradicts three documents either way. **OWNER DECISION**; engineering prepares nothing until answered, but the docs must stop promising it.

**PROD-008 demo credentials.** Challenge: the owner may demo from production using them. Response: gating the *display* on the dev build removes nothing from the owner, who knows the credentials; a security questionnaire screenshot does not. **ACCEPT.**

## 3. Master priority backlog

Statuses: NOT STARTED · INVESTIGATING · IMPLEMENTING · BLOCKED · TESTING · REVIEW · DONE · REJECTED · DEFERRED.

| ID | P | Task | Owner | Deps | Risk | Cx | Validation | Status |
|---|---|---|---|---|---|---|---|---|
| SEC-001 | P0 | Unusable-password sentinel + retire legacy hashes by migration + route reproduction test | Security/Lead | — | LOW (sentinel is a strict subset of old behaviour) | S | 6 new tests incl. the pre-fix 200 → post-fix 401 reproduction; full regression | NOT STARTED |
| SEC-002 | P1 | Drop `--proxy-headers '*'`; `TRUSTED_PROXY_COUNT=1` in all deployment files; structural test | DevOps/Security | — | MED (client IP for audit changes source) | S | test asserting no `forwarded-allow-ips '*'`; rate-limit spoof test | NOT STARTED |
| QA-001 | P1 | Add six pg-only files to CI postgres job + meta-test | QA | — | LOW | S | meta-test; CI run | NOT STARTED |
| BE-001/002/003 | P1 | Jobs: guard `retry` to dead/failed; dead-letter on stale reclaim at max attempts; savepoint in `enqueue` | Backend | — | MED (queue semantics) | M | 3 regression tests each proven to bite | DONE 3e9358c |
| BE-005 | P1 | Commit the charge claim before `charge_mit` | Backend | — | LOW | S | patched-provider double-run test | DONE 0e41463 |
| PROD-001 | P1 | Billing routes reachable for a suspended org's billing manager | Backend/Product | — | MED (auth dependency variant) | M | route tests: suspended org → `/billing` 200, other routes 401 | DONE f1dbfd8 |
| WO-AE | P1 | IdP role vocabulary (in flight) | Lead | — | LOW | M | 11 backend + 5 e2e | DONE d398d00 |
| SEC-003/004, OPS-008 | P1 | nginx: CSP, repeat headers in `/assets/`, proxy `/health/queue`; compose log rotation | DevOps | — | MED (CSP can break the SPA) | S | serve `dist/` under the CSP locally and run smoke e2e | DONE 63a6408 |
| OPS-002 | P1 | CI post-deploy `/health/ready` assertion | DevOps | — | LOW | S | workflow lint; next main run | DONE 63a6408 (host var DEPLOY_HEALTH_URL: owner) |
| OPS-004 | P1 | `scripts/backup.sh` + documented cron; host install BLOCKED on owner | DevOps | — | LOW | S | script dry-run against scratch PG | DONE 63a6408 (cron install: owner) |
| OPS-005/006 | P1 | compose: hide db/minio ports + require passwords in prod.yml; `ENVIRONMENT` default dev in base; worker secret in prod.yml | DevOps | — | LOW (not the live path) | S | `docker compose config` renders; validator test | DONE 63a6408 |
| OPS-001/010, QA-009 | P1 | Docs: corrected `deploy.sh`, SECRET_KEY/KEK consequence, `.env` in backup, RELEASE-READINESS truth-up | DevOps/QA | — | LOW | S | docs-truth gate extended to RELEASE-READINESS | DONE 5ac16f7 |
| DB-004 | P1 | Unique `(org_id, sha256)` on `bank_statements` + IntegrityError→ReconError | DB | — | LOW (pre-flight) | S | duplicate-import test; migration round-trip | DONE 2dc9851 |
| DB-001 | P1 | `billing_payments.amount_eur` → `Numeric(14,2)`; Decimal in service/route | DB | — | LOW | S | migration round-trip; exact-equality test | DONE 02ed215 |
| DB-002 | P1 | Unique invoice/credit prefix per org at the service; distinct default for a new issuer | Backend/DB | — | LOW | S | second-issuer collision test | DONE 1e3b69e |
| Blocking I/O | P1 | `run_in_threadpool` for `filesec.check` (11 sites), Stripe SDK calls, `fx._fetch`, report writers, bcrypt in auth routes; clamd timeout | Backend/Perf | — | LOW | M | thread-identity tests | DONE ac313de |
| PERF-004 | P1 | Harness seeds `workflow_state` + `IssuedInvoice`/`Payment`; re-measure | Perf | scratch PG | LOW | S | harness run; ceilings re-justified | DONE 3196e2c (+ PERF-002/003 fbc14df, PERF-005/010 0c24bc8) |
| QA-002 | P1 | SEPA multi-payee `CtrlSum`/`NbOfTxs` oracle | QA | — | LOW | S | test | DONE (this commit) |
| QA-003 | P1 | `openapi.json` drift gate (ERD pattern) | QA/Arch | — | LOW | S | test | DONE (this commit) — no drift found; snapshot at docs/api/openapi.json |
| DB-012 | P1 | Parity test compares uniques/checks/indexes/FK ondelete | DB/QA | — | MED (may reveal drift) | S | test | DONE (this commit) — zero constraint drift found at head e6a8c0d2f4b6 |
| FE-001/002/003 | P1 | MutationCache backstop; public-route 401 allowlist + `?next=`; ErrorBoundary in shell | Frontend | — | LOW | S | e2e: silent-mutation toast; accept-invite 401 message; boundary renders | DONE (this commit) |
| FE-007/008 | P1 | Four `overflow-hidden`→`overflow-x-auto`; confirm the unconfirmed deletes; `Customers` verb | Frontend | — | LOW | S | e2e | DONE (this commit) — 17 deletes confirmed, not 13 |
| PROD-003 | P1 | Nav visibility from served permissions | Frontend/Product | — | MED | M | e2e per role | DONE (this commit) — `/auth/me` serves `permissions`; every nav item names its router's permission; in-page controls still on the ladder mirror (P2, PROD-010) |
| PROD-008/011 | P1 | Demo creds gated on DEV; onboarding href `/team` + invitation status filter | Frontend/Backend | — | LOW | S | tests | DONE (this commit) |
| WO-AF | P1 | Statement-byte vaulting | Lead | — | LOW | M | tests | DONE (this commit) — `docs/plan/plan-a/wo/WO-AF-statement-byte-vaulting.md` |
| PROD-002/006/007, grace policy | P1 | Owner decisions | Owner | — | — | — | recorded in DECISIONS-NEEDED | BLOCKED (owner) |
| OPS-001/003/007 host side | P1 | Host `deploy.sh` → `vps-deploy.sh`; image cutover; migrate service | Owner+DevOps | docs above | MED | M | manual deploy rehearsal | BLOCKED (owner) |
| R2-C1 | P2 | BE-005 residual: the dedupe key is per DAY, so a settled charge whose response was lost is charged AGAIN the next day (not "missed once") — key the claim on the period paid or reconcile by `order_reference` first | Backend | Phase 12 review | MED | S | patched-provider test: raise after settle → next-day run charges 0 | DONE (P2 batch 1) — claim keyed on the PERIOD paid; lost-response case stalls as duplicate with the order_reference logged for reconciliation |
| R2-C4 | P2 | A suspended owner who downgrades to Free stays suspended (only a settled payment clears `status`) — belongs with DECISIONS §18 | Backend/Owner | Phase 12 review | LOW | S | route test | BLOCKED (owner §18) |
| R2-B2 | P2 | FE-001 backstop double-surfaces on 6 pages that render `mutation.error` inline without `onError` (Billing, Issuer, Sessions, VerifyEmail, ResetPassword, Portal) | Frontend | Phase 12 review | LOW | S | e2e: one surface | DONE (P2 batch 1) — `meta: { silent: true }` on the 7 inline-rendering mutations; e2e asserts one surface |
| R2-A3 | P2 | 43 page-level controls still gate on the stored-role mirror (`hasVatPerm`/`isAdminOrAbove`) while the nav reads served permissions (= PROD-010 scope) | Frontend | Phase 12 review | MED | M | e2e per role | DONE (P2 product set) — 20 pages on `hasPerm(...)`; e2e: role `user` + served `settings.manage` sees "New tax code", role `admin` without it does not, no served list → mirror (ADR-A25) |
| R2-T2 | P2 | Threadpool test covers `ensure_customer` only; `start_checkout`, `create_portal_url`, `report_usage` unasserted | QA | Phase 12 review | LOW | S | test | DONE (P2 batch 1) |
| R2-A2 | P3 | `cash_position` imports underscore-private helpers from `ap_aging` | Backend | Phase 12 review | LOW | S | — | DEFERRED |
| R2-C3 | P3 | `reclaim_stale` SELECT-then-UPDATE window vs a worker finishing in between (old code had the sibling race) | Backend | Phase 12 review | LOW | S | pg race test | DEFERRED |
| P2 batch 1 | P2 | BE-004 (enqueue answers 200 `deduplicated`), BE-016 (DB failures in `audit.record` propagate), SEC-007 (login password ≤200), SEC-011 (pip-audit CI step; pypdf 6.15.0→6.16.1 for three CVEs; PYSEC-2026-1325 ignored with reason) | Backend/DevOps | — | LOW | S | tests per row | DONE (P2 batch 1) |
| P2 batch 2 | P2 | SEC-005 (portal/calendar tokens masked in the app access log and nginx origin log), BE-009 (inbound email Message-ID idempotency: column, index, migration f1a2b3c4d5e6, both webhook routes), BE-010 (the four bank-file exports are POST; GET is 405; SPA + tests follow) | Backend/DevOps/Frontend | — | LOW–MED (verb change is client-visible) | S–M | 3 + 1 + 1 tests | DONE (P2 batch 2) |
| P2 product set | P2 | FE-004/005 (Issue, SupplierCosts, ExpenseDetail error states + `check-query-errors.mjs` ratchet listing the 20 pages still without one, CI step), FE-009 (42 inline error boxes `role="alert"`), PROD-010 (business-name role labels, MANUAL §1.2 8-row grid with stored key + permission count, `test_prod010_role_docs.py`), PROD-004 (402 copy names the owner and Plan & billing; Upload pre-cap notice with the upgrade path), R2-A3 (above) | Frontend/Product/Docs | — | LOW (wording + visibility; server behaviour unchanged) | M | 8 e2e + 5 backend tests; 4 seeded violations proven | DONE (147fc2e) — CERTIFIED: full backend 3086/15/0, e2e 455/0, CI #537 green, on main (CI #538 deploying) |
| Queue tail | P2 | WO-AJ (receipt-control run as job kind `transport.receipt_control` + panel button), WO-AH (`due_soon` view + route + dashboard card), WO-AG (`vat_country_requirements` + readiness helper + routes + panels), WO-AI (`archive_exports` + zip build job + owner route + public request/download + page), BE-011 (payment-run list grouped count + bound), BE-012 (review queue grouped counts), concurrency measurement mode (harness `--concurrency`, CI informational step, `docs/perf/CONCURRENCY-2026-09-06.md` — datapoint 0 recorded verbatim from CI #543 with its two artefacts, plan cap and per-token limiter, both removed from the measurement; findings CONC-001/002) | Backend/Frontend/DevOps/Docs | — | LOW–MED (two new tenant tables with RLS; one public POST/GET pair, no-enumeration) | L | 18 backend + 2 probes + 13 e2e + 1 structural; 5 seeded violations proven | DONE (595ab85) — batch-end regressions + CI pending; first concurrency datapoint pending from CI |
| PERF-016 | P1 | Freeze the startup heap at the end of `lifespan` (`app/core/gc_tuning.py`); harness freezes once per process and reclaims the seed's garbage before measuring; growth table prints the p50 ratio beside the gated p95; `docs/perf/GC-PAUSE-2026-09-06.md` | Lead | — | LOW (one documented CPython call, after every startup write; no behaviour change) | S | 4 tests (functional freeze, lifespan order, harness order in both modes, gate stays on p95); shape ×3 locally 2.66/1.50/2.45 (was 4.25–12.24); full backend 3111/15/0 (44:35); CI on the branch; main | DONE — see ledger |
| P2 batch 3 | P2 | Data integrity + correctness: DB-007/008/009 (RESTRICT), DB-017 + DB-018 (composite issuer link; column-list SET NULL on the project links), DB-011 (six financial CHECKs with a refusing pre-flight), DB-013 (AP ledger verified), BE-014 (`issuer.lock` never commits), BE-020 (inbound bounds), BE-008 (register corrected — done in `ac313de`); migration `c4d6e8f0a2b4`; seed corrected; OpenAPI snapshot | Lead | — | MED (one migration that can REFUSE on unexpected state values — the backend container gates uvicorn on it; the refusal names the rows; rollback = correct the rows, restart) | M | 11 SQLite tests + 1 pg-only test (CI postgres job list), each seeded red (5 seeds); `alembic check` clean on SQLite; migration applied on local Postgres 16 and constraints read back; full backend 3120/2/16 with both failures the CHECK biting on tests that stored a foreign claim status on purpose (updated to prove the refusal, then bypass the CHECK on their SQLite connection); CI #550 all green; CI #551 all nine jobs incl. deploy | DONE 04a6f07 — production 2026-09-06 15:51 UTC, migration pre-flight passed |
| P2 batch 4 | P2 | Performance: PERF-001 (reconciliation amount window in SQL), PERF-006 (P&L summary in a fixed number of statements — `_live_figures_bulk`, `_invoice_costs_bulk`, `estimated_revenue_bulk`, shared `_compose`), PERF-009 (integrity routes count first; above `INTEGRITY_SYNC_LIMIT` = 500 they queue the job and answer 202; SPA card handles it), ARCH-013 / PERF-011 (daily sweep: advisory lock, one prefetch, one commit), PERF-017 (gen-2 threshold measured — 10 → 100 default, ADR-A33; harness prints thresholds + peak RSS), CONC-001 concluded by the pool measurement (pool 2 / 8 / 20: throughput flat — CPU on one loop, not the pool) → PERF-018 opened; OpenAPI snapshot | Lead | — | LOW–MED (no migration; one runtime GC setting reversible by env; a route that can answer 202 where it answered 200, handled by its only caller) | M | 7 SQLite tests + 1 pg-only test (CI postgres list), six seeded violations red; targeted suites 100 / 0; frontend tsc + gates clean; full backend **3126 passed / 2 failed / 17 skipped (45:07)** — the OpenAPI snapshot (regenerated) and an order-dependent assertion in the new scheduler test (the FX carrier is the lowest org id and moved when a lower UUID registered; test now computes it), both re-run green ×3 | DONE 584fb21 — CI #553 green (eight active jobs), CI #554 all nine incl. deploy; production 2026-09-06 18:29 UTC; the runner read the R15 gate at 1.85× / 1.92× with the new threshold (2.59× / 2.50× before) |
| P2 batch 5 | P2 | QA group: QA-011 (the SQLite suite enforces foreign keys — four vacuous fixtures corrected, no product orphaning path found), QA-005 (extraction arithmetic: line maths, per-line VAT rounding, stated e-invoice total is a warning), QA-006 (the reminder's demanded figures after a part payment and a partial credit note, with interest), QA-007 (every cash-application refusal's quoted figure as the balances move); BE-021 found and fixed (a garbled amount cell parsed as 0.00 silently → quantity × unit price + a per-line warning) | QA / Lead | — | LOW (one parser fallback that turns a silent 0.00 into the arithmetic an empty cell already gets, with a warning; the rest is tests) | S | 8 new tests (`tests/test_p2_batch5_qa.py`), seven seeded violations red; neighbouring suites 49 / 0; QA-011 sweep 3124 / 4 / 17 with the four explained and fixed; final full backend regression on the batch tree **3136 passed / 0 failed / 17 skipped (56:13)** | DONE ed67fb5 — CI #556 green (eight active jobs), CI #557 all nine incl. deploy; production 2026-09-07 10:19 UTC |
| P2 batch 6 | P2 | Frontend group: FE-011 (91 unlabelled controls labelled; inverse a11y gate at zero debt), FE-013 (31 mutation buttons guarded; gate), FE-018 (one locale in format.ts; gate; i18n → DECISIONS §21), FE-019 (already done in the P1 set — register corrected; cancellation half declined with the reason), FE-021 (/design DEV-only; bundle gate) | Frontend | — | LOW (labels and disabled states; a locale constant that was already the money locale; a route tree that loses its showcase in production) | M | 3 new gates in CI + package.json lint, 4 e2e (`fe-frontend-set.spec.ts`), five seeded violations red (label removed, guard removed, browser-locale call, design routes outside DEV, e2e label); tsc + all frontend gates green; production build 424.7 kB raw / 124.4 kB gz (budget 460 / 135); full e2e 472 passed / 0 failed (4.7m) | DONE c4739d1 — CI #560 green (eight active jobs), CI #561 all nine incl. deploy; production 2026-09-07 15:30 UTC |
| P2 set (remaining) | P2 | DB-014, ARCH-002/008, PERF-018 (the dashboard's per-request CPU — CONC-001's remedy), OPS-003/007/009/011/013, FE-004's ratchet (20 pages still listed), PROD-005/009 (PROD-009's ex-client export half = WO-AI DONE; workspace deletion / whole-tenant export still open) | various | | | | | IN PROGRESS by batches — batches 3 (data integrity + correctness), 4 (performance), 5 (QA) and 6 (frontend) delivered; what remains is the ops/config group, DB-014, PERF-018 and FE-004's ratchet |
| Reference integration R3 (2026-09-07) | P1/P2 | Security governance / supply chain from the PSC, Paperless, Stirling, Flask, Scrapling and Twenty cycles: all 33 `uses:` lines (25 pre-existing + 8 in the new workflow) pinned to upstream-verified commit SHAs with a stdlib checker in CI and a repository test; read-only token permissions and `persist-credentials: false` everywhere (the VR-baseline job's unneeded `contents: write` dropped); `security-supply-chain.yml` (actionlint, zizmor, TruffleHog verified-secrets PR scan, dependency-review); the security-control register `docs/security/security-controls.json` + gate + generated view (SEC-SSRF-001 verified by R1, SEC-SC-001 by this batch, SEC-JWT-001 new); PR template with AI disclosure; JWT signing key separated from `SECRET_KEY` with staged verify-only rotation (SEC-JWT-001); `AI-ENGINEERING-POLICY.md`; ADR-0030 AI-agent action safety; migration plan-before-apply merged into `engineering-rules.md` §9; SEC-GOV-001 disclosure decision doc (SECURITY.md stays the placeholder — owner §22) | Lead | R1, R2 | LOW–MED (CI-only changes + an auth-code path that is byte-identical when `JWT_SIGNING_KEY` is unset) | M | 114 targeted incl. 17 gate tests; review panel (seven lenses) → SEC-012 fixed, zizmor cooldown, VR-baseline write restored, compose forwarding; 13/13 seeded red; zizmor 1.30.0 exit 0; full backend **3244 passed / 20 skipped / 0 failed (54:04)**; CI recorded in DASHBOARD | TESTING → CI |
| Reference integration R4 (2026-09-08) | P2 | Performance / product from the DuckDB, Twenty and Flask cycles + the R2 review's Billing follow-up: PERF-DUCK-001 AP-inbox pushdown (one projected SELECT, prebuilt with bound parameters after the re-measurement caught per-call construction) · PERF-DUCK-002 re-measure → **PERF-019 found** (the tenant guard's 108 loader options on every SELECT ≈ half of every request; R5) · TW-P2-01 "Go to page" palette over the filtered nav · FLASK-P3-01 inventory (1 warning in 3,266 tests) → `error` · Billing activating state (bought-plan settle, query cleanup, truthful 90 s timeout) · FE-022 focus-trap root cause + Portal StrictMode detach fixed · DB-020, BE-022, SEC-013 recorded | Lead / Frontend / Perf | — | LOW–MED (one read reshaped with a differential probe proving parity; a focus-trap change every modal inherits, covered by 481 e2e; a pytest policy that makes a future upstream deprecation red by design) | M | 12 + 6 backend, 27 e2e touched, 16/16 seeded red, full e2e 481 / 0, full backend **3248 passed / 20 skipped / 0 failed / 0 warnings (57:26)**; review panel eight lenses, two FAILs fixed before certification; CI #574 green, CI #575 deployed | DONE 078d775 — production 2026-09-08 12:34 UTC |
| Reference integration R6 (2026-09-08) | P2 | The R2 review's ops deferrals — a worker LIVENESS probe (`app.worker_probe` on a file the loop touches every tick and every lease heartbeat; compose healthcheck + k8s livenessProbe on the pool and both lanes; deliberately NOT `/health/queue`, which stays the paging check) and dead-letter depth by job kind (`dead_by_kind`, `invoiceiq_jobs_dead{kind}` with stale-label reset) · BE-022 one subscription per workspace (409 before the provider is reached; the Portal button) with the three holes the panels found closed — BE-023 the in-app free switch (flagged business change, DECISIONS §25), the pre-webhook window (a persisted in-flight Checkout marker settled/abandoned by the provider's events, the session expiring alongside), the canceled-id lockout (the webhook clears it) — plus provider-specific `has_subscription` and an honest module-loss dialog · R5 P-4 the harness seeds approval steps, which found **PERF-020** (the gate measured an unanalysed-table plan at 8.08×; the harness now analyses its seed; 1.94× with the inbox's rows) · found: BE-024, OPS-014, DB-021 | Lead / DevOps / Security / Product | — | MED (two billing paths a subscriber could take now answer 409 — both were money-losing; a probe that restarts a wedged worker; no migration; rollback = revert the commit) | M | 149 + 41 backend targeted, 11 e2e touched, 24/24 seeded red, full e2e 487 / 0, full backend **3288 passed / 22 skipped / 0 failed / 0 warnings (43:57)**; review panel seven lenses in two agents, every required follow-up applied before certification | TESTING → CI |
| Reference integration R5 (2026-09-08) | P2 | PERF-019 — the tenant guard's mechanism: one `with_loader_criteria` on the declarative `Base` per registry (tenant + soft-delete), per-class decision by the mapper's columns, tenant id as a typed bind parameter closed over, alias-aware predicates, reserved bind name; reach rule and rollback path in ADR-0004's addendum; `test_rls.py` hygiene | Lead / Security | — | MED (isolation layer 2's mechanism replaced — every proof the Security lens named passes on SQLite and Postgres; RLS unchanged behind it; revert + restart is the rollback) | M | 15 mechanism tests (+ Postgres proof in CI's postgres job), parity 98/98, RLS 5/5, guard SQLite suites 69/0, 7/7 seeded red on the final tree, throughput ×1.6–2.1 on every harness scenario; review panel six lenses, one CONFIRMED defect (aliased `User`) fixed before certification; full backend **3262 passed / 21 skipped / 0 failed / 0 warnings (58:09)** | DONE — CI #577 all active jobs green on cb8763d (13:06–13:45 UTC; the Postgres-engine mechanism test ran in the `postgres` job); main fast-forwarded to cb8763d (CI #578, the deploy run) |
| Reference integration R2 (2026-09-07) | P1 | Billing durability from the Lago cycle: BILL-REL-001 durable Stripe webhook (event committed as a `billing.apply_subscription_event` job before 200; 503 when it cannot be persisted; customer re-resolved at run time, mismatch dead-letters via the new `jobs.PermanentJobError`; response body drops `applied` — contract change recorded in ADR-0013), BILL-METER-001 frozen usage segments (`usage_counters.reporting_target`, migration `e7f9a1c3d5b8`; commit before the provider call; same quantity + identifier on replay), BILL-QA-001 reliability scenarios | Lead | R1 | MED (billing path; one additive migration; webhook body contract) | M | 104 targeted incl. `test_migrations.py` + docs truth; migration applied on local Postgres 16; RLS suite green; review panel (eight lenses) → supersession check, replay-path commit, compare-and-set advance, id-less guard added with tests + a Postgres twin of the commit-order proof; 9/9 seeded red; full backend **3213 passed / 20 skipped / 0 failed (54:56)**; CI #566 green (seven active jobs), CI #567 all nine incl. deploy, migration applied by the container gate | DONE a789e98 — production 2026-09-08 04:37 UTC |
| Reference integration R1 (2026-09-07) | P1/P2 | Queue/runtime hardening from the eight-repository archive (`docs/plan/REFERENCE-INTEGRATION-PLAN.md`): STIR-P1-01 lease heartbeat (60 s renewal on its own connection, predicate id + RUNNING + locked_by), STIR-P2-01 OCR runtime budget (`OCR_PROCESS_TIMEOUT_SECONDS`, `processing_timeout` outcome, the receipt path the archive missed), PAT-004 Retry-After floor on the queue backoff, PAT-028 connect-time DNS pin for webhook delivery (closes SEC-009), PAT-030 job id/kind in worker JSON logs, FLASK-P2-02 route-topology guard (zero shadowed routes on the live app) | Lead | — | LOW–MED (no migration; queue code path; webhook transport swap; one new 503 branch on three OCR routes) | M | 29 + 39 + 3 + 12 targeted; 2 pg-only tests in the CI postgres list; 9/10 seeded violations red (the 10th — CGNAT/NAT64 list — is not load-bearing on Python 3.11, recorded); ruff/mypy clean; review panel (nine lenses) → S-5/A-1 fixed; full backend **3198 passed / 19 skipped / 0 failed (54:35)**; CI #564 green (seven active jobs), CI #565 all nine incl. deploy | DONE 2c631d5 — production 2026-09-07 21:02 UTC |
| P3 set | P3 | BE-013/015/017/018/019, DB-005/006/010/015, ARCH-006/007/009/010/012/015, PERF-010/014/015, OPS-012, SEC-006/009, QA-004/008/010, FE-006/010/012/014/015/016 | various | | | | | DEFERRED |
| P4 | P4 | SEC-010, DB-016 | | | | | | DEFERRED |

## 4. Decision log

- **ADR-A1 — Unusable password is a non-hash sentinel, not a nullable column.** Keeps `users.hashed_password NOT NULL` (every reader unchanged); `verify_password` refuses any non-`$2` value explicitly. Alternative (nullable column) rejected as a wider change for the same guarantee.
- **ADR-A2 — One client-IP authority.** uvicorn's proxy-header handling is removed; `ratelimit._client_ip` with `TRUSTED_PROXY_COUNT` is the single implementation for rate limiting and audit. Alternative (pin `--forwarded-allow-ips` to a CIDR) rejected: the compose subnet is not stable across hosts.
- **ADR-A3 — No architectural rewrite.** The four god-controllers are recorded debt, extracted incrementally when touched for a defect. Rejected: an application-layer extraction pass.
- **ADR-A4 — Business-behaviour changes go to the owner.** Trial expiry, payment-grace policy, seller-of-record VAT, password policy/MFA, i18n locales, DPA/ToS are recorded as decisions, not decided in code.
- **ADR-A5 — Contract gate before codegen.** A checked-in `openapi.json` with a drift test (the ERD pattern) lands first; `types.ts` generation is a later project.
- **ADR-A6 — Host-side deployment changes are owner-executed.** The repo documents the corrected `deploy.sh`, ships `backup.sh`, and asserts health from CI; nothing in the repo can change `/root/deploy.sh`.
- **ADR-A15 — Phase 12 conditions closed before main (R2-S1, R2-A1, R2-C1-doc).** (S1) `GET /transport/statements/{sha}/file` uses `security_headers.content_disposition` (RFC 5987, injection-safe) — the hand-rolled header stripped only `"` and would 500 on a non-latin-1 filename; a catalog row whose object is missing is a 404 `statement_not_found`, never an empty 200. (A1) Migration `c3e5a7b9d1f2` scans only users of orgs that have an SSO connection or a SCIM-provisioned identity — bcrypt-verifying two literals costs ~0.5 s per user, and the deploy runs `alembic upgrade head` under a 300 s health gate; the production user count is UNKNOWN from the repository, so the bound is applied regardless. (C1) BE-005's ledger row states the true residual (see R2-C1); the code fix stays P2 because it changes billing semantics (period-keyed claim) and deserves its own certification.
- **ADR-A16 — DB-002 also refuses one issuer using the same prefix for invoices and credit notes** (`issuer.py:118-119`, tested). Unflagged in the DB-002 commit; recorded here. An existing profile with equal prefixes cannot be saved until one is changed — the refusal names both.
- **ADR-A17 — WO-AE tie-break** (later in `ASSIGNABLE_ROLES` wins when ranks tie) is a stated rule in `WO-AE-idp-role-mapping.md`; recorded here for completeness.
- **ADR-A18 — `audit.record` no longer swallows database errors (BE-016).** A SQLAlchemyError while appending the event propagates; attribution/hash failures stay best-effort. Behaviour change, flagged: an operation whose audit row cannot be written now fails instead of committing unaudited (ADR-0012's rule) or failing later at its own commit with a misleading message.
- **ADR-A19 — the renewal claim is keyed on the period paid (R2-C1).** A settled charge whose response was lost now stalls as "duplicate" every day until an operator reconciles it against the provider by the logged `order_reference` and advances `everypay_next_charge` by hand; it can no longer charge the card a second time. The stall is the recoverable side; the runbook line is the warning the worker logs.
- **ADR-A20 — bank-file exports are POST (BE-010).** `POST /payment-runs/{id}/export|sepa` and `POST /reimbursements/{id}/export|sepa`; the GET forms answer 405. A client-visible API change, flagged: the SPA (`downloadFile(..., { method: "post" })`) moved with it and the contract snapshot records both. Producing a payment file advances the export-once counter and audits — a browser prefetch or proxy revalidation must never be able to consume "the one export".
- **ADR-A21 — Message-ID is the only inbound-email identity (BE-009).** A delivery without one is stored on every retry; the platform does not guess an identity from sender + subject + attachment digests, because two genuine messages can share all three.
