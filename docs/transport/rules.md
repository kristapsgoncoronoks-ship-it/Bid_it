# Transport vertical — harvested rule ledger

Every rule harvested from the retired Fleet Fuel system (requirements R1–R76,
specified in `docs/plan/shared/specs/BA_fleet_fuel.md`) lands here as one row:
R-number → implementing module → proving test → legal source. The binding
process — three artifacts per rule, no R-test no merge, synthetic fixtures
only — is defined in [`harvest-protocol.md`](harvest-protocol.md). Read that
first; it is not optional.

A row is added in the **same PR** that implements the rule. An empty table
below a rule's Epic-G task means the rule is not yet harvested — never that
it was waived.

| R | Rule (short) | Module | Test | Legal source |
|---|---|---|---|---|
| R1 | Claim grain `(org, entity, refund_country, ref_period)`; same key upserts, never duplicates. **WO-76 route consumer:** `POST /api/v1/transport/claims` (structural VAT_WRITE; deliberately 200 on both calls — the idempotency IS the contract) + `GET` list/detail (VAT_READ), proven in `backend/tests/transport/test_wo76_claim_routes.py` **WO-78 UI consumer:** the `/vat-claims` SPA page creates on the grain and the button reads "Open or create" — the idempotency is stated to the operator rather than mimicked as a create (`frontend/src/pages/VatClaims.tsx`, `frontend/e2e/vat-claims.spec.ts`). | `app/models/transport/vat_claim.py` (`VatRefundClaim`), `app/services/transport/claim.py` (`get_or_create_claim`) | `backend/tests/transport/test_r1_claim_grain.py` | Art. 5 / Art. 16 Dir. 2008/9/EC |
| R3 | One centralized `is_synthetic(ref, vat_id)` predicate, now with its blocking consumers WIRED: the **workbook/evidence builders** (WO-74 — `claim_pack._load_pack` refuses ANY frozen synthetic line, `synthetic_line_in_pack`) and the **LOCK GATE** (WO-75 — `lock.submit_claim` calls `claim_gates.enforce_no_synthetic_lines`, the head of D5's engine-gate group per C9's `bad`-gate-first order: a claim whose materialized unfrozen lines include an UNMATCHED/INPUT/aggregate ref refuses 409 `unresolved_invoice_refs` BEFORE the R6 duplicate machinery and the R10 doc-gate — nothing freezes or locks). The advisory checklist/stage surface (WO-59/WO-60 `unresolved_refs` item -> stage 1A) names the same predicate-defined set at the transaction grain (so it can NAME suppliers). Structural tests prove `lock.py`/`claim_pack.py` define no rival predicate. **WO-76:** the lock-gate refusal surfaces over HTTP — `POST /transport/claims/{id}/submit` → 409 `unresolved_invoice_refs`, nothing mutated, proven at the route level (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-78 UI consumer:** the lines table flags a synthetic ref with an "unresolved" badge via a documented mirror of the predicate (`transportClaims.isSyntheticRef`) used ONLY to pre-seed the submit dialog — it pre-empts no gate; the 409 `unresolved_invoice_refs` still comes from the server and renders as "Some claim lines aren't tied to a real invoice yet". | `app/services/transport/claim_gates.py` (`is_synthetic`, `unfrozen_synthetic_refs`, `enforce_no_synthetic_lines`), `app/services/transport/lock.py`, `app/services/transport/claim_pack.py` | `backend/tests/transport/test_r3_is_synthetic.py`, `backend/tests/transport/test_r3_lock_gate.py`, `backend/tests/transport/test_g2_12_claim_pack.py` | Art. 9 Dir. 2008/9/EC (line must tie to a real invoice) |
| R11 | The Art. 9 goods-code mapping (Diesel/HVO/Promo adj → "1", Toll/Fees → "4", AdBlue/Parking/Service/Other → "10") is harvested verbatim from `BA_fleet_fuel.md` A6; an unrecognised `product_group` also defaults to "10" — **never "9"**, enforced both by the mapping function AND the pre-existing DB CHECK constraint (`ck_vat_claim_lines_goods_code_never_9`, WO-49). `build_claim_lines` (G2.4) populates every line's `goods_code` at construction. | `app/services/transport/goods_code.py` (`GOODS_CODE`, `derive_goods_code`), `app/services/transport/claim_lines.py` | `backend/tests/transport/test_g2_8_goods_code.py`, `backend/tests/transport/test_r11_goods_code_shape.py` | Art. 9 Dir. 2008/9/EC; Reg. 1174/2009 & 79/2012 Annex III |
| G1.2 (`ARCH_plan.md` tags this `R29`/`R30`, but those R-numbers are actually "engine owns product data, read-only app access" and "claims store separate from analytics store" — real rules, already satisfied by this codebase's existing separation, but not specific to this table; the real source for this row is `BA_fleet_fuel.md` **section 4.2** "the core business entity — a validated fuel TRANSACTION" + **section 8.1 items 4-6**, see `docs/plan/plan-a/wo/WO-50-G1.2.md`) | Typed `fuel_transactions` model (no duplicated positional schema); split `note` into `invoice_ref`+`provenance_note`; a structural natural key `(org, entity, supplier, period, line_seq)` so ingestion is insert-or-no-op, never DELETE-by-period; `product_group` derivation, precedence PROMO → HVO → {AdBlue,Parking,Toll/Fees} → Diesel → Service/Other (default). **WO-79 route consumer — the FIRST code path that RETURNS one of these rows:** `GET /api/v1/transport/fuel-transactions` (router-level VAT_READ; required `entity_id`+`period`, optional `supplier`/`country`; `period` takes `YYYY-MM` or a claim reference period, expanded through the SHARED `claim_lines.period_months` mapping) — module-gated then opaque-404 entity fetch before any row is read, every amount AND `qty` Decimal-as-string on the wire, the FX provenance quadruple carried verbatim; `backend/tests/transport/test_wo79_fuel_routes.py`. Because a route now returns them, `fuel_transactions` stops being an EXEMPT row in `backend/tests/test_tenancy_parity.py` and proves its isolation over the real HTTP read path with overlapping data. **WO-79 UI consumer:** the claim detail page's submit dialog is a PICK-LIST over these rows — each ticked transaction supplies the `(supplier, invoice_ref, fuel_transaction_id)` tuple `lock.submit_claim` keys its locks on, so the operator no longer types a supplier or a UUID (`frontend/src/pages/VatClaimDetail.tsx`, `frontend/e2e/vat-claims.spec.ts`). | `app/models/transport/fuel_transaction.py` (`FuelTransaction`), `app/services/transport/product_group.py` (`derive_product_group`), `app/services/transport/fuel_ingest.py` (`ingest_transaction`), `app/services/transport/fuel.py` (`list_fuel_transactions`) | `backend/tests/transport/test_g1_2_fuel_transactions.py`, `backend/tests/transport/test_g1_2_product_group.py`, `backend/tests/transport/test_wo79_fuel_routes.py` | `BA_fleet_fuel.md` §4.2, §8.1 items 4-6 |
| R4 | **M** | One-invoice-one-submission lock, `UNIQUE(org, entity, refund_country, supplier, invoice_ref)`, acquired in the same transaction as the claim's status change via a plain INSERT (never an upsert); a lost race raises `IntegrityError` and aborts the whole transition. Widened with `org_id` per this codebase's standing convention — the harvested BA text omits it (Fleet Fuel was single-tenant), but `ARCH_plan.md`'s own G2.2 wording already includes it. **WO-79 UI consumer:** the `(supplier, invoice_ref, fuel_transaction_id)` tuple this lock keys on is now SELECTED from the period's real fuel transactions rather than typed — the supplier and the transaction id come off the chosen row verbatim, which removes the transcription error that would otherwise lock the wrong invoice. Deriving the tuple server-side from the frozen lines remains "a future slice of G2.6" (`lock.py`'s own docstring); this order changed the input method, not the contract. | `app/models/transport/lock.py` (`VatClaimedInvoice`), `app/services/transport/lock.py` (`submit_claim`) | `backend/tests/transport/test_g2_2_lock.py`, `backend/tests/test_transport_lock_concurrency.py` (real Postgres, two concurrent submissions over the same invoice: exactly one wins, the loser's status reads back unchanged from a fresh query) | `BA_fleet_fuel.md` §3.C5, §4.3 schema fragment |
| R5 | **M** | Only an explicit `withdraw_claim` releases locks — `rejected`/rejection(3B)/confiscation(3C)/appeal(3D) all keep them. Modeled here by there being no OTHER function in `app/services/transport/lock.py` that deletes a `VatClaimedInvoice` row (proven structurally by a grep-based test) and by directly mutating a claim's `status` never cascading a lock release. After withdraw, the freed invoice key can be locked by a DIFFERENT claim. **WO-76 route consumer:** `POST /transport/claims/{id}/withdraw` (VAT_SUBMIT — releasing locks is as consequential as taking them), proven in `backend/tests/transport/test_wo76_claim_routes.py` **WO-78 UI consumer:** the Withdraw action on the claim detail page, shown only to a VAT_SUBMIT holder; `claim_not_locked` renders as "There's nothing to withdraw on this claim". **WO-94 — §3.D D7's SECOND half, previously unimplemented:** *"Only `withdraw_claim` releases, and it also NULLs `status_code`."* The release was built in WO-51; the code clear was not, so every withdrawn claim kept the `"2"` `submit_claim` stamps (or whatever `status.set_status_code` last wrote, per R17's vocabulary) and read as *submitted* / *under appeal* beside a `withdrawn` engine status. WO-93 found it and recorded it; WO-94 fixes it at the transition that owns the withdrawal — both columns move in the SAME flush as the lock deletion, so a rollback still discards the release, the status and the code together, and the existing `transport.claim_withdraw` audit event now carries old→new under `set_status_code`'s own field names (one meta shape per column, not two). The clear is deliberately NOT put in `set_status_code`, which already refuses a withdrawn claim (`claim_not_submitted`) and so can neither reach the state nor undo it. Rows already carrying the inconsistent pair are repaired by the data-only revision `f2a91c07d4e6`, which counts and prints what it changes; it WRITES rather than refusing (unlike the two FX-provenance revisions) because D7 states the one correct value for every matching row and it is a constant. WO-93's client-portal immunity test keeps its name and now proves more: it asserts the fix, then writes a stale code back by hand and asserts the portal still shows nothing. **WO-95 — what D7 does NOT say, pinned:** G2.9 asked whether a withdrawal should also clear the frozen contingency fee. It should not, and the answer is D7's own enumeration: it names the locks and `status_code` and nothing else. Nothing anywhere in the harvest clears a frozen figure on withdrawal, and `withdraw_claim` correspondingly leaves `vat_eur`/`vat_local`/`currency` standing today — clearing the fee but not the base it was computed from would put the claim in a shape the specification never describes. The lifecycle settles it beyond interpretation: `submit_claim` accepts only a `draft` claim and a withdrawn one never returns to `draft` (the assertion this row already carries), so a frozen fee can never be re-frozen, contradicted or made stale. `withdraw_claim` is untouched by WO-95 and `test_wo95_a_withdrawn_claim_keeps_its_frozen_fee` proves the reading rather than leaving it in a docstring. | `app/services/transport/lock.py` (`withdraw_claim`), `backend/alembic/versions/f2a91c07d4e6_wo94_withdrawn_claims_null_status_code.py` | `backend/tests/transport/test_g2_2_lock.py` (`test_g2_2_withdraw_claim_is_the_only_function_that_deletes_a_lock_row`, `test_g2_2_directly_flipping_claim_status_never_cascades_a_lock_release`), `backend/tests/test_transport_lock_concurrency.py::test_withdraw_then_reclaim_by_a_different_claim_succeeds`, `backend/tests/transport/test_wo94_withdraw_status_code.py` (`test_wo94_withdraw_nulls_the_status_code`, `test_wo94_the_withdraw_audit_event_carries_old_to_new`, `test_wo94_withdraw_changes_nothing_else_on_the_claim`, `test_wo94_a_withdrawn_claim_cannot_be_resubmitted_or_recoded`, `test_wo94_the_backfill_repairs_a_legacy_row`), `backend/tests/transport/test_wo93_client_status.py::test_wo93_a_withdrawn_claim_is_not_shown_and_its_stale_code_is_never_read`, `backend/tests/transport/test_wo95_fee_freeze.py::test_wo95_a_withdrawn_claim_keeps_its_frozen_fee` | `BA_fleet_fuel.md` §3.C7, §3.D **D7** |
| R2 | Claim lines are built ONLY from registered invoices, one row per (invoice, product code); never an `ALL:` aggregate. An unmatched transaction becomes an `UNMATCHED` line, never an invented aggregate. **WO-76 route consumer:** `POST/GET /transport/claims/{id}/lines` (VAT_WRITE/VAT_READ) materialize + read the lines over HTTP, Decimal-as-string on the wire — `backend/tests/transport/test_wo76_claim_routes.py` **WO-78 UI consumer:** the claim detail page renders the materialized lines at exactly this grain (invoice ref × product group, with the Art. 9 goods code) and never sums them into a header figure (`frontend/src/pages/VatClaimDetail.tsx`). | `app/services/transport/claim_lines.py` (`build_claim_lines`) | `backend/tests/transport/test_g2_4_claim_lines.py` | Art. 9 Dir. 2008/9/EC; `BA_fleet_fuel.md` §3.C1 |
| R16 | Note→invoice overrides change only the invoice ASSOCIATION, never an amount, and are validated at BOTH set time and read time against the live registered set. De-registering the target ⇒ the override row is deleted (`ondelete=CASCADE` — a composite-FK `SET NULL` is not viable here, see the model docstring) ⇒ the line reverts to UNMATCHED. An override never displaces a successful heuristic match. **WO-77 route consumer:** `GET/PUT /api/v1/transport/note-overrides` (VAT_READ/VAT_WRITE) list and curate the overrides over HTTP — the list shows a row AS CURATED (a target since reassigned still appears: that is the state an admin must see to fix it), while the live re-validation that stops it RESOLVING stays in `resolve_invoice_ref`. Refusals surface verbatim: 422 `override_note_is_synthetic` / `override_target_not_registered`. No removal route exists because no removal FUNCTION was harvested (de-registering the target CASCADE-deletes the row) — the route surface mirrors the service surface exactly. Proven in `backend/tests/transport/test_wo77_admin_routes.py` **WO-80 UI consumer:** the "Invoice references" panel of `/vat-admin` lists and sets the mappings (VAT_WRITE-gated cosmetically) and renders NO delete control — the absence is asserted, because the service has no removal verb to call; `override_note_is_synthetic`/`override_target_not_registered` render as sentences (`frontend/e2e/vat-admin.spec.ts`) | `app/models/transport/note_override.py` (`VatNoteInvoiceOverride`), `app/services/transport/invoice_match.py` (`resolve_invoice_ref`, `set_note_override`) | `backend/tests/transport/test_g2_4_invoice_match.py` | `BA_fleet_fuel.md` §3.C3/C4 |
| R31 | The monthly close is an independent, idempotent-by-`(org, period)`, restartable orchestrator with one audit trail per run; it halts on the first failure with nothing partially applied (translated onto the pre-existing `app.services.jobs` durable-job framework rather than Fleet Fuel's `process_lock`+pickle hand-off — no ETL pipeline to port, since `fuel_transactions` ingestion is already insert-or-no-op, G1.2). | `app/services/transport/close.py` (`run_close`, `enqueue_close`), `app/services/job_handlers.py` (`transport.close` handler) | `backend/tests/transport/test_g1_3_close.py` | `BA_fleet_fuel.md` §8 R31; Art. 2008/9/EC procedural context |
| R60 | The close never runs inline in a web request — the only reachable entry point is `enqueue_close` → the worker's `jobs.run_once`; no `api/routes/transport/*` route exists to call it synchronously. | `app/services/transport/close.py` | `backend/tests/transport/test_g1_3_close.py::test_g1_3_run_once_through_the_job_worker_processes_the_enqueued_close` | `BA_fleet_fuel.md` §8 R60 |
| R30 (G1.4) | Locked lines are protected from a re-close: `run_close` only ever queries `status == "draft"` claims (a submitted claim's lines are invisible to it, inherited from `build_claim_lines`'s own refusal), and `vat_claimed_invoices`' pre-existing `RESTRICT` FK into `fuel_transactions` (WO-51) independently refuses deleting a locked transaction at the database level. | `app/services/transport/close.py`, `app/models/transport/lock.py` (`fk_vat_claimed_invoices_fuel_transaction`) | `backend/tests/transport/test_g1_4_close_protects_locked_lines.py` | `BA_fleet_fuel.md` §8 R30; ADR-P3 |
| G2.5 (ADR-P3; strengthens R30 — no dedicated R-number) | Frozen claim lines + frozen VAT base at submission: `submit_claim` stamps `frozen_at` on every line and sets `claim.vat_eur`/`vat_local`/`currency` from EXACTLY the claim's own lines (C10), in the SAME flush as lock acquisition — a lost lock race rolls back the freeze too. Refuses (both at the line-build level and the freeze level) if the underlying figures span more than one currency, never silently summing across them (§4.14). | `app/services/transport/freeze.py` (`freeze_claim_lines`), `app/services/transport/lock.py` (`submit_claim`), `app/services/transport/claim_lines.py` | `backend/tests/transport/test_g2_5_freeze.py` | `BA_fleet_fuel.md` §3.C10; ADR-P3 |
| R7 | Hard period-end gate — `submit_claim` refuses (409 `period_not_ended`) unless `today > period_end_date(ref_period)`; checked BEFORE the freeze/lock machinery, so an unended period never mutates anything. **WO-76:** surfaces as 409 `period_not_ended` from `POST /transport/claims/{id}/submit`; the service's `today` test seam is deliberately NOT exposed on the wire (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-78 UI consumer:** 409 `period_not_ended` renders as "The reference period hasn't finished yet" plus what to do next. | `app/services/transport/deadline.py` (`period_end_date`, `period_ended`), `app/services/transport/lock.py` | `backend/tests/transport/test_g2_6_deadline.py`, `backend/tests/transport/test_g2_6_submission_gates.py` | `BA_fleet_fuel.md` A5; Art. 5 Dir. 2008/9/EC |
| R8 | Art. 17 minimum-amount gate, compared in the CORRECT currency: euro countries + Poland compare `vat_eur` against €400 (quarterly)/€50 (annual); Sweden/Denmark compare `vat_local` against their own fixed statutory amounts. Previewed BEFORE freezing (D5's own gate order) so a below-minimum claim never freezes or locks; an explicit override submits anyway and records the exact comparison in `status_note`. **WO-76:** 409 `below_minimum` over HTTP; the `override_minimum` body flag rides `POST /transport/claims/{id}/submit` (VAT_SUBMIT) and the recorded note is asserted (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-78 UI consumer:** 409 `below_minimum` renders the human sentence AND the explicit override action, which re-posts with `override_minimum=true` (VAT_SUBMIT-gated, same as the submit) — the override is never silent, and the test asserts the captured request body. | `app/services/transport/minimum.py` (`NATIONAL_MINIMUMS`, `min_for`, `below_minimum`), `app/services/transport/lock.py` (`submit_claim`, `override_minimum`) | `backend/tests/transport/test_g2_6_minimum.py`, `backend/tests/transport/test_g2_6_submission_gates.py` | `BA_fleet_fuel.md` A3; Art. 17 Dir. 2008/9/EC |
| R9 | 30-Sep-year+1 filing deadline surfaced with a 60-day risk window (`"ok"`/`"at_risk"`/`"overdue"`) — deliberately NOT a submission gate (this software never blocks a late filing attempt; the refunding tax authority is what actually enforces the time-bar). Pure functions, ready for a future dashboard (G4.3) to consume. **WO-81 consumer — the future arrived:** `recovery.recovery_dashboard` calls `deadline_status` for every unfiled claim of a refund year, producing BOTH the `deadline` readiness bucket (a claim that is otherwise fileable with the clock running) and the separate `deadline_risk_claims` count (every unfiled claim at risk, whatever its blocking bucket) — the north-star KPI "deadline misses = 0" made countable. Still not a gate: the dashboard reports, refuses nothing. The 60-day window is proven on BOTH sides of its edge one day apart over the real service (`test_wo81_deadline_risk_boundary_is_inclusive_at_exactly_60_days`: 60 days out is at risk, 61 is not), and a filed claim is never counted (it is past the time-bar). | `app/services/transport/deadline.py` (`filing_deadline`, `deadline_status`, `DEADLINE_RISK_DAYS`), `app/services/transport/recovery.py` | `backend/tests/transport/test_g2_6_deadline.py`, `backend/tests/transport/test_wo81_recovery.py` | `BA_fleet_fuel.md` A4; Art. 15 Dir. 2008/9/EC; CJEU C-294/11 *Elsacom* |
| R6 | An ANNUAL claim is the MOP-UP: an invoice already locked by a QUARTERLY claim is silently excluded (not a conflict); an invoice already locked by ANOTHER ANNUAL claim is a fail-closed blocking duplicate (an interpretation, not given verbatim). A QUARTERLY claim treats ANY overlap with an existing lock as a duplicate and blocks the WHOLE submission (before any freeze/lock write). An annual claim left with an empty set after exclusion is refused ("nothing to claim annually"). **WO-76:** 409 `duplicate_invoice_lock` over HTTP (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-78 UI consumer:** 409 `duplicate_invoice_lock` renders as "One of these invoices is already claimed on another filing" with the server's own detail naming the invoice. | `app/services/transport/lock.py` (`_apply_annual_mop_up_or_duplicate_block`) | `backend/tests/transport/test_g2_6_annual_mop_up.py` | `BA_fleet_fuel.md` §3.C6 |
| R10 | Every invoice in the claim's currently-unfrozen, RESOLVED lines (never an `UNMATCHED` one, whose `invoice_id` is always NULL) must have >=1 vaulted document — a capture with a REAL `source_sha256` (a manually-typed invoice with no captured file is not "vaulted"). Checked with ONE batch query (`extraction.invoice_ids_with_documents`, never a per-invoice loop) against the claim's own materialized `vat_claim_lines`, not `submit_claim`'s caller-supplied `invoices` tuple list. Wired into `submit_claim` after the R6 mop-up/duplicate-block gate and BEFORE the freeze. **WO-76:** 409 `invoice_document_missing` over HTTP (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-78 UI consumer:** 409 `invoice_document_missing` renders as "An invoice on this claim has no stored document", with the service's own list of references underneath. | `app/services/transport/document_gate.py` (`enforce_document_presence`), `app/services/extraction.py` (`invoice_ids_with_documents`), `app/services/transport/lock.py` | `backend/tests/transport/test_g2_6_document_gate.py` | `BA_fleet_fuel.md` §3.C8; Art. 15 Dir. 2008/9/EC |
| R15 | Receipt-control waivers are permitted ONLY for a genuinely uninvoiced supplier — `set_waiver` refuses (422 `waiver_supplier_has_invoices`) any supplier with >=1 registered invoice for the claim's refund country (reusing `invoice_match.registered_invoices`, never re-deriving the check), otherwise records the waiver idempotently on a `draft` claim. `claim_lines.build_claim_lines` excludes a waived supplier's transactions from grouping ENTIRELY, before the resolution step — they never become an `UNMATCHED` line. `submit_claim` stamps every active waiver into `status_note` at submission. `remove_waiver` un-waives (CRUD symmetry, not itself a harvested requirement). **WO-77 route consumer:** `GET/POST /api/v1/transport/claims/{id}/waivers` + `DELETE .../waivers/{supplier}` (VAT_READ/VAT_WRITE — a waiver configures how lines are BUILT; it flips no status and takes no lock), surfacing 422 `waiver_supplier_has_invoices` and 409 `claim_not_draft` with nothing mutated. Proven in `backend/tests/transport/test_wo77_admin_routes.py` **WO-78 UI consumer:** none — waiver administration belonged to the second UI slice; the claim workspace shows the `status_note` the waiver stamp writes. **WO-80 UI consumer:** the claim detail page now carries the waiver panel itself (list/waive/un-waive, offered only for VAT_WRITE on a DRAFT claim, mirroring `claim_not_draft`), with copy that steers an operator away from waiving a supplier whose transaction has merely not been tied to its invoice yet; `waiver_supplier_has_invoices` renders as a sentence naming the consequence (`frontend/e2e/vat-claims.spec.ts`). | `app/models/transport/receipt_waiver.py` (`VatReceiptWaiver`), `app/services/transport/waiver.py` (`set_waiver`, `remove_waiver`, `waived_suppliers`), `app/services/transport/claim_lines.py`, `app/services/transport/lock.py` | `backend/tests/transport/test_g2_6_waiver.py`, `backend/tests/transport/test_g2_4_claim_lines.py::test_g2_4_a_waived_suppliers_transaction_produces_zero_claim_lines` | `BA_fleet_fuel.md` §3.C9, rule "R5 case (a)" |
| R17 | Pre-submission codes `1A/1B/1C/1E` (`AUTO_CODES`) are system-derived and NEVER user-settable — `set_status_code` refuses any of them with a message containing "system-controlled". The only legal first manual step from an unlocked (`draft`) claim is `2` (Submit) — performed exclusively by `lock.submit_claim`'s gated procedure, never as a bare label write; `set_status_code` refuses EVERY manual code (including `"2"` itself, and e.g. `"3A"`) while the claim is still `draft` (`code="claim_not_submitted"`). `derive_stage` computes the correct AUTO code for a draft claim in D3's literal order (non-period items -> period-end -> a "caveat" -> ready), a DOCUMENTED INTERPRETATION of "checklist items"/"a caveat" pending the still-unbuilt G2.10 checklist engine. **WO-76:** `GET /transport/claims/{id}/stage` exposes `derive_stage` read-only (VAT_READ, no commit); the "2" stamp still only ever rides the gated submit route (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-77:** `POST /transport/claims/{id}/status-code` exposes `set_status_code` under VAT_SUBMIT (it is the claim-STATUS surface) and `GET /transport/status-codes` returns the VOCABULARY — `AUTO_CODES` + `MANUAL_CODES`, the actual symbols in this codebase; Fleet Fuel's `STATUS_LABELS` was never harvested as data and is deliberately NOT invented. The refusals surface verbatim over HTTP: 409 `status_code_system_controlled` on an AUTO code, 422 `unknown_status_code`, 409 `claim_not_submitted` on a draft — each leaving the stored code unchanged (`backend/tests/transport/test_wo77_admin_routes.py`). **WO-78 UI consumer:** the claim detail page renders the ladder from `GET /transport/status-codes` (the service's OWN vocabulary — no label mapping is invented), marks the derived stage on a draft claim via `GET .../stage`, and offers ONLY the manual codes in the set-code dialog; `status_code_system_controlled` renders as "That code is set by the system, not by hand". **WO-80 UI consumer:** `/vat-admin`'s "Status codes" panel renders the vocabulary read (auto vs manual) as a REFERENCE only and links to the claim — the VAT_SUBMIT set-code action is deliberately not duplicated there (`frontend/e2e/vat-admin.spec.ts`). **WO-93 CLIENT consumer — the codes' first READ-ONLY translation:** `client_status.STAGE_BY_CODE` maps every code a claim can carry (`AUTO_CODES` + `MANUAL_CODES` + `"2"`, which `MANUAL_CODES` deliberately excludes because only `lock.submit_claim` stamps it) onto one of R39's six plain-language client stages, and `/transport/claim-status` returns the STAGE and never the code. The translation consumes the vocabulary and changes nothing about it: no code is set, no AUTO code is derived by any path other than `derive_stage`, and a claim whose stored code is unknown to the map falls back to the coarse engine state rather than raising or leaking (`backend/tests/transport/test_wo93_client_status.py`). | `app/services/transport/status.py` (`AUTO_CODES`, `MANUAL_CODES`, `derive_stage`, `set_status_code`), `app/services/transport/lock.py` (`submit_claim` stamps `status_code="2"`), `app/services/transport/client_status.py` (`STAGE_BY_CODE`, `stage_for_code`) | `backend/tests/transport/test_g2_7_status.py`, `backend/tests/transport/test_wo93_client_status.py` | `BA_fleet_fuel.md` §3.D1/D3/D4 |
| R12 | `2B` (document request received) and `3D` (under appeal) carry an `action_deadline` and are SOFT worklist reminders — never an auto-reject or forfeiture gate (CJEU C-133/18 *Sea Chefs*). `set_status_code("2B", action_deadline=...)` stamps the label + deadline, leaves the coarse engine `status` UNCHANGED, and blocks no other action (proven directly: `withdraw_claim` still succeeds immediately afterward). **WO-78 UI consumer:** the claim detail header shows an `action_deadline` as an "Action by <date>" badge, and the status-code dialog carries the optional deadline field labelled as a reminder only. | `app/services/transport/status.py` (`set_status_code`) **WO-77 route consumer:** `POST /transport/claims/{id}/status-code` carries the optional `action_deadline`; a `3D` + deadline over HTTP stamps both and leaves the coarse engine `status` at `submitted` (`backend/tests/transport/test_wo77_admin_routes.py`). | `backend/tests/transport/test_g2_7_status.py::test_g2_7_set_status_code_2b_with_a_deadline_changes_nothing_else_and_blocks_nothing` | `BA_fleet_fuel.md` §3.D; CJEU C-133/18 *Sea Chefs* |
| R45 | The submission checklist is DATA (`vat_checklist_rules`: key/label/scope/check_type/reference/active/sort), not code — `set_active` is the ONLY writer of `active`; deactivating a rule removes it from the gate entirely (verbatim acceptance test). **WO-AB (slice 2) COMPLETES THE HARVEST**: slice 1 seeded only `customer_data`/`bank_account` and named its blockers; slice 2 supplies both — `IssuerProfile.nace_code` (Art. 11's business activity; PRESENCE only, since national NACE derivatives differ in shape) and `vat_claimant_documents` (a claimant document store WITH an expiry, RLS in-migration, bytes through the one `documents.store` choke point) — so `contract`, `nace`, `trade_register` and `power_of_attorney` are seeded and evaluated. `submission_checklist` now evaluates BOTH scopes and BOTH check types: a `scope="country"` rule reads THIS claim's `refund_country` (a PoA held for FR does not satisfy an LV claim), and `valid_until` is inclusive of its last day while a NULL expiry is a FACT ('this does not lapse'), not an absence. "Not held" and "expired on <date>" are different reasons because they are different jobs. `tax_authority.TAX_AUTHORITY` (§3.F F5) names the national authority in a failing country-scope reason; an unknown country contributes NOTHING rather than a guess. The two `unsupported_*` raises STAY — `reference` is free text, so a rule naming a verifier or document kind that does not exist is still reachable. §3.E's `_open_poa_request_note` is NOT built: the F4 document-request workflow does not exist here, so the note has no subject. The four claim-level items (receipt control, unresolved refs, documents attached, period ended) reuse R3/R10/R15/R7's existing pure checks, never re-derived; `status.derive_stage` (G2.7) now consults this evaluator, replacing WO-59's own two-check proxy. **WO-76:** `GET /transport/claims/{id}/checklist` exposes the evaluator read-only (VAT_READ, NO commit — even the idempotent default-rule seed is discarded with the request session; advisory per §4.19, proven mutation-free in `backend/tests/transport/test_wo76_claim_routes.py`). **WO-77:** the rule ADMIN lands — `GET /transport/checklist-rules` (the rows as DATA), `POST /transport/checklist-rules/seed` (the idempotent `DEFAULT_RULES` seed; it needs its own COMMITTING route precisely because the claim checklist GET never commits) and `POST /transport/checklist-rules/{key}/active`. R45's own acceptance is now proven over HTTP end to end: deactivating `customer_data` makes it disappear from the claim's checklist evaluation (`backend/tests/transport/test_wo77_admin_routes.py`); an unknown key is 404 `checklist_rule_not_found`. **WO-78 UI consumer:** the claim detail page renders the checklist as an ADVISORY panel — a failing item shows its reason and NEVER disables the Submit button (§4.19, asserted in `frontend/e2e/vat-claims.spec.ts`). **WO-80 UI consumer:** the rule ADMIN lands in the SPA — `/vat-admin`'s "Checklist rules" panel lists the rows, toggles `active` per key and exposes the committing seed; an empty table is presented as the first run it is (the claim checklist GET never commits), and `checklist_rule_not_found` renders as a sentence (`frontend/e2e/vat-admin.spec.ts`). | `app/models/transport/checklist_rule.py` (`VatChecklistRule`), `app/models/transport/claimant_document.py` (`VatClaimantDocument`, `DOC_KINDS`), `app/services/transport/checklist.py` (`DEFAULT_RULES`, `seed_default_rules`, `set_active`, `DATA_VERIFIERS`, `submission_checklist`), `app/services/transport/claimant_documents.py` (`record`, `has_valid`, `expiring`), `app/services/transport/tax_authority.py` (`TAX_AUTHORITY`, `authority_for`) | `backend/tests/transport/test_g2_10_checklist.py`, `backend/tests/transport/test_g2_7_status.py` (2 extended tests), `backend/tests/transport/test_wo_ab_claimant_documents.py` (25), `backend/tests/test_tenancy_parity.py` (`vat_claimant_documents` probe) | `BA_fleet_fuel.md` §3.E; Art. 8(1)/Art. 11 Dir. 2008/9/EC |
| R21 | Supplier matching is MARKER-ONLY (admin-curated brand/VAT registrations, country-scoped) — no fuzzy auto-pairing. `supplier_entity.get_registration` is a single exact-(supplier, country)-key SELECT; no other function anywhere assigns a supplier/entity pairing without an explicit curated marker (`set_registration`) or the R22 learning rule. | `app/models/transport/supplier_registration.py` (`SupplierVatRegistration`), `app/services/transport/supplier_entity.py` (`get_registration`, `set_registration`) | `backend/tests/transport/test_g3_1_supplier_entity.py` | `BA_fleet_fuel.md` §3 (Eurowag/E100 marker-only matching) |
| R22 | Per-country entity LEARNING: `learn_registration` seeds a NEW `source="capture"` row ONLY when no registration exists yet for the exact (supplier, country) key — it NEVER overwrites an existing row of EITHER source (a curated `admin` marker is permanent; a second, possibly-conflicting capture never silently replaces an earlier learned row). Never queues a pending-change request (unlike A2.3's vendor-bank-detail dual control — this is diagnostic/filing metadata, not a payment-redirection vector) and never touches a `Vendor`/group-primary row. WO-61 shipped this at the function level with no real caller; WO-62 (G3.2 slice 1) is the real caller — proven end to end (`test_g3_2_ingest_statement_never_overwrites_a_curated_registration`). | `app/services/transport/supplier_entity.py` (`learn_registration`), `app/services/transport/statement_ingest.py` (`ingest_statement`, the real caller) | `backend/tests/transport/test_g3_1_supplier_entity.py`, `backend/tests/transport/test_g3_2_statement_ingest.py` | `BA_fleet_fuel.md` R20-R22 (Eurowag per-country footer, E100 anchor) |
| R20 (G3.2 slice 1+2) | Capture reads the SELLER legal entity PRINTED ON THE DOCUMENT — never the buyer, never a factoring entity. `EurowagParser` anchors ONLY to lines carrying the literal `"Pārdevējs / Verkoper:"` label (verbatim from `BA_fleet_fuel.md` §3.B1) matched against the harvested legal-form token set `(BVBA\|GmbH\|UAB\|SIA\|s.r.o.\|d.o.o.\|a.s.\|S.A.\|AB\|SE\|BV)`; an UNRELATED factoring-entity disclosure sentence elsewhere in the same document (not carrying the label) is never inspected, so the Czech "W.A.G. Issuing Services, a.s." factoring entity can never be picked up by accident. `E100Parser` (WO-63, G3.2 slice 2) anchors ONLY to lines carrying the literal `"E100 International Trade"` marker string (verbatim, `BA_fleet_fuel.md` §3.B1's own SECOND worked example) — a co-present, unrelated buyer-VAT annexe line ("repeats on every annexe page") is never inspected, so it can never enter `parsed.entities`. `E100Parser` also derives `net_local`/`vat_local` by the REVERSE VAT-inclusive-gross calculation (`net_local = gross_local / (1 + vat_rate/100)`), since E100 invoices give only `gross_local` + a `vat_rate`, unlike Eurowag's independently-given `net_local`/`vat_local`/`gross_local` — proven numerically (`net_local + vat_local == gross_local`, pre-rounding Decimal arithmetic). `app.services.transport.fuel_card_parser` is the deterministic-first registry every network's parser plugs into (mirrors the AP-domain `extraction_provider.py` PATTERN, not its AP-invoice-shaped output type) and now dispatches BOTH networks without collision; `statement_ingest.ingest_statement` reads the network OFF the parsed statement (never a caller-supplied string) and calls `learn_registration` per detected entity — UNCHANGED by WO-63, proving its network-agnostic design for a second, structurally different money model. **CLOSED for Eurowag AND E100 — and stays CLOSED at exactly those two networks.** `Q8Parser` (WO-64, G3.2 slice 3) registers a THIRD network into the same `fuel_card_parser` registry, proving it dispatches a statement that legitimately spans MORE THAN ONE country/currency in a single file (`BA_fleet_fuel.md` §5.1's own "per-line country + currency" quirk for Q8) — but Q8 carries no R20 claim of its own: `BA_fleet_fuel.md` gives no seller-footer label or anchor marker for Q8 the way it does for Eurowag/E100, so `Q8Parser` deliberately attempts NO entity detection (`entities` is always `[]`, with an explanatory warning distinct from "none found"); a per-country Q8 entity, if ever needed, is admin-curated via `set_registration`, never learned. Q8's `net_eur_eff` (the Port One off-invoice-rebate figure) is likewise deliberately left at `ingest_transaction`'s default (`= net_eur`) — that reconciliation is `ARCH_plan.md`'s own separate, later **G4.2** board item (M5, R49/R50), not part of this row's scope. `DKVParser` (WO-65, G3.2 slice 4) registers a FOURTH network with the THIRD distinct money model — the supplier-STATED-EUR conversion (`BA_fleet_fuel.md` §5.1's DKV row, "trusts the supplier's per-line EUR and pro-rates"): the statement's own per-line `net_eur` figure rides in the new additive `ParsedFuelLine.stated_net_eur` field and is used VERBATIM as the line's `net_eur` (never an ECB recomputation, even when a cached rate exists — the invoice is what a refund state audits against), with `vat_eur` PRO-RATED at the same implied basis (`vat_local * stated_net_eur / net_local`), the implied applied rate recorded (`fx_rate = net_local / stated_net_eur`, 6 dp), the OFFICIAL ECB reference frozen best-effort (NULL when uncached — a stated line never fails on missing coverage), and `fx_source="stated"` (the platform's pre-existing ADR-0010 enum value, `expenses.apply_item_fx` precedent, reachable from transport for the first time). Fail-closed: an implied rate that is not strictly positive (zero/sign-flipped stated pair) refuses the WHOLE statement (`fx_stated_inconsistent`) before any write; an EUR-currency DKV row must state `net_eur == net_local` (a mismatch is a malformed row) and rides the unchanged identity branch. DKV's flat 1.30 SEK/L diesel discount + 5.63% parking/services service fee are ON-INVOICE ("already inside `net_eur`", §4.2) — read, never recomputed (verifying contracted terms is G3.4). Like Q8, DKV carries NO R20 claim of its own (no worked marker in the harvested spec; `entities` always `[]`, the "never attempted" warning; entity via admin curation only). `TFCParser` (WO-67, G3.2 slice 5) registers a FIFTH network with the FOURTH distinct money model — the DERIVED-NET hub-only discount (`BA_fleet_fuel.md` §5.1's TFC row: "−0.205/L only at TFC hubs (Meer −0.19); third-party stations undiscounted. Flat 21% VAT"): the statement prints litres, a LIST unit price and a per-line station classification, and the parser DERIVES `net_local = qty × (list_price_eur_l − tier)` (tiers 0.205/`hub`, 0.19/`meer`, 0/`third_party` — the harvested constants), `vat_local = net_local × 21%` (flat, harvested) and `gross_local = net_local + vat_local`, pure unrounded Decimal (the derivation basis recorded in `provenance_note`). The hub discount is ON-INVOICE (§4.2 "already inside `net_eur`") — deriving it IS how it enters the invoiced figures; `net_eur_eff` stays at the default (= `net_eur`), no off-invoice layer. Fail-closed refusals abort the WHOLE statement: an unknown `station_class` (a guessed tier is an invented discount), a non-EUR row (the tier constants are EUR/L; §5.1 gives TFC as an EUR network — the one parser where the currency check is load-bearing), and a non-positive discounted unit price (a mis-keyed list price, named at the parser instead of surfacing downstream as a review-gate `net_not_positive`). Every figure rides the UNCHANGED `ParsedFuelLine`/`statement_ingest` contract (parser-local arithmetic — WO-65's contract-stability argument holds); every TFC line is EUR-identity `fx_source="eur"`. TFC is the FIRST network onboarded against the LIVE G3.3 regimes: its ingest suite proves the €0.02-allowed/€0.03-blocked coversheet tie-out over the parser's DERIVED figures (§5.1's "PASS = trained" run against the real WO-66 gate). Like Q8/DKV, TFC carries NO R20 claim of its own (no worked marker; `entities` always `[]`, the "never attempted" warning; entity via admin curation only). `MoeveParser` (WO-68, G3.2 slice 6) registers a SIXTH network with the per-line-IVA VAT-INCLUSIVE money model (`BA_fleet_fuel.md` §5.1's Moeve row: "ALL amounts VAT-inclusive; per-line IVA rate (10% gasoleo / 21% EcoBlue); cash-at-pump nets against transfer; 6-dp internal calc") — E100's reverse calculation varied three ways: the IVA rate genuinely VARIES line-by-line within one statement (each line split at ITS OWN printed rate, never a statement-wide constant); the derivation runs at the harvested 6-dp internal precision (`net_local = q(gross/(1+rate/100), 6dp)` ROUND_HALF_UP, `vat_local = gross − net` EXACT so `net+vat==gross` holds identically — `q2` stays at the ingest boundary only); and a per-line `payment` settlement channel (`transfer`/`cash_at_pump`, fail-closed on any other value — a settlement channel is never guessed). THE CASH-AT-PUMP DECISION (documented in WO-68): a flagged row IS a fuel line — invoiced, VAT-bearing, its VAT exactly as reclaimable; "nets against transfer" is SETTLEMENT (M4/G3.5 territory), so the flag rides `provenance_note` only, NEVER mutates a figure, the batch tie-out ties the INVOICE total (all lines, cash included), and the parse surfaces one advisory warning (count + gross sum). Rate policy deliberately lives in the WO-66 gate, not the parser: `iva_rate` is bounded [0,100] (E100's defensive bound) with NO {10,21} whitelist and NO product→rate mapping — rule 9's HARVESTED ES dual entry `(21, 10)` exists for exactly this network's rates and is REUSED, never duplicated (proven both ways: 10/21 ES lines raise no `vat_rate_incoherent`; a 15% ES line warns and still registers — warnings never block). The MOEVE "PRN off pump PVP" discount is ON-INVOICE (§4.2, inside the printed gross the parser reverse-calculates from) — `net_eur_eff` stays at the default (= `net_eur`); verifying the contracted discount is G3.4/M5. No currency enforcement (E100's own reasoning — this money model never depends on the currency, unlike TFC's EUR/L tier constants; §5.1's EUR expectation lives in the fixtures). Like Q8/DKV/TFC, Moeve carries NO R20 claim of its own (no worked marker; `entities` always `[]`, the "never attempted" warning; entity via admin curation only). `BPParser` (WO-69, G3.2 slice 7 — **G3.2 CLOSED**) registers the SEVENTH and LAST §5.1 network (network code `"BP"` per §4.2's supplier column) — the money model is deliberately the SIMPLE one (independently-given `net_local`/`vat_local`/`gross_local` read VERBATIM, Eurowag/Q8's shape; nothing in the harvested row says BP derives anything), and the network's quirks live elsewhere (`BA_fleet_fuel.md` §5.1: "PLN; monthly; Poland split-payment (MPP) mandatory; A2 toll ~2.5% ORS fee lines; FX via dated ECB rate with `month_config.FX` fallback"). It is the first ALL-PLN network, so ingestion's EXISTING dated-ECB-rate branch (`fx.to_eur` at each line's own `txn_date`, `fx_source="ecb"`, both amounts at the same resolved rate, official reference frozen) is its DEFAULT conversion path — and the harvested `month_config.FX` static fallback is deliberately NOT reproduced (master-context §4.15's "never a guessed number" wins: no coverage ⇒ `fx_rate_unavailable`, zero rows, the two-phase guarantee). THE ORS-FEE-LINE DECISION (documented in WO-69): an ORS fee line IS a `ParsedFuelLine` — an ordinary VAT-bearing statement line, never a coversheet adjustment and never merged into its toll line (§5.1 calls them fee LINES; §4.2 models non-fuel lines; DKV's 5.63% fee is the ON-INVOICE precedent): its 23% VAT is exactly as reclaimable, the batch tie-out ties the INVOICE total fee lines included, and the Art. 9 goods-code path stays honest (toll → `Toll/Fees` → code 4, ORS fee → `Service/Other` → code 10, fuel → `Diesel` → code 1); the contracted ~2.5% ratio is NEVER verified at capture (G3.4/M5 contract-audit territory) — ONE advisory warning surfaces the ORS count + per-currency ORS and toll gross sums so a reviewer can eyeball the ratio. THE MPP DECISION (documented in WO-69): Polish split-payment (mechanizm podzielonej płatności) is SETTLEMENT — the payment's VAT leg routes to the dedicated VAT account while the invoice's printed figures are unchanged (the same capture/settlement boundary WO-68's cash-at-pump decision drew); the parser scans the statement level for the statutory `MECHANIZM PODZIELONEJ PŁATNOŚCI` literal — present ⇒ ONE advisory warning with the per-currency VAT total ("settlement-side only; the invoiced figures and the VAT claim are unchanged"), absent ⇒ ONE fail-OPEN advisory (mandatory-network wording; an absent settlement annotation corrupts no captured figure, so blocking would invent policy; warnings never block, §2.1a); advisory totals are PER-CURRENCY, never a raw cross-currency sum (§4.14). Fail-closed: an unknown per-line `line_type` (`fuel`/`toll`/`ors_fee`) aborts the WHOLE statement — a guessed line kind would mis-map the goods code, the same discipline as TFC's `station_class` and Moeve's `payment`. Rule 9's HARVESTED PL dual entry `(23, 8)` coheres the 23% fuel/ORS + 8% A2-toll mix with no parser-side rate table (reused, never duplicated). Like Q8/DKV/TFC/Moeve, BP carries NO R20 claim of its own (no worked marker; `entities` always `[]`, the "never attempted" warning; entity via admin curation only) — **R20 stays CLOSED at exactly Eurowag and E100**. With BP/Aral shipped, ALL SEVEN §5.1 networks parse deterministically: **G3.2 is closed** (`docs/plan/plan-a/wo/WO-69-G3.2-slice7.md`); the transport capture backlog continues at G3.3 slice 2 (anti-drift), G3.4, G3.5. | `app/services/transport/fuel_card_parser.py`, `app/services/transport/parsers/eurowag.py`, `app/services/transport/parsers/e100.py`, `app/services/transport/parsers/q8.py`, `app/services/transport/parsers/dkv.py`, `app/services/transport/parsers/tfc.py`, `app/services/transport/parsers/moeve.py`, `app/services/transport/parsers/bp.py`, `app/services/transport/statement_ingest.py` | `backend/tests/transport/test_g3_2_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_statement_ingest.py`, `backend/tests/transport/test_g3_2_e100_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_e100_statement_ingest.py`, `backend/tests/transport/test_g3_2_q8_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_q8_statement_ingest.py`, `backend/tests/transport/test_g3_2_dkv_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_dkv_statement_ingest.py`, `backend/tests/transport/test_g3_2_tfc_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_tfc_statement_ingest.py`, `backend/tests/transport/test_g3_2_moeve_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_moeve_statement_ingest.py`, `backend/tests/transport/test_g3_2_bp_fuel_card_parser.py`, `backend/tests/transport/test_g3_2_bp_statement_ingest.py` | `BA_fleet_fuel.md` §3.B1, §4.2, §5.1 |
| R25 | **Two INDEPENDENT validation regimes** (WO-66, G3.3) — they answer different questions and stay structurally separate (`BA_fleet_fuel.md` §2.1a: "Two separate regimes"). **(1) The capture review gate** (`capture_review.check_line`/`validate_batch`): the nine-rule verdict lattice `ok < warn < error` — invoice number present (error); date `YYYY-MM-DD`, empty passes (warn); country in the 23-country set (warn); net/vat parse as numbers (error + early return); `net > 0` (error); `vat >= 0` (error); `vat <= net` (error); `net <= 5,000,000` (warn); VAT-rate coherence ±0.5pp of a known rate with `vat == 0` and unknown countries deliberately skipped (warn). Batch tie-out `abs(q2(Σ net+vat) − q2(coversheet_total)) <= 0.02` **compared on Decimals** (a boundary diff never flips on binary-float noise); commit gate `can_commit = (errors == 0) and (tie is None or tie.ok)`; **warnings never block**. Wired into `statement_ingest.ingest_statement` after parse and before FX — a blocked batch writes ZERO rows (`capture_review_blocked`), and the refusal names every error at once. The `VAT_RATES` table: the 23-country count, the ±0.5pp window, the skip rules and the four dual entries (PL 23/8, ES 21/10, EE 22/20, FI 24/25.5) are harvested; the remaining membership/rates are a documented reconstruction from public VAT law (the source table may not be copied). **(2) The engine tie-out** (`tie_out.check_period` + `fuel_tieout_expectations`): per-(entity, supplier, period, currency) figures a HUMAN types from the invoice PDF — `lines` tolerance 0 (EXACT, always), `gross_local` within a per-row [0.02, 0.05] tolerance (default 0.02), `net_eur`/`gross_eur` 0.05, `diesel_litres` (Diesel product-group only) 0.05 (interpreted — no harvested litres tolerance). `close.run_close` evaluates EVERY expectation row, then raises ONE `tie_out_failed` naming ALL failures; the job framework's whole-session rollback is the literal "the hand-off artifact is not written" — no claim-line rebuild survives a failed tie-out. Fail-OPEN by absence (an untyped supplier is not checked — the expectation IS the §5.1 "PASS = trained" onboarding target), fail-CLOSED once typed. The grain's `currency` column is a documented interpretation (WO-64 proved multi-currency statements; §4.14 forbids cross-currency `gross_local` sums). `set_expectation`/`remove_expectation` are audited (old→new) and tenant-opaque; the table ships FORCE RLS in its own migration (`b3f1c6d2a904`). **WO-77 route consumer (regime 2):** `GET/PUT /api/v1/transport/tie-out-expectations` + `DELETE` by natural key (VAT_READ/VAT_WRITE) — the typed figures cross the wire as Decimal-backed strings and the SERVICE stays the single point that quantizes them (§4.9); the harvested tolerance band is proven on the wire both ways (0.05 accepted, 0.06 refused 422 `invalid_gross_tolerance`), alongside 422 `invalid_period`/`invalid_currency`/`invalid_expected_lines`, 404 `entity_not_found` and 404 `tieout_expectation_not_found` (`backend/tests/transport/test_wo77_admin_routes.py`). The check RUN itself stays inside the close (R31/R60) — no route runs it. **WO-80 UI consumer:** `/vat-admin`'s "Tie-out expectations" panel types, retypes and removes them, and states the CONSEQUENCE in words ("These stop the monthly close", and absence never halts anything) — the deliberate contrast with the advisory receipt-control panel beside it. Figures render and re-post as the exact wire strings (a 16-digit gross survives the round-trip), untyped optional figures post as `null` not zero, and `invalid_gross_tolerance` renders the harvested 0.02–0.05 band (`frontend/e2e/vat-admin.spec.ts`). Anti-drift (`extraction_baseline`/`regression_check`) is deliberately NOT this row — named follow-up G3.3 slice 2. | `app/services/transport/capture_review.py`, `app/services/transport/tie_out.py`, `app/models/transport/tie_out.py` (`FuelTieOutExpectation`), `app/services/transport/statement_ingest.py` (the gate wiring), `app/services/transport/close.py` (the halt) | `backend/tests/transport/test_g3_3_capture_review.py`, `backend/tests/transport/test_g3_3_capture_gate_ingest.py`, `backend/tests/transport/test_g3_3_tie_out.py`, `backend/tests/transport/test_g3_3_close_halts_on_tie_out.py` | `BA_fleet_fuel.md` §2.1a, §5.1, R25, Appendix B "VALIDATION" |
| R26 | **Deterministic advisory post-capture checks** (WO-71, G3.4) — the harvested `capture_checks.py`, "advisory only" (`BA_fleet_fuel.md` §3.I7): (1) IBAN ISO 13616 + MOD-97, severity **error**, composing `app/core/bank_id.py` (never reimplemented); unknown country prefix = UNCHECKABLE = no finding — the one documented divergence from the SEPA path's fail-closed posture, which stays untouched. (2) VAT-ID **structural** check per the public VIES format table (EU-27, `EL`→GR), severity **warn**; registry allocation ranges deliberately NOT modelled ("fail toward not crying wolf"). (3) `vies_check` — NO I/O AT ALL, always `not_checked` (the live VIES lookup is deliberately not inline; a future online job is new scope no R-rule requires). (4) Cross-entity invoice duplicates: same normalized ref (uppercase + strip spaces, separators KEPT) + equal `q2` amount + SAME currency, across ALL entities in the org; prior registration = **error**, in-batch normalization collision = **warn**; the current `(entity, supplier, period)` ingestion group is excluded so idempotent replays never self-flag. EVERY finding is advisory (§4.19): `CheckFinding` is deliberately a different type from `capture_review`'s lattice, so an error-severity advisory finding can never block registration — proven end to end (rows written WITH the warning). Warnings ordering extends WO-70's contract: parser → review → **post-capture** → drift. Nothing persisted — no table, no migration. | `app/services/transport/capture_checks.py`, `app/services/transport/statement_ingest.py` (the wiring) | `backend/tests/transport/test_g3_4_capture_checks.py`, `backend/tests/transport/test_g3_4_checks_on_ingest.py` | `BA_fleet_fuel.md` §3.I7, R26 |
| G3.5 (§3.J — no dedicated R-number) | **Receipt control: cadence × activity, orphan check, overrides survive** (WO-72, G3.5) — the harvested `invoice_control.py` (`BA_fleet_fuel.md` §3.J, §2.3: "A missing invoice is un-recoverable VAT. Cadence × activity is the only way to know what SHOULD have arrived"). (1) CADENCE: the three-value closed set `semi-monthly` / `monthly` / `monthly-per-country` (§3.J item 1, verbatim), configured per free-text supplier code in `vat_supplier_cadences` (`set_cadence`/`remove_cadence`, audited old→new; the WO-61 registry precedent — never a column on the AP `Vendor`), over the harvested §5.1 per-network defaults (`DEFAULT_CADENCES`: E100/DKV semi-monthly, MOEVE/BP/TFC/PORTONE monthly, Q8 monthly-per-country; Eurowag deliberately absent, §5.1 "—"); admin row beats default; neither ⇒ NO expectation at all (opt-in by absence, the R61/WO-66 discipline — the supplier is only counted as `unmanaged_suppliers`). (2) EXPECTATION = cadence × ACTIVITY (`run_receipt_control`): slots per (entity, supplier, period) — monthly `M`; semi-monthly `H1` days 1-15 / `H2` day 16-end (the 15/16 split is a documented interpretation of "one invoice per half-month"); monthly-per-country one `M` slot per country WITH activity (an inactive country is structurally unenumerable, verbatim). A slot with no transactions is `no_activity` — "no invoice expected, OK", NEVER chased. (3) CROSS-CONTROL: ≥1 slot transaction resolving via the ONE `invoice_match.resolve_invoice_ref` (C3 — never a second matching heuristic) ⇒ `received_doc` (≥1 resolved invoice documented via WO-58's `extraction.invoice_ids_with_documents` batch seam, one query per run) or `received_no_doc`; none ⇒ `missing` ("chase the supplier"). Plus the ORPHAN check (`orphan_transactions`): every transaction must be covered by a registered invoice — transaction-grain, read-only, persists nothing; a synthetic `INPUT` ref is always an orphan. (4) PERSISTENCE + OVERRIDES SURVIVE (§3.J item 4): results persist in `vat_receipt_controls` (natural key org × entity × supplier × period × slot × country, `country=""` never NULL); the re-run upsert writes ONLY the computed columns (`status`/`txn_count`) and never deletes a row; `set_control_override` is the ONLY writer of `waived`/`note` (grep-proven, the R5 structural pattern) — a slot waiver is a worklist mute, NOT WO-58's claim-level legal waiver (R15), and has ZERO effect on any claim/line/lock/freeze/checklist/ingest. ADVISORY THROUGHOUT (master-context §4.19): §3.J's verbs are "answers"/"persist"/"chase" — never "block"/"halt" (the deliberate contrast with R25); a `missing` slot never gates anything, proven end to end (a claim over a period with missing + waived rows submits identically). `close.run_close` runs the control as its `run_control` stage (§3.H H4's harvested stage list) AFTER the claim-line rebuild — findings never fail the close; an exception still halts per R31, and the R25 tie-out gate still halts BEFORE the stage. **WO-77 route consumers:** `GET /transport/cadences` + `PUT/DELETE /transport/cadences/{supplier}` (the admin assignment; 422 `invalid_cadence` on anything outside the harvested closed set — absence in the list is NOT "no cadence", the per-network defaults still resolve in `cadence_for`), `GET /transport/receipt-controls?period=` (the persisted grid) and `POST /transport/receipt-controls/{id}/override` (the ONLY writer of `waived`/`note`; the computed `status` column is visibly untouched by an override, and the override survives into the next read). `run_receipt_control`/`orphan_transactions` are deliberately NOT routed — the RUN is the close's stage and R60 forbids running the close inline in a web request. Advisory throughout: every one of these reads changes no claim (`backend/tests/transport/test_wo77_admin_routes.py`). **WO-80 UI consumers:** `/vat-admin`'s "Cadences" panel (assign from the harvested closed set / drop back to the default; `invalid_cadence` as a sentence) and its "Receipt control" panel (the persisted grid per period + the mute/annotate override). The advisory posture is carried in the COPY and asserted as text — the board states it "blocks no claim, halts no close and changes no figure", and the mute is named as a worklist mute, not R15's claim-level waiver; no run/resolve/block verb is offered because none exists (`frontend/e2e/vat-admin.spec.ts`). | `app/models/transport/receipt_control.py` (`VatSupplierCadence`, `VatReceiptControl`), `app/services/transport/receipt_control.py`, `app/services/transport/close.py` (the stage) | `backend/tests/transport/test_g3_5_cadence.py`, `backend/tests/transport/test_g3_5_receipt_control.py`, `backend/tests/transport/test_g3_5_orphan_check.py`, `backend/tests/transport/test_g3_5_close_runs_control.py` | `BA_fleet_fuel.md` §3.J, §2.3, §3.H H4, §5.1 |

| R44 | **M** | **Customer lifecycle `prospect → pending → active → inactive`, every legal/claim gate keyed on `active`** (WO-73, G2.11 — `BA_fleet_fuel.md` §3.F F1/F3, §3.E "Activation gates layered on top", §4's state tables). Lifecycle + the per-(customer × refund country) activation ladder (`(none) → requested → active`) live on TWO transport-local tenant tables keyed to `issuer_profiles` via the composite `(org_id, entity_id)` RESTRICT FK (the WO-61/WO-72 registry precedent — never columns on the shared AP/AR `IssuerProfile`; the harvested `customers.db` was itself a separate store, §4.1). The edge set is EXACTLY the harvested one: `add_prospect` ((none)→prospect, idempotent, NEVER downgrades a real client of any status — the harvested `company_name` idempotency key translated to this codebase's customer identity, the entity row), `promote_prospect` (prospect→pending, the onboarding handoff), `set_activation` (F1's pending↔active toggle), `set_inactive` (§4's churn edge; `inactive` is TERMINAL in this slice — no re-onboarding edge is harvested, recorded rather than invented), `request_country`/`set_country_activation` (the linear country ladder; activating a never-requested country refused). Every real transition is an explicit operator call, audited old→new (§4.16); idempotent no-ops audit nothing. THE GATE: `enforce_activation` — the ONE predicate (the `is_synthetic()` centralization discipline) — refuses `customer_not_active` (§3.E bullet 1's wording) unless the lifecycle row is `active`, then `country_not_activated` (§3.E bullet 2's PoA wording) unless the refund country's row is `active`; FAILS CLOSED on absence in both halves (an unonboarded entity must never out-privilege an onboarded pending one). Wired into `lock.submit_claim` AFTER the R8 minimum and BEFORE the R6 duplicate block — D5's "checklist → period-end → minimum → then `set_status(engine)`" places the engine group there, and §3.E's "layered on top (`set_status`…)" puts activation at that group's head; a refused claim never freezes or locks (the D5 nothing-mutated property, proven). The gate is UNCONDITIONAL (Fleet Fuel's `gate_activation=False` escape existed because the checklist superseded the coarse flags on its second path; here the checklist checks data, never the status flag, R44's acceptance names the ACTIVATION message, and there is exactly one submission path). Preparation surfaces stay deliberately UNGATED: `get_or_create_claim`, `build_claim_lines`, `submission_checklist` and `run_close` all work for a prospect/pending entity — F1 scopes the rule to legal/claim GATES, and the harvested 1A stage exists precisely to describe an incomplete customer's draft claim. `country_requirements` + `country_ready_to_activate` (F3's informational helper, "not a gate") are deferred to the customer-document-store slice WO-60 already named — deferred NOT silently skipped (the R45 partial-harvest precedent); preserved by construction meanwhile (the only activation writers are the explicit audited calls). **WO-76:** 409 `customer_not_active` / `country_not_activated` over HTTP from the submit route (`backend/tests/transport/test_wo76_claim_routes.py`). **WO-77 route consumers — the gap this module's own docstring recorded ("no `api/routes/transport/*` route exists yet, so the admin-click requirement lands as the documented intent for that route's permission") is now CLOSED:** `GET /transport/customers/{entity_id}/lifecycle` (the lifecycle row + every country-activation row; a `null` status is the MEANINGFUL "never onboarded" absence, reported not 404'd — the 404 keys off the ENTITY) and the six transition routes `/prospect`, `/promote`, `/activation`, `/inactive`, `/countries/{country}/request`, `/countries/{country}/activation`, each VAT_WRITE (they govern whether a FUTURE submission may proceed; the gate itself stays fail-CLOSED inside `submit_claim`). The full ladder is proven over HTTP, including that activation is never skippable (409 `lifecycle_transition_invalid` from prospect), `not_a_prospect`, `country_not_requested`, `country_transition_invalid`, 422 `invalid_country`, `inactive` being terminal — and end to end that activating over HTTP is exactly what lets a submission past R44 (`backend/tests/transport/test_wo77_admin_routes.py`). **WO-78 UI consumer:** 409 `customer_not_active` / `country_not_activated` render as "This entity isn't activated for refund claims" / "This refund country isn't activated for the entity" with the onboarding step named. **WO-80 UI consumer:** `/vat-customers` is the screen those two sentences now send an operator to — the lifecycle state and every country-activation row, with only the transitions legal from the current state offered (mirrored from this service in `frontend/src/lib/transportAdmin.ts`, cosmetic per §6). A `null` status renders as "Never onboarded", the real state the gate refuses, never as blank or an error; `inactive` offers no step and the page says why rather than inventing a re-onboarding edge; `not_a_prospect`/`lifecycle_transition_invalid`/`country_not_requested`/`country_transition_invalid`/`invalid_country` each render as sentences (`frontend/e2e/vat-admin.spec.ts`). | `app/models/transport/customer_lifecycle.py` (`VatCustomerLifecycle`, `VatCountryActivation`), `app/services/transport/customer_lifecycle.py` (`enforce_activation`, the transitions), `app/services/transport/lock.py` (`submit_claim`, the wiring) | `backend/tests/transport/test_g2_11_customer_lifecycle.py`, `backend/tests/transport/test_g2_11_activation_gate.py` (R44's acceptance verbatim: a prospect's claim → 409 `customer_not_active` with the activation message) | `BA_fleet_fuel.md` §3.F F1/F3, §3.E, §3.D D5; Art. 8(1) Dir. 2008/9/EC (the payout mandate the activation checklist exists to secure) |
| G2.12 (§3.K K6, §2.2, §3.M M1 — no dedicated R-number) | **Evidence pack + claim workbook** (WO-74, G2.12) — the two filing artifacts of a FROZEN claim, both rendered from ONE loaded pack (`claim_pack._load_pack` → `_render_workbook`; the evidence pack embeds the workbook rendered from the SAME pack, so `ARCH_plan.md`'s acceptance "the workbook and the evidence pack agree line-for-line" is structural, the R41 both-artifacts-one-source discipline). Workbook = `Claim` header sheet + `Lines` at the R2 grain (one row per (invoice, product code), Art. 9 goods code per line) + a TOTAL row equal to the frozen `vat_eur`/`vat_local` — a recomputed `q2` sum that REFUSES on drift (`claim_totals_drift`, §4.10). Evidence pack = ZIP filing bundle under the human-navigable vault tree `<Customer> <RegNo>/<Year>/<Country>/<Period label>/` (§3.M M1 — every segment Windows/SharePoint/FTP-sanitized; `period_label` `Qn`/`Annual`) containing the workbook, every vaulted invoice document (SHA-256-deduped; missing object-store bytes refuse, `evidence_document_unavailable`) and a `manifest.csv` of per-file SHA-256s (§3.K K4's manifest discipline). Frozen lines only (`claim_not_frozen` for a draft — the G2.5/ADR-P3 translation of Fleet Fuel's separate legal DB); any synthetic frozen line refuses via THE R3 predicate (`synthetic_line_in_pack`). Read-only: persists nothing, mutates nothing, audits nothing (grep-proven). **WO-77 route consumers — the "no route yet" status is CLOSED:** `GET /api/v1/transport/claims/{id}/workbook` (xlsx) and `GET .../evidence` (ZIP), both VAT_READ (a read-only rendering of claim data the `/lines` route already serves, deliberately NOT `EXPORT_RUN`, which guards the accounting-ledger export hub), served as bytes with the shared injection-safe `content_disposition` + `X-Content-Type-Options: nosniff`. The tests parse the REAL bytes: openpyxl reads the `Claim`/`Lines` sheets and asserts the TOTAL row equals the claim's frozen `vat_eur`; zipfile reads the bundle and re-hashes every entry against its `manifest.csv` SHA-256 row. All four refusals surface verbatim over HTTP — 409 `claim_not_frozen` on a draft, `claim_totals_drift` after a header tamper, `synthetic_line_in_pack` after a frozen-ref tamper, and `evidence_document_unavailable` on a dangling vault reference while the workbook still renders (`backend/tests/transport/test_wo77_admin_routes.py`). **WO-78 UI consumer:** the claim detail page's "Claim workbook" and "Evidence pack" buttons hit the WO-77 download routes; every refusal (`claim_not_frozen`, `synthetic_line_in_pack`, `claim_totals_drift`, `claim_currency_mismatch`, `evidence_document_unavailable`) renders as a human sentence — the evidence one explicitly states the workbook still downloads. | `app/services/transport/claim_pack.py` (`build_workbook`, `build_evidence_pack`, `vault_prefix`, `period_label`) | `backend/tests/transport/test_g2_12_claim_pack.py` | `BA_fleet_fuel.md` §2.2 (line 204), §3.K K6, §3.M M1, §3.C C2; Art. 10/15 Dir. 2008/9/EC |
| R38 | **M** | **Cash-recovery dashboard** (WO-81, G4.3 — the first M5 row): every claim of a refund year bucketed into the six harvested readiness states (`ready · deadline · missing · below · submitted · paid`, `BA_fleet_fuel.md` §2.4) with the north-star euros beside them — recovered, awaiting, claimable, the deadline-risk count and the median days-to-refund. **R38's binding constraint is honoured literally: nothing here computes a figure of its own.** **WO-85 (G4.1/R51) turns that from discipline into structure:** every row-selection `recovery_dashboard`'s inputs reach now goes through `app/services/transport/queries.py`, the canonical query registry, and `backend/tests/transport/test_wo85_canonical_queries.py` refuses — by AST scan, with a seeded-violation self-test — any `select()` over `fuel_transactions`/`vat_claim_lines` or any `<Model>.org_id` filter written outside it. R38's *"never a forked query"* is no longer a promise a future consumer can quietly break. The claim set comes from `claim.list_claims` (which gains an ADDITIVE `year` keyword so the ONE listing query stays one), a draft's stage from `status.derive_stage` (R17/D3), its VAT base from `freeze.preview_vat_base` (the same preview `lock.submit_claim` gates R8 on), the Art. 17 threshold from `minimum.below_minimum` and the time-bar from `deadline.deadline_status` (R9) — no forked query, no second implementation, and a structural test pins the absence of one. FOUR documented interpretations the spec does not settle: stage `1B` (period not ended) folds into `ready` (nothing to fix; no seventh bucket is invented, §10); a waiver-only `1C` is `ready`, not `below` (only a THRESHOLD caveat is a threshold problem, so `below_minimum` is re-asked rather than inferred from the shared code); `deadline` means "otherwise fileable with the clock running", so a `missing`/`below` claim inside the window keeps its BLOCKING bucket and is still counted in the SEPARATE `deadline_risk_claims` (which is why R38 lists that count apart from the six states); and `withdrawn`/`rejected` match none of the six and are reported in an explicit `excluded` block by reason — `Σ buckets + Σ excluded == total_claims` is asserted, so a silent drop can never make the row count lie. **§4.14 over multi-country claims:** every euro is the single-currency `vat_eur` — `vat_local` and the currency-ambiguous `paid_amount` are never read (an AST assertion pins that absence), and a DRAFT whose lines span currencies is caught PER CLAIM (`preview_vat_base` refuses it): it buckets `missing`, contributes `0.00`, and increments `currency_mismatch_claims` so the shortfall is visible — one bad claim never blanks the year, and no foreign amount is ever labelled EUR. §4.9 throughout, including an exact-`Decimal` median (no `statistics`, no float). R38's acceptance ("the dashboard totals reconcile exactly") is asserted directly: `recovered + awaiting + claimable` equals the whole bucketed VAT. **Route consumer:** `GET /api/v1/transport/recovery-dashboard?year=` on `TRANSPORT_READ` — the permission WO-79's `fuel.py` explicitly RESERVED for this derived analytics slice ("it returns no claim row, no evidence, no object id, only aggregates"); Decimal-as-string on the wire, 200-with-zeroes on an empty year, 422 `invalid_year` on a year no `ref_period` could carry, 403 `module_not_enabled` unentitled. READ-ONLY: no write, no audit row, no commit, and reading it twice leaves every claim column byte-identical. **Two DEVIATIONS were recorded, not faked; the FIRST is now CLOSED (WO-82):** the `overcharges` euro needed `contract_audit`/`overcharge.py` (board **G4.5/R41**, then absent from this codebase) so the field was OMITTED rather than emitted as a misleading zero — **WO-82 shipped G4.5, so `recovery_dashboard` now carries `overcharges_eur`, obtained by CALLING `overcharge.recovered_total(year=…)` (R38's never-a-forked-query clause honoured for the sixth time: this module still holds no query and no arithmetic of its own). It is §2.4's BOOKED-CASH reading ("`recovered_total()` = the booked-cash north star"), not the €-exposure detected, and it sits deliberately OUTSIDE the `recovered + awaiting + claimable` VAT reconciliation — a second cash stream over the same ledger, not a component of the first; §4.14 needs no special handling for it because it is natively EUR.** The second deviation stands: and nothing writes `submitted_date`/`paid_date` yet (G2.9, decision-gated), so `median_days_to_refund` ships against the harvested definition with a `days_to_refund_sample` beside it — `null` reads as "no claim has both dates", never as zero days. | `app/services/transport/recovery.py` (`recovery_dashboard`, `READINESS_STATES`, `validate_year`), `app/services/transport/claim.py` (`list_claims(year=…)`), `app/schemas/transport_recovery.py`, `app/api/routes/transport/recovery.py`; **UI consumer (WO-86):** `frontend/src/pages/RecoveryDashboard.tsx` — the six buckets, the north-star euros, deadline risk kept OUTSIDE the buckets, and both honest nulls rendered as facts (`median_days_to_refund` null ⇒ "not yet measurable" + its sample size; `currency_mismatch_claims` > 0 ⇒ a named notice explaining the €0.00 contribution) | `backend/tests/transport/test_wo81_recovery.py`, `frontend/e2e/recovery.spec.ts` | `BA_fleet_fuel.md` §2.4 (line 218), R38 (line 1410), A3/A4, §3.D; Art. 15/17 Dir. 2008/9/EC; CJEU C-294/11 *Elsacom* |
| R41 | **M** | **Supplier overcharge claim-back** (WO-82, G4.5 — the second M5 row, and the order that CLOSES WO-81's deviation 1). TWO halves, structurally separate. **(1) DETECTION** (`contract_audit.py`, READ-ONLY over `fuel_transactions`): the harvested contract audit, `BA_fleet_fuel.md` §2.5 verbatim — *"two term types only"* (`expected_discount_eur_l`, a rebate in €/L; `max_net_eur_l`, a contracted NET price ceiling in €/L) on §4.4's own `supplier_discounts` grain (`supplier × country × station_like × product_group`, `active`), the two flag strings verbatim (`"short discount"` when `applied < expected − tol`, `"over ceiling"` when `eff_l > max + tol`) at the harvested **`TOLERANCE = 0.005 €/L`**, and `recover_eur = gap × litres`, *"dropped if ≤ 0"* — which is also what makes the EXACT-BOUNDARY case (applied == expected, `eur_l_eff` == max) produce nothing at all rather than a zero-euro finding (proven on BOTH sides one 0.0001 step apart). §2.5's closing sentence is honoured as a CONSTRAINT: **no volume-tier / stepped-rebate / annual-bonus / card-fee modelling** — a term the model cannot express is not approximated. BASIS: **NET EUR/L, final — VAT excluded, rebates applied** (§3.G G1 / R49, verified against the spec rather than assumed) — `eur_l_eff = net_eur_eff / qty`, `eur_l_doc = net_eur / qty`, and the discount ACTUALLY applied is their difference, which is §4.2's own two-tier discount model (on-invoice *"already inside `net_eur`"*, off-invoice *"lands ONLY in `net_eur_eff`"*) read as an identity, not an invention; the basis string AND R53's legal framing (*"Money the supplier owes"* — a contractual claim-back, deliberately NOT the overpay analysis's *"negotiation evidence"*) ride the result itself so no surface can render the euro without them. **§4.9**: comparisons on UNROUNDED `Decimal` quotients so a boundary can never flip on a presentation rounding, the gap quantized ONCE to 4 dp and the euro once through `money.q2`, so gap × litres × euro reconcile exactly. **§4.14**: EUR-ONLY and never a coercion — both price columns are EUR by construction (ingestion refuses a line it cannot convert), so a supplier month spanning EUR and PLN totals natively in EUR; the three document-currency amount columns are NEVER read (asserted structurally by name) and a line's own currency rides each finding as PROVENANCE that is never totalled. THREE fail-safe readings, each documented: exactly ONE term applies per line (most specific wins — a wildcard and a station-specific term both matching would DOUBLE-COUNT real money); NO configured term ⇒ **no finding at all**, counted as `lines_without_terms` (*"we have not told the system what was agreed"* is not *"the supplier honoured the contract"* — never a false positive); a `qty <= 0` line (§4.2's promo correction) is SKIPPED and counted, never divided by. Detection writes nothing — no `db.add`, no attribute assignment, no audit row (R41's own *"read-only over the analytics"*), proven structurally AND by a column-for-column before/after comparison of every fuel transaction. **(2) THE CLAIM-BACK LIFECYCLE** (`overcharge.py`, own tenant table): §4.5's chain drawn LITERALLY — `detected → packaged → claimed → recovered \| rejected \| written_off`, the three outcomes terminal, **no shortcut edge invented** (the WO-73 `inactive`-is-terminal precedent). An illegal edge refuses 409 `overcharge_transition_invalid` with NOTHING mutated (asserted column-for-column); every real move is audited old→new in the SAME transaction (§4.16). The euro is FROZEN at `open_claim` (the G2.5/ADR-P3 reasoning applied to a demand letter: the figure quoted to a supplier must not move under the operator when a later ingest changes a line), `open_claim` is idempotent on `(org, supplier, period)` and refuses 422 `no_overcharge_detected` when the audit found nothing (a claim-back at €0 is a letter demanding nothing). `recovered_eur` is bounded to `(0, detected_eur]` — master-context §4 invariant 13 (no over-crediting) applied to this ledger, since a north-star total unbounded by its own evidence is not a measurement. **`recovered_total`** is §2.4's *"booked-cash north star"* and is the figure `recovery.recovery_dashboard` now reports as `overcharges_eur` — R38's completed consumer. The €-exposure DETECTED and the € BOOKED BACK are never reported under one name (R52's label-them-distinctly discipline). **Route consumer:** `GET/PUT/DELETE /api/v1/transport/contract-terms`, `GET /transport/overcharges/audit`, `GET/POST /transport/overcharges`, `GET /transport/overcharges/total`, `GET /transport/overcharges/{id}` and `POST /transport/overcharges/{id}/advance` — router-level **`TRANSPORT_READ`** (the reservation `fuel.py` recorded names `overcharges.py` in as many words) with the write verbs overriding to **`VAT_WRITE`** (the `admin.py` `_WRITE` pattern; `VAT_SUBMIT` stays reserved for lock-acquiring VAT-claim actions, and an overcharge claim-back touches no claim, no lock and no VAT figure). NO permission member invented; the split is load-bearing and proven — an AUDITOR holds `TRANSPORT_READ` and not `VAT_WRITE`, so it reads the worklist and cannot drive it. Both new tables ship FORCE RLS in their own migration (`a7c2e9f14b58`) and were converted STRAIGHT into real tenancy-parity HTTP probes over deliberately overlapping data (same supplier code, same period, same amounts). **(3) THE TWO SEND-READY ARTIFACTS — SHIPPED (WO-83), R41 CLOSED.** `overcharge_pack.py` renders the Excel EVIDENCE PACKET and the formal PDF CLAIM LETTER from the ONE line source R41 names: `contract_audit.audit()`, the same call `open_claim` snapshots. *"Both artifacts for the same (supplier, period) show identical lines and totals"* is STRUCTURAL three times over — one `_load_packet()` (loaded once, rendered twice), one `_COLUMNS` spec both renderers project through the same accessors, and two SYNC renderers that hold no `AsyncSession` and therefore cannot reach a second source — and is then asserted CELL-FOR-CELL across the parsed bytes of both (openpyxl over the `Evidence` sheet, pypdf over the letter text, both against hand-computed Decimals), the G2.12/`claim_pack` precedent applied to the surface `claim_pack`'s own docstring said it was standing in for. The packet carries the breach lines (flag, agreed/actual €/L, gap, litres, recoverable EUR) with a TOTAL row plus workbook-only LINEAGE columns (claimant entity, invoiced/effective €/L, document currency, transaction id); the letter carries OUR letterhead from the existing `IssuerProfile` registry (resolved READ-ONLY — never `get_or_create`, which would commit a row), the supplier's own per-country legal entity (R20/R21, marker-only via `supplier_entity.get_registration`; an unregistered country prints the supplier code and invents nothing), the SAME table, and §2.4's demand verbatim — a **credit note or refund within 30 days** (`DEMAND_DAYS = 30`, harvested twice at §2.4 lines 221/236), with the due date printed as letter date + 30. **R53 is carried, not paraphrased:** both artifacts PRINT `LEGAL_FRAMING` and `PRICE_BASIS` — the same constants the JSON API returns — so a surface cannot drift from the wire. FOUR fail-CLOSED refusals, each because the alternative is a MISLEADING document: `no_overcharge_detected` (422 — an empty packet reads as *"we found nothing owed"*, a different and false statement); `overcharge_evidence_drift` (409 — the live line source no longer reproduces the FROZEN `detected_eur`, so the demand and its own enclosure disagree: the `claim_pack.claim_totals_drift` twin, §4.10; the missing re-snapshot edge is RECORDED in `docs/DECISIONS-NEEDED.md` §13, never invented); `overcharge_claim_closed` (409, LETTER ONLY — a live 30-day demand must not assert a debt the ledger records as `recovered`/`rejected`/`written_off`, while the PACKET stays reproducible in every state per WO-74's *"reproducing what WAS filed is audit-trail behaviour"*); and `issuer_profile_incomplete` (409, letter only, naming the missing Art. 226 fields). **Route consumers:** `GET /transport/overcharges/{id}/packet` and `GET /transport/overcharges/{id}/letter` on the SAME router-level `TRANSPORT_READ` — generation is a READ (nothing persisted, mutated, audited or committed), which is why an AUDITOR downloads both and still cannot advance the worklist; correct media types, `content_disposition` filename and `nosniff`, the WO-77 artifact-download precedent. NO new table, NO migration, NO new permission and NO new dependency (openpyxl and reportlab are already pinned; degradation mirrors `invoice_pdf.PdfUnavailable` as a 503 `pdf_renderer_unavailable` `AppError`, so the route stays a thin controller). Building both artifacts leaves the claim-back and every fuel-transaction column byte-identical and writes no audit row — asserted, not promised. **WO-84 (G4.2/R50) CONSUMER UPDATE:** detection compares against `net_eur_eff`, which until WO-84 was an exact copy of `net_eur` on every production row — so `applied` was identically `0.0000` and this rule's `short discount` flag over-claimed by the WHOLE contracted rebate against any supplier paying off-invoice, a figure `open_claim` freezes and the claim letter prints. The rebate merge makes the comparison complete; R41's own arithmetic, artifacts and lifecycle are BYTE-UNCHANGED (both artifacts quote the post-merge figure by construction — one line source, `contract_audit.audit()`, never forked), and the existing `overcharge_evidence_drift` 409 is the CORRECT response when a merge lands after a claim froze its `detected_eur`. `ContractAuditResult` additionally carries R50's advisory `source_warnings` (§4.20). | `app/models/transport/contract_term.py` (`VatSupplierContractTerm`), `app/models/transport/overcharge.py` (`VatOverchargeClaim`), `app/services/transport/contract_audit.py` (`TOLERANCE`, `FLAG_SHORT_DISCOUNT`, `FLAG_OVER_CEILING`, `PRICE_BASIS`, `LEGAL_FRAMING`, `set_term`, `audit`), `app/services/transport/overcharge.py` (`TRANSITIONS`, `open_claim`, `advance_claim`, `recovered_total`), `app/services/transport/overcharge_pack.py` (`_load_packet`, `_COLUMNS`, `build_evidence_packet`, `build_claim_letter`, `DEMAND_DAYS`), `app/services/transport/recovery.py` (`overcharges_eur`), `app/schemas/transport_overcharge.py`, `app/api/routes/transport/overcharges.py`; **UI consumer (WO-86):** `frontend/src/pages/Overcharges.tsx` (detection · claim-back ladder from a verbatim `TRANSITIONS` mirror · both artifacts) + `frontend/src/lib/transportRecovery.ts` | `backend/tests/transport/test_wo82_contract_audit.py`, `backend/tests/transport/test_wo82_overcharge_lifecycle.py`, `backend/tests/transport/test_wo82_overcharge_routes.py`, `backend/tests/transport/test_wo83_overcharge_artifacts.py`, `backend/tests/transport/test_wo83_overcharge_artifact_routes.py`, `frontend/e2e/recovery.spec.ts` | `BA_fleet_fuel.md` §2.5 "Contract audit" (line 250), §4.4 (line 1066), §4.5 (line 1082), §2.4 (lines 221/236), R41 (line 1413), R49/R52/R53, §3.G G1 |
| R53 | **S** | **Legal framing per analysis must not be flattened** (WO-83, G4.5 — R53's FIRST consumer; the rule itself was materialised as a constant by WO-82 and is now PRINTED). `BA_fleet_fuel.md` R53, verbatim: *"contract breach = 'money the supplier owes' (claim letter); same-day overpay = 'negotiation evidence, NOT a contractual claim-back' (printed on every sheet); peer/excise/estimate = 'indicative, verify'"*, with the acceptance *"Each workbook carries its framing text."* The contract-breach framing is `contract_audit.LEGAL_FRAMING` — *"Money the supplier owes — a contractual claim-back, not negotiation evidence"* — and it rides the `ContractAuditResult` itself, so the JSON API (`GET /transport/overcharges/audit`), the Excel evidence packet (BOTH the `Claim-back` header sheet AND the `Evidence` sheet that carries the numbers) and the PDF claim letter all render the SAME STRING: a paraphrase cannot drift from surface to surface because there is only one. Printed beside it on every surface is §3.G G1 / R49's price basis (`PRICE_BASIS` = *"NET EUR/L, final — VAT excluded, rebates applied"*), which G1 requires *"on any new report surface"*. The framing is not decoration — it is the reason this analysis alone gets a formal demand with a 30-day deadline while the same-day overpay figure (R52's other grain) gets none, and why §2.4's framing table is reproduced as a table rather than collapsed into one advisory sentence. **R53's SECOND CONSUMER LANDED WITH WO-87 (G4.7).** `savings.LEGAL_FRAMING` is R53's own words for the overpay analyses — *"Negotiation evidence, NOT a contractual claim-back"* — and it rides all three of `same_day_overpay`/`internal_benchmark`/`expected_rebate`, the three response schemas and the three GETs, exactly as the first framing rides `ContractAuditResult`. **The separation is STRUCTURAL, not editorial**, which is the point: an overpay figure is not a debt (nobody agreed a supplier would match the cheapest rival on the day, so there is no term, no breach and nothing owed), and WO-83 had already made the FIRST framing client-reachable as a 30-day payment demand on the client's own letterhead. Four asserted absences hold the two apart — (1) different constants, each stating what it means; (2) no field name on any `savings` result dataclass, any `transport_savings` schema or any route path contains `recover`/`owed`/`owes`/`claim`/`demand`/`due`/`debt`/`payable` (scanned with a seeded-violation self-test), so there is no `recoverable_eur` for a client to render as a demand; (3) an AST import scan in BOTH directions — `savings` imports no claim-back module and no claim-back module (service OR route) imports `savings`, so no overpay euro can reach `overcharge.detected_eur`, the figure the demand letter prints; (4) the savings router declares **no write verb at all** (`methods == {"GET"}`, and a POST is a 405 on the wire) — there is nothing to open, freeze, package or send, which is what makes "this is not a claim" a property of the surface rather than a sentence on it. **STILL OPEN:** the *"indicative / advisory — verify before relying"* framing rides **G4.6** (excise) and **G4.8** (refund estimate); neither analysis exists yet and neither is stubbed here (master-context §10). **THE SECOND FRAMING GAINED ITS UI CONSUMER WITH WO-90** (`/savings`), and the four structural absences were carried into the SPA rather than restated there — which is the only way a framing survives a screen. (1) `legal_framing` and `price_basis` are rendered VERBATIM off each of the three responses on each of the three panels: there is ONE string, so a re-wording in the SPA cannot soften it (the WO-86 `Overcharges.tsx` precedent, applied to the other framing). (2) No identifier, string or comment in `frontend/src/pages/Savings.tsx` or `frontend/src/lib/transportSavings.ts`, and no FIELD NAME in the eight new `lib/types.ts` wire interfaces, carries `recover`/`owed`/`owes`/`claim`/`demand`/`due`/`debt`/`payable` — scanned at source at WORD BOUNDARIES (so `recoverable_eur` is caught while the innocent substring in "lowest" is not) with a seeded-violation SELF-TEST asserting the scan still catches a `recoverable_eur`, an "amount owed" heading, an "open a claim-back" button and a "send the demand letter" comment. (3) The page names no path into the contract-breach flow and no link inside its `<main>` points at one, asserted in both the source and the DOM — so no overpay euro can travel into the frozen figure a formal letter prints. (4) The page exposes NO mutating control: no form, and every button on the surface is asserted to be a tab. Nothing on the screen offers to send anything to a supplier. | `app/services/transport/contract_audit.py` (`LEGAL_FRAMING`, `PRICE_BASIS`), `app/services/transport/overcharge_pack.py` (both renderers print them), `app/api/routes/transport/overcharges.py`; **second consumer (WO-87):** `app/services/transport/savings.py` (`LEGAL_FRAMING`, `PRICE_BASIS`, `GRAIN_SAME_DAY`, `GRAIN_COUNTRY_MONTH`), `app/schemas/transport_savings.py`, `app/api/routes/transport/savings.py`; **UI consumer (WO-86):** `frontend/src/pages/Overcharges.tsx` renders `price_basis` and `legal_framing` VERBATIM off the response rather than restating them, so the framing cannot be flattened by a re-wording in the SPA; **UI consumer of the SECOND framing (WO-90):** `frontend/src/pages/Savings.tsx`, `frontend/src/lib/transportSavings.ts` (which deliberately holds NO framing string of its own), `frontend/src/lib/nav.ts` (the destination is labelled "Negotiation evidence", borrowing no word from the other family) | `backend/tests/transport/test_wo83_overcharge_artifacts.py::test_wo83_both_artifacts_carry_the_r53_framing_and_the_price_basis`, `backend/tests/transport/test_wo87_r53_framing.py` (the whole file — the four structural absences), `backend/tests/transport/test_wo82_overcharge_routes.py`, `frontend/e2e/recovery.spec.ts::"overcharges: the price basis and legal framing render verbatim"`, `frontend/e2e/savings.spec.ts` (the six `r53:` specs — verbatim framing and basis on all three panels, the source/field-name vocabulary scan, its seeded-violation self-test, the absent route into the contract-breach flow, and the absent mutating control) | `BA_fleet_fuel.md` §2.4 framing table (line 236), R53 (line 1430), §3.G G1 / R49 |
| R50 (+ R49's second half) | **M** | **`net_eur_eff` carries off-invoice rebate layers, and the input source is GUARDED** (WO-84, G4.2 — the third M5 row). `BA_fleet_fuel.md` §4.2's two-tier discount model is the whole rule, verbatim: an **on-invoice** discount (TFC's hub tier, E100's station-colour tiers, MOEVE's PRN off pump PVP, DKV's SEK/L + service fee) is *"already inside `net_eur`"*, while an **off-invoice** rebate *"lands ONLY in `net_eur_eff`"* — the canonical case being a network invoicing at LIST price per country while a rebate partner issues a *"SEPARATE rebate invoice per country"*, which is *"the entire reason the `net_eur_eff` column exists"*. **WHAT WAS ACTUALLY TRUE BEFORE THIS ROW:** `net_eur_eff` had exactly ONE writer (`fuel_ingest.ingest_transaction`'s `q2(net_eur_eff if ... else net_eur)` default) and no production caller ever passed a value — `statement_ingest` omits the argument and all seven G3.2 parsers say so in as many words — so it was an exact COPY of `net_eur` on every stored row, while `contract_audit._breach_for` already read it as though a merge had happened. Its `applied = eur_l_doc − eur_l_eff` term was therefore identically `0.0000`, and every line governed by an `expected_discount_eur_l` term flagged `short discount` for the FULL contracted rebate — a euro `overcharge.open_claim` FREEZES and WO-83's `build_claim_letter` PRINTS in a 30-day payment demand, i.e. this platform demanded money a supplier paying off-invoice had already paid. **THE MERGE:** `app/services/transport/rebate.py` records the rebate DOCUMENT (`vat_off_invoice_rebates`, grain `(org, supplier, country, period, source_ref)`) and `merge_period` allocates it into `net_eur_eff`. The allocation formula is the ONE thing the spec does not supply, so it is a DOCUMENTED INTERPRETATION stated with its reasoning (the `contract_audit._specificity` precedent): **pro-rata by `qty` (litres)** — §5.1's rebate vocabulary is uniformly €/L, it makes `applied` a CONSTANT €/L across a country's lines (the shape a €/L `expected_discount_eur_l` is compared against), and a value-weighted alternative would manufacture short-discount findings on the expensive lines. Rounding is CUMULATIVE-then-quantize (`cum_eur_i = q2(total × cum_qty_i / total_qty)`, `share_i = cum_eur_i − cum_eur_(i−1)`) so `Σ share == rebate_total` EXACTLY — quantizing each share independently would lose or invent a cent onto a price surface. Zero-litre promo lines (§4.2 row 9) receive nothing. **IDEMPOTENT BY CONSTRUCTION:** the new effective net is always derived from the AS-INVOICED `net_eur`, never from the column's current value, so a re-close writes nothing and audits nothing, and a CORRECTED rebate document self-heals instead of compounding a delta. **THE ENGINE OWNS THE WRITE (§3.H):** `record_rebate` is web-reachable and touches no transaction; the merge is the FIRST `close.run_close` stage, so `net_eur_eff` on a validated transaction is only ever moved by the engine, every change audited old→new (§4.16), and a refusal rides `jobs.run_once`'s whole-session rollback. **THE SOURCE GUARD, BOTH HALVES.** §4.2 names the hazard: *"swapping in the raw file **silently loses the rebate layer**, corrupting every price/benchmark/overpay figure"*. (1) *A rebate is never INFERRED* — `merge_period` reads recorded rows and has no code path that derives, estimates or carries one forward; `record_rebate` refuses a blank/whitespace `source_ref` or `source_party` fail-CLOSED before any query (`rebate_source_required`), and the table repeats both as `CHECK`s so a raw path cannot bypass it. (2) *A rebate layer never silently DISAPPEARS* — a question about a period with NO row, which no constraint can answer: `missing_source_warnings` learns the expectation from the registry's own HISTORY exactly as §2.5's "Expected rebate" row prescribes (*"Learns the typical €/L rebate per (supplier, country) from all history; flags a line where it is **missing**… catches rebates that arrive on a separate invoice we often don't see"* — which names this very case), and flags a `(supplier, country)` that carried a recorded document in an earlier period, has litres now and has no document for this month. ADVISORY per §4.19 — it blocks nothing and changes no euro; R50's acceptance is *"fails **or** warns loudly"*, so this row WARNS at the analytics surface (`ContractAuditResult.source_warnings`, additive per §4.20, obtained by CALLING `rebate.missing_source_warnings` — never a forked query) and FAILS wherever a wrong figure would be written (`rebate_has_no_transactions`, `rebate_no_litres_to_allocate`, `rebate_exceeds_net`, each two-phase so a refusal leaves ZERO rows changed). It is silent for a supplier that never had a layer — with no history there is no expectation, and crying wolf would train an operator to ignore the one warning that matters. **§4.15 IS LIVE HERE:** a non-EUR rebate converts through `fx.to_eur` at the DOCUMENT's own date (ECB units per 1 EUR, so converting DIVIDES) and is REFUSED when the cache has no rate (`fx_rate_unavailable`) — `amount_eur` is NOT NULL, so *"never a guessed number"* means *"no row at all"*. There is deliberately NO `stated` branch (unlike DKV's statement lines): a rebate invoice states an AMOUNT, never a rate, and accepting a caller-supplied rate would invent the one number §4.15 exists to forbid. The merge therefore sums `amount_eur` ONLY — §4.14 holds structurally, not by a per-call check. **CONSUMER IMPACT, PROVEN:** the same term over the same line yields a €50.00 `short discount` demand BEFORE the merge and NO finding after it, while a genuinely short rebate still reaches WO-82's own €150.00; WO-83's two artifacts quote the post-merge figure by construction (one line source, `contract_audit.audit()` — not forked), and its existing `overcharge_evidence_drift` 409 is the CORRECT response when a merge lands after a claim froze `detected_eur`. R49's *"both the as-invoiced (`eur_l_doc`) and effective (`eur_l_eff`) prices are exposed so the rebate value is visible"* shipped with WO-82; this row is what makes the effective half differ from the invoiced one. **`contract_audit`'s arithmetic is byte-unchanged** — it reads a column that now carries what its name always claimed. | `app/models/transport/off_invoice_rebate.py` (`VatOffInvoiceRebate`), `app/services/transport/rebate.py` (`record_rebate`/`merge_period`/`missing_source_warnings`), `app/services/transport/close.py` (the merge stage), `app/services/transport/contract_audit.py` (`source_warnings`, additive), `app/api/routes/transport/rebates.py`, `app/schemas/transport_rebate.py`, `alembic/versions/b3d8f1c04e97_off_invoice_rebates.py` (FORCE RLS in the same migration); **UI consumer (WO-86):** `frontend/src/pages/Rebates.tsx` — records the DOCUMENT (no EUR field: the server resolves it), states the §3.H close boundary in words and offers NO preview or merge verb because the backend has none, and renders `fx_rate_unavailable` as "nothing was recorded" | `backend/tests/transport/test_wo84_rebate_merge.py`, `backend/tests/transport/test_wo84_rebate_routes.py`, `backend/tests/test_tenancy_parity.py` (`vat_off_invoice_rebates`, a real HTTP probe), `frontend/e2e/recovery.spec.ts` | `BA_fleet_fuel.md` §4.2 (row 16 + the two-tier discount model + the hazard), R50 (line 1427), R49 (line 1426), §2.5 "Expected rebate", §5.1 (Q8 + rebate partner), §3.G G1, §3.H H1 |
| G4.1 (R51) | **M** | **One canonical query layer — nothing forks the math** (WO-85, G4.1 — the fourth M5 row). `BA_fleet_fuel.md` R51 verbatim: *"One canonical query layer. Every report, export, dashboard and materialized metric derives from it; nothing forks the math"*, verified as *"Rename a canonical function ⇒ every consumer breaks; no duplicate implementation exists"*; `ARCH_plan.md` G4.1 and ADR-0023's projection rule say the same. **WHAT WAS ACTUALLY TRUE BEFORE:** R38's clause had been honoured BY HAND since WO-81, and hand-discipline had already slipped — 18 call sites built a row-selection predicate over the two money-bearing transport tables, including ONE byte-identical fork in the claim-building path (`claim_lines.build_claim_lines`, what a claim's lines are BUILT from, vs `checklist._unresolved_suppliers`, what the checklist REPORTS as blocking that same claim — four identical predicates written twice, with nothing stopping one from acquiring a fifth: the drift files a claim short at the 30-Sep time-bar or chases a supplier never in scope), the unfrozen-claim-line cut re-typed THREE times behind the R3 synthetic-ref gate, the R10 document gate and the G2.5 freeze/preview (`claim_gates`' own docstring named the hazard and then mirrored the shape by hand) plus a FOURTH writer of it — `build_claim_lines`' `DELETE`, the dangerous one — and 14 hand-typed `org_id ==` filters, each an independent chance to omit the one term whose absence returns another tenant's rows with a **200** (§4.1/§4.4). **THE REGISTRY:** six named, PURE, org-scoped `Select` builders (`fuel_transactions`, `claim_scope_transactions`, `fuel_transaction_by_natural_key`, `fuel_transactions_by_normalized_invoice_ref`, `vat_claim_lines`, `resolved_vat_claim_lines`, plus `vat_claim_line_criteria` for the one statement that is a DELETE and the shared `NORMALIZED_INVOICE_REF` expression) under a stated charter — *the registry owns WHICH ROWS (the WHERE clause); the consumer owns HOW THEY ARE READ* (projection via `with_only_columns`, ordering, grouping, pagination stay at the call sites, because ordering is presentation and two consumers reading the same rows in different orders fork nothing). No `AsyncSession`, no `await`, no `Decimal`, no entitlement check — all asserted structurally. The module NAME is the spec's own vocabulary (`queries.q_savings` §2.4, `queries.q_ledger` §N10, neither in scope: G4.7 and the export hub own them); the `q_` prefix is a Fleet Fuel stylistic detail and master-context §6 governs the function names instead — a recorded INTERPRETATION, the WO-82/WO-84 tie-break precedent. **BEHAVIOUR PRESERVED WHERE THE TREE DISAGREED WITH ITSELF, and reported rather than tidied:** three sites read a falsy `supplier`/`country` as "every" (`if supplier:`) and one read it as a literal (`is not None`); the registry filters on `is not None` and the falsy-tolerant sites bridge with `or None`, because unifying them on one truthiness rule would have silently changed one figure. `fuel`'s `country.upper()` stays at its call site for the same reason. **THE DELIVERABLE IS THE GUARANTEE, NOT THE FILE:** the AST scan covers the transport SERVICE and ROUTE packages (a controller typing a `select()` would evade a services-only scan and the layering test says nothing about it), ships a seeded-violation self-test per `WORK_ORDER_TEMPLATE.md` rule 6, and was demonstrated against the real tree — re-introducing the `checklist.py` fork fails the test naming the file and line. R51's acceptance is asserted BOTH ways: *no duplicate implementation* by the scan, *rename ⇒ every consumer breaks* by a no-orphan check over every registry entry (`CANONICAL_MODELS` is the one exemption, for the stated reason that it is the scanner's own target list, so guard and registry cannot name different tables). **PARTIAL HARVEST, DOCUMENTED:** R51's materialised-metric half (*"a drift check that recomputes through the same code path"*, *"an un-materialized period still renders via a live fallback"*) has NO subject — all 17 `app/models/transport/*` tables are sources of record, none a rollup — so a drift check over nothing would be invented functionality (§10); the trigger for that slice is named: the first transport rollup table, whose recompute path must be this registry. Read-only: no table, no migration, no route, no permission, no SPA change, and not one pre-existing test file edited. | `app/services/transport/queries.py`; consumers `claim_lines.py`, `checklist.py`, `claim_gates.py`, `document_gate.py`, `freeze.py`, `claim_pack.py`, `contract_audit.py`, `receipt_control.py`, `rebate.py`, `tie_out.py`, `fuel.py`, `fuel_ingest.py`, `capture_checks.py` | `backend/tests/transport/test_wo85_canonical_queries.py` | `BA_fleet_fuel.md` R51, §2.4, §N10; `ARCH_plan.md` G4.1; ADR-0023 (projection rules) |

| G4.7 (R52 + R53's second framing) | **S** | **The two overpay grains, the internal benchmark, and the rebate the invoice forgot** (WO-87, G4.7 — the fifth M5 row). `BA_fleet_fuel.md` §2.4 records the nuance the rebuild must preserve: *"two different 'overpay' numbers coexist and will NOT reconcile"* — `queries.q_savings` (same-day, same-country, cheapest rival) and `pricing_intelligence.internal_benchmark` (country × month, best-of-your-own-suppliers) — *"Different grain, different denominator. **They are supposed to differ. Label them distinctly.**"* R52 states the same as a requirement. **(1) AVOIDABLE OVERPAY (SAME-DAY)**, §2.5 row 1 verbatim: `litres × (this supplier's eff €/L − the cheapest same-day, same-country rival's eff €/L)`, *"diesel only"*, *"requires ≥2 suppliers that day"*, *"positive deltas only"*, *"attributed to the country of supply and the supplier that charged the premium"*. **"Rival" excludes the supplier itself**, so the cheapest supplier of a day compares against the SECOND cheapest and drops out on the sign test while everyone else compares against the cheapest. An **exact tie is not an overpay** (the comparison is made on the UNROUNDED quotients, so the boundary cannot move on a presentation rounding) and a **day with one supplier yields nothing**, counted in `days_without_a_rival` — never a false positive, and never a silent zero that reads as *"you were competitive"*. **(2) INTERNAL BENCHMARK**, §2.5 row 2 verbatim: `Σ_supplier qty × (supplier_eff − best_eff)` per **country × month** = *"money you could have saved by routing volume to the cheaper supplier you were ALREADY using"*; `best_eff` INCLUDES the supplier being measured (so the best supplier's own gap is exactly zero and it stays on the sheet — it is the supplier the volume would be routed TO). Self-sourced ⇒ *"no antitrust exposure"*, which is why the PEER benchmark (R55's `PEER_MIN_CONTRIBUTORS` gate) is deliberately NOT built here. **R52 is proven on ONE fixture**: two suppliers alternating cheapest across two days give 200.00 on the same-day grain and 0.00 on the month grain, and the test asserts they differ. The two euro fields are NAMED DIFFERENTLY (`avoidable_eur` / `benchmark_gap_eur`) so no consumer can add them by writing the obvious loop, and each result carries its grain string. **(3) EXPECTED REBATE**, §2.5 row 8 verbatim: *"learns the typical €/L rebate per (supplier, country) from all history; flags a line where it is missing (`abs(rebate) < 0.005`)"* — the half WO-84 recorded as a PARTIAL harvest (it took only the per-pair EXISTENCE expectation, `rebate.missing_source_warnings`). `applied = (net_eur − net_eur_eff) / qty` is §4.2's own identity, not an estimate; the typical is the exact-Decimal **MEDIAN** over the pair's rebate-BEARING history (a mean would be set by one freak line, and including the zero lines would drag any average toward zero in exactly the situation the analysis exists to detect). **No minimum-sample constant is invented** — §2.5 states none and WO-84's posture forms an expectation from a single prior period — so `learned_from_lines` rides every expectation and every finding instead. A pair with no rebate history is SILENT. **§4.14/§4.15 are enforced, not assumed.** Every comparison partitions on country and on `product_group == "Diesel"` before any arithmetic, so like is compared with like; and the EUR basis is CHECKED per row, because `contract_audit`'s *"a EUR figure exists for every stored row"* argument holds for `statement_ingest` but not for the row writer (`fuel_ingest.ingest_transaction` takes `fx_source` as an optional argument defaulting to `None` and never checks it against `net_eur`; the column is nullable with `"unknown"` legal). A row with `fx_source == "unknown"`, or a non-EUR currency with no recorded conversion at all, makes the analysis **REFUSE** (`fx_rate_unavailable`) — not exclude, because a comparison set is not a list of independent objects: silently dropping one supplier's line changes the cheapest rival for every OTHER supplier that day and can turn a real overpay into a zero. A EUR line with a NULL provenance passes (EUR is the identity, no rate involved), which is what keeps every `fuel_ingest`-written line in scope. **R53's second framing** is carried structurally — see the R53 row above. **DOCUMENTED INTERPRETATIONS** (stated, the `contract_audit`/`rebate` precedent): both overpay grains are restricted to Diesel (a blended €/L across Diesel + AdBlue + Toll measures product MIX, not price); a supplier's price in a grain cell is VOLUME-WEIGHTED `Σ net_eur_eff / Σ qty` (§2.5's `litres ×` needs one €/L per cell, and §2.5 itself rejects a simple mean elsewhere because it *"let a 50 L fill move the pack as much as a 40,000 L fill"*); and `expected_rebate_eur = typical €/L × litres` is an advisory magnitude, never a claim. **PARTIAL HARVEST, DOCUMENTED:** §2.5 lists nine analyses; this row implements three COMPLETELY. The peer benchmark (R55) needs a cross-entity cohort policy decision, the margin report needs §3.H H5's `my_prices`/`wholesale_prices` tables, supplier reliability needs an append-only `advertised_prices` table, anomalies (R54) are six rules and an order of their own, and the FX markup trend needs a market-wide markup series — each recorded as a follow-up slice WITH its blocker rather than half-built (§10). Read-only: no table, no migration, no permission member, no SPA change, and not one pre-existing test file edited. **UI CONSUMER — WO-90 (`/savings`, the negotiation-evidence workspace).** The three analyses were `curl`-only until WO-90 gave them one screen with three panels. R52 is rendered rather than only documented: each overpay panel prints the response's OWN `grain` string beside a sentence stating that the two totals measure different things over the same rows, with the reason (different comparison, different denominator) and the instruction that neither is to be added to the other — so the first operator to open both is TOLD why they differ instead of raising a defect. The two euro totals are asserted never to appear together on one screen. §2.5's *"requires ≥2 suppliers that day"* becomes a presentation rule: `days_without_a_rival` is a NAMED COUNT with its explanation (*"there was no rival price to compare against and no finding was produced… that is not the same as a zero"*), never a €0.00 line, because "the comparison could not be made" and "you were competitive" are different facts. The learned rebate is presented as an advisory MAGNITUDE with `learned_from_lines` beside every expectation, and a pair with no history stays silent. No arithmetic reaches a figure: every euro, €/L and litre value is the server's own decimal string (grep-proven, `parseFloat`/`Number(`/`toFixed`/`Math.` all absent), asserted on a `99999999999999.99` fixture an IEEE-754 round-trip would destroy. ZERO backend change — no route, schema field, permission member or constant. | `app/services/transport/savings.py`, `app/services/transport/queries.py` (`DIESEL`, `price_comparison_transactions` — the spec's `q_savings`, named per this codebase's convention and deliberately taking NO `supplier` argument), `app/schemas/transport_savings.py`, `app/api/routes/transport/savings.py`; **UI consumer (WO-90):** `frontend/src/pages/Savings.tsx`, `frontend/src/lib/transportSavings.ts`, `frontend/src/lib/types.ts` (eight additive wire interfaces), `frontend/src/lib/nav.ts`, `frontend/src/App.tsx` | `backend/tests/transport/test_wo87_savings.py`, `backend/tests/transport/test_wo87_savings_routes.py`, `backend/tests/transport/test_wo87_r53_framing.py`, `frontend/e2e/savings.spec.ts` (36 specs — three `r52:` among them) | `BA_fleet_fuel.md` §2.4 (the two-grains nuance + the framing table), §2.5 rows 1/2/8, R52, R53, R49/§3.G G1, R51; `ARCH_plan.md` G4.7 |

| R56 | **S** | **FX provenance is CONSISTENT with the euro it labels — enforced at the WRITER and in STORAGE, not only at a reader** (WO-88 + WO-89, the G4.7 follow-ups; R56's first ledger row and its second enforcement consumer). Three combinations are refused, not two: a euro that denies a rate was USED (`unknown` beside a EUR figure; a non-EUR currency with no provenance at all — WO-88) and a euro that denies a rate was NEEDED (a non-EUR currency claiming the EUR identity — WO-89). `BA_fleet_fuel.md` R56 verbatim: *"FX: one convention (foreign units per 1 EUR); the rate used is the most recent on or before the transaction date; both the APPLIED and the OFFICIAL reference rate are frozen per line with a provenance flag. No coverage ⇒ **NULL, never a fabricated pass**"*. The convention half was already true (`fx.resolve_rate` takes the most recent rate on or before the date; `statement_ingest._resolve_line` freezes the applied AND the official ECB reference per line and refuses `fx_rate_unavailable` when the cache has none; `rebate._resolve_eur` does the same for a rebate document). The **"never a fabricated pass"** half was NOT: `fuel_ingest.ingest_transaction` took `fx_source` as an optional argument and never compared it with `net_eur`, and `ck_fuel_transactions_fx_source` constrained only the VALUE DOMAIN — so a row could assert both *"no rate was available"* (`fx_source='unknown'`, which `app/models/fx.py` defines as *"EUR figure is NULL, never a guessed number"*) and a non-NULL `net_eur`, and every consumer downstream (claim lines, `contract_audit`'s demand-letter euro, the tie-out, the close, the recovery dashboard) summed it as a real conversion. WO-87 refused it at ONE read boundary (`savings._require_eur_basis`); WO-88 makes the row **unrepresentable**: `_require_fx_provenance` refuses it at the one writer, fail CLOSED, before any DB read (so a refused call writes no row and no audit event), and `ck_fuel_transactions_fx_provenance` refuses it underneath for every path that never comes through the service. The EUR/NULL identity carve-out is deliberate and shared with `savings` (EUR involves no rate). `vat_off_invoice_rebates` — WO-84's other money-bearing table, `amount_eur` NOT NULL — shipped with the FX quadruple and **no `fx_source` constraint of any kind**, so it gains BOTH the value-domain CHECK every other FX-bearing table has carried since WO-8 and the same consistency CHECK; its writer was already correct. The migration inspects before it constrains (the WO-8 precedent) and, since a violating row cannot be corrected without inventing a rate, prints it and **REFUSES to migrate** rather than guessing or deleting history. Verified on BOTH dialects (SQLite batch rebuild preserves the natural key, the pre-existing CHECKs and all three indexes; PostgreSQL 16 rejects the same two inserts). **WO-89 — R56's SECOND enforcement consumer, the WRONG-provenance half.** WO-88 closed the euro that denies a rate was USED; it left open the euro that denies a rate was NEEDED — a non-EUR document currency carrying `fx_source='eur'`, the IDENTITY provenance (`app/models/fx.py`: *"the amount was already EUR (identity, rate 1)"*). Recon proved it reachable through the PRODUCTION writer, not merely representable: driving `fuel_ingest.ingest_transaction` with `currency='PLN', fx_source='eur', net_eur=1400.00` STORED the row. WO-88's gate passed it (its two clauses test `unknown` and a NULL provenance), its CHECK passed it (`fx_source IS NOT NULL` satisfies the second conjunct), and `savings._require_eur_basis` passes it too — so a fabricated conversion reached the claim lines, the WO-83 demand letter, the tie-out, the close and the recovery dashboard unopposed. WO-89 adds a THIRD clause to the SAME predicate (never a rival one) and a THIRD conjunct (`upper(currency) = 'EUR' OR fx_source <> 'eur'`) to BOTH constraints under their EXISTING names — the constraint always meant *"this row's euro does not contradict its own provenance"*, and a second same-shaped constraint beside it would fragment one invariant across two objects. Its code is DISTINCT — `fx_provenance_inconsistent`, after the tree's own `fx_stated_inconsistent` — because every `fx_rate_unavailable` message ends in *"go and obtain the rate"*, which would send an operator to the wrong fix when the rate is cached and the claim that none was needed is what is broken; WO-88's two refusals keep their own code. The migration DROPS before it creates, so its pre-flight scans all THREE combinations, not only the new one, and a failed run is asserted to leave the OLD constraint in place. **The AP/AR core (`invoices`/`expense_items`) is closed by ANALYSIS, not by a constraint:** `fx.eur_total` returns `'eur'` on exactly one branch (`if currency == "EUR"`, after `.upper()`) and is the only writer of `invoices.fx_source`; `expenses.apply_item_fx` stamps `eur` only when `ccy == report_ccy == "EUR"` and overwrites any client-supplied value on every one of its branches. `expense_items` additionally has NO EUR column at all — its converted `amount` is in the REPORT currency — so the rule would there be a different invariant. Both remain representable at the storage layer: a platform-level gap, recorded, not smuggled into a transport order. | `app/services/transport/fuel_ingest.py` (`_require_fx_provenance`, `FX_SOURCE_EUR`), `app/models/transport/fuel_transaction.py` (`FX_PROVENANCE_CHECK`, `ck_fuel_transactions_fx_provenance`), `app/models/transport/off_invoice_rebate.py` (`ck_vat_off_invoice_rebates_fx_source`, `..._fx_provenance`), `alembic/versions/e4a7c1d92f08_wo88_fx_provenance_consistency.py`, `alembic/versions/a7f2e9c41b83_wo89_fx_wrong_provenance.py`; the third layer, unchanged by BOTH orders: `app/services/transport/savings.py` (`_require_eur_basis`) | `backend/tests/transport/test_wo88_fx_provenance.py`, `backend/tests/transport/test_wo89_fx_wrong_provenance.py`, `backend/tests/test_migrations.py::test_wo88_fx_provenance_migration_refuses_to_run_over_a_violating_row`, `backend/tests/test_migrations.py::test_wo89_wrong_provenance_migration_refuses_to_run_over_a_violating_row`, `backend/tests/transport/test_wo87_savings.py` (the two §4.15 tests, all three layers now) | `BA_fleet_fuel.md` R56 (§7.5); ADR-0010 (FX provenance); master-context §4.15 / §4.14 |

| R42 (+ R53's THIRD framing) | **M** | **Diesel excise-duty refund — the second recoverable-cash stream, and the figure that asserts nothing** (WO-91, G4.6 — the sixth M5 row). `BA_fleet_fuel.md` §2.4's `/excise` row is the whole surface, verbatim: *"7 countries (BE·FR·IT·SI·HU·ES·HR); `litres × rate/1,000L`; rate default **EUR 30/1,000 L is an explicit PLACEHOLDER** in the reported EUR 25-33 band, admin-overridable. **Asserts NO eligibility.** Separate regime, filed with **CUSTOMS**."* R42 adds the grain and the artifact (*"over the same validated diesel lines, per (entity × country) … with an Excel packet for customs"*), §1.3 gives it its own money-flow branch, and §6.1 places it in law (*"a **different regime**, claimed from **CUSTOMS**, trucks **>= 7.5 t**, not harmonised, rates change quarterly"*, ETD 2003/96 + national law). **THE ANALYSIS** (`excise.excise_report`, READ-ONLY over `fuel_transactions`): the period's validated DIESEL litres summed EXACTLY per (entity × country) — the predicate is the canonical-registry cut `queries.excise_transactions` (diesel only, one accounting month, optional entity/country scope), deliberately NOT `price_comparison_transactions` with an extra argument, because that function's docstring forbids a scope parameter (an overpay comparison is RELATIVE, so scoping changes the answer; excise is ABSOLUTE, so it cannot). The euro is ONE `money.q2` from the exact litre total and the exact rate — `q2(litres / 1000 × rate)`, so 1,234.567 L at EUR 30.0000 is exactly EUR 37.04 — never from a rounded intermediate (§4.9; litres are never money-quantized, §4.2 row 9). **THE RATE** is an operator override over a code constant (new tenant table `vat_excise_rates`, natural key `(org, country)`, `Numeric(12,4)`, FORCE RLS in its creating migration, audited old→new on every change): `set_rate` accepts ONLY the harvested seven states (422 `excise_country_not_supported` — a DOCUMENTED INTERPRETATION, fail-CLOSED: storing a rate for an eighth would assert a refund regime exists where the spec records none) and refuses a non-positive rate (422 `invalid_excise_rate` + a DB CHECK — the absence of the regime is expressed by holding no rate, never by a zero). `rate_for` therefore returns **`None`, never `0`**, outside the seven, and a state with no rate produces **NO ROW** — it is reported in `skipped_countries` with its litres and deliberately WITHOUT a euro field, because *"we hold no rate for this state"* and *"this state refunds you nothing"* are different facts. **THE ELIGIBILITY LIMITATION IS STRUCTURAL, NOT EDITORIAL** (§3.L *"Asserts NO eligibility (vehicle >=7.5t / carrier registration not modelled)"*; §9.2 item 14 files who confirms it as an OPEN owner question): ONE server-side `ELIGIBILITY_STATEMENT` naming both conditions and denying the entitlement reading in words, rendered by every result shape, every response schema and BOTH sheets of the customs workbook; a REQUIRED literal `eligibility_asserted: false` so the denial survives a surface that truncates prose; no field name anywhere in the service or the schemas containing a claim word (the euro is `indicative_excise_eur` — the qualification is inside the identifier), scanned against WO-87's `CLAIM_WORDS` **imported** rather than re-typed; and no import path in EITHER direction between this family and the claim-back family, so no excise euro can reach `overcharge.detected_eur`. Each scanner ships a seeded-violation self-test. **§4.14/§4.15 land differently here and that is stated, not inherited:** this module reads `qty` and NO currency amount at all (asserted structurally, by column name), so nothing is converted and nothing is summed across currencies — and unlike `savings.py` an `fx_source='unknown'` row does NOT refuse, because a litre count is not arithmetic on euros and refusing would drop real litres from a customs packet. **THE PACKET** (`evidence_workbook`) is R42's Excel filing for CUSTOMS, rendered from the SAME `excise_report` through ONE `_COLUMNS` spec by a SYNC function holding no session (the WO-74/WO-83 one-source-two-renderers rule, structural); it REFUSES rather than emitting an empty document (422 `no_excise_findings`), prints the caveats on both sheets and the un-rated litres on the cover, and is formula-injection-safe (CWE-1236) on free text while keeping numbers as numbers. **Routes** (`api/routes/transport/excise.py`, registered in the package aggregator the module's own docstring had named it in): `GET /transport/excise`, `GET /transport/excise/packet`, `GET /transport/excise/rates` on the RESERVED `TRANSPORT_READ` (whose definition has read `# fuel/toll analytics, excise (advisory)` since it was introduced), and `PUT`/`DELETE /transport/excise/rates` on the EXISTING `VAT_WRITE` that `overcharges.py` already gates contract terms with — **no permission member added** (§10). Decimal-as-string on the wire, litres included. **WO-91 UI consumer (partial):** no `/excise` page ships in this order (a named follow-up UI slice); what DID land on the SPA is the `invalid_period` SPLIT this surface would otherwise have inherited — one wire code carrying three different server sentences was mapped to the CLAIM instruction only, so four month-shaped pages told an operator to type `2026-Q2` into a `YYYY-MM` field (`frontend/src/lib/transportClaims.ts` `PERIOD_SHAPE_REFUSALS`, `RefusalNotice`'s `periodShape` prop; both sentences asserted in `frontend/e2e/savings.spec.ts` and `frontend/e2e/vat-claims.spec.ts`). No wire slug changed. **WO-92 COMPLETES THE UI HALF** (`frontend/src/pages/Excise.tsx` at `/excise`, nav-gated `transport` + `transport.read`): two panels — the month's per-(entity × country) figures with the customs-packet download, and the rate registry with the two `vat.write` controls — and R42's acceptance line (*"the UI shows the indicative-rate and eligibility caveats on every surface that shows the number"*) is finally testable against a UI. **THE NON-ASSERTION IS PRESERVED THE WAY THE BACKEND BUILT IT, NOT RESTATED:** `eligibility`, `rate_caveat`, `legal_framing`, `filed_with` and `litre_basis` are rendered VERBATIM off the wire, on the page header AND again inside each panel (a panel scrolled away from the header is its own surface); the SPA holds NONE of those five strings — a Playwright test walks every file under `frontend/src/` proving it, so a re-wording in the UI is impossible rather than merely discouraged; `eligibility_asserted: false` is STATED ("Eligibility asserted by this figure: none") rather than dropped, which is the whole reason the boolean rides beside the prose; and the source scan over `pages/Excise.tsx` + `lib/transportExcise.ts` + the five new `lib/types.ts` interfaces' FIELD NAMES uses WO-87's `CLAIM_WORDS` **plus `entitle`**, with a seeded-violation self-test. **`skipped_countries` is rendered as its own table with litres and a line count and NO euro column** — the wire shape carries no euro field, so the €0.00 that would say *"this state refunds you nothing"* cannot be rendered even by accident, and the copy says in words that no rate held is not a figure of zero. Every rate on both panels shows its SOURCE in the customs workbook's own two words (*verified override* / *indicative default*), so the shipped placeholder is never presented as a statutory figure. No arithmetic anywhere: euros through `decimalMoney` (string surgery), rates and litres as received, grep-proven. The three codes no page had yet rendered (`excise_country_not_supported`, `invalid_excise_rate`, `no_excise_findings`) gained ADDITIVE refusal sentences; the other three were already mapped. 39 Playwright specs (`frontend/e2e/excise.spec.ts`); zero backend files touched. | `app/models/transport/excise_rate.py` (`VatExciseRate`), `app/services/transport/excise.py` (`excise_report`, `rate_for`, `set_rate`/`remove_rate`, `build_evidence_workbook`/`evidence_workbook`), `app/services/transport/queries.py` (`excise_transactions`, `excise_rates`), `app/api/routes/transport/excise.py` | `backend/tests/transport/test_wo91_excise.py`, `backend/tests/transport/test_wo91_excise_eligibility.py`, `backend/tests/transport/test_wo91_excise_packet.py`, `backend/tests/transport/test_wo91_excise_rates.py`, `backend/tests/transport/test_wo91_excise_routes.py`, `backend/tests/test_tenancy_parity.py` (`vat_excise_rates` probe) | `BA_fleet_fuel.md` §1.2, §1.3, §2.4, §3.L, §6.1, §9.2 items 13-14, R42, R53; ETD 2003/96 + national law (the regime is NOT harmonised; eligibility is NOT modelled) |

| R13 | **M** | **The contingency fee FREEZES with the VAT base — and the engine REFUSES rather than invent a rate** (WO-95, G2.9 — the last M3 *service* row, unblocked by the owner decision of 2026-08-08 recorded at the top of `docs/DECISIONS-NEEDED.md`: *"contingency fee on recovered VAT (no-win-no-fee)"*, with **the percentage and any minimum still open**). R13 verbatim: *"**Fee rate FROZEN at first submission; fee CHARGED on the PAID amount.** The frozen VAT base is computed over **exactly the locked claim set**, not a period SUM."* Its acceptance line: *"Change the customer fee % after submission ⇒ the claim's fee is unchanged."* **THE FORMULA** is C11 verbatim — *"`% fee` takes priority; if it falls below the per-declaration minimum, the **minimum** is charged. Returns `(fee, basis)` where basis ∈ {`percent`, `minimum`}"* — implemented as a PURE function so R40's *"reusing the same fee function the real claim uses"* costs no second implementation; the EXACT TIE belongs to `percent` (a percentage equal to the minimum has not *"fallen below"* it) and both sides of that boundary are asserted one cent apart. **THE RATE** is C11's chain — *"per-(customer, country) override → customer default"* — plus the org-level STANDARD rung R40 names (*"Standard fee is admin-editable; a per-client fee overrides it"*) and the owner decision names again (*"the rate as an org-level setting"*), stored in the new tenant table `vat_fee_rates` (three rungs, one table, uniqueness by a plain constraint for the two customer rungs PLUS a partial unique index `WHERE entity_id IS NULL` because SQL treats NULLs as distinct and two org standards would otherwise both store). **The CUSTOMER is the claimant entity** — not invented here: WO-73 shipped `VatCustomerLifecycle`, keyed `(org, entity_id)` and named for the customer, and `submit_claim` already gates every filing on it, which retires the *"no mapping from a claim to a billable customer"* blocker `DECISIONS-NEEDED.md` §10 recorded. **TWO DELIBERATE DEVIATIONS FROM THE HARVEST, both argued in `fee.py`'s docstring rather than slipped in:** Appendix B's `pricing_fee_pct 15%` is NOT used, and C11's terminal `(0, 0)` rung is replaced by a REFUSAL (`fee_rate_not_configured`) — a frozen `fee_eur = 0.00` is a positive assertion that a filing earns nothing, which nobody made, and under no-win-no-fee a silent zero either forfeits the vertical's entire revenue or is later "corrected" by rewriting a frozen figure, which is what R13 exists to prevent. The excise placeholder precedent does not transfer: that figure is ADVISORY, LABELLED, and the STATE'S, whereas this one is ours and binding. An explicitly typed `(0, 0)` IS accepted — the refusal is about ABSENCE, and a typed zero is an audited human decision. **CONSEQUENCE, stated loudly:** an org with the module enabled and no configured rate can no longer submit a claim (409 `fee_rate_not_configured`), and the rate resolution runs as the LAST gate — after every statutory gate, before the freeze — so a missing COMMERCIAL setting is reported after every LEGAL problem and while the session is still unmutated (asserted, not assumed: a claim failing R7 or R10 reports the legal code). **WITHDRAWAL DOES NOT CLEAR A FROZEN FEE** — §3.D **D7** enumerates exactly what a withdrawal changes (the locks and `status_code`) and nothing in the harvest clears a frozen figure; `withdraw_claim` leaves `vat_eur` standing too, and a withdrawn claim can never return to `draft` (WO-94) so the fee can never be re-frozen or contradicted. **PARTIAL REJECTION IS NOT BUILT** — a documented seam only (`fee.py` records that C10's second bullet recomputes over a REDUCED base at the FROZEN rate, which is why `compute_fee` takes the rate as arguments and why `fee_pct`/`fee_min` live on the claim), per `DECISIONS-NEEDED.md`'s 2026-08-08 §13 note. Audit: the freeze rides the existing `transport.claim_submit` event with old→new for all three columns plus the BASIS and the RUNG (WO-94's one-event-per-lifecycle-moment precedent), and the rate CRUD carries its own audited old→new. **R39 (WO-93) re-proven, not weakened:** all three WO-93 files pass byte-for-byte unmodified, and the shared `enable_transport` fixture now seeds the standard rate so their previously-vacuous wire scan runs over a claim carrying a REAL frozen fee. | `app/models/transport/fee_rate.py` (`VatFeeRate`, `ANY_COUNTRY`), `app/services/transport/fee.py` (`compute_fee`, `resolve_fee_rate`, `freeze_fee`, `set_rate`/`remove_rate`/`list_rates`), `app/services/transport/lock.py` (`submit_claim` — the gate + the stamp), `backend/alembic/versions/d4c7b1e93f27_fee_rates.py` | `backend/tests/transport/test_wo95_fee_rates.py`, `backend/tests/transport/test_wo95_fee_freeze.py`, `backend/tests/transport/test_wo95_client_surface_with_fees.py` | `BA_fleet_fuel.md` §2.2, C10, C11, R13, R40; §3.D **D7** (withdrawal); the owner decision of 2026-08-08 (`docs/DECISIONS-NEEDED.md` §10) |

<!-- Row template:
| R9 | 30-Sep filing deadline is a fatal time-bar | app/services/transport/claim_gates.py | tests/transport/test_r9_deadline_time_bar.py | Art. 15 Dir. 2008/9/EC; CJEU C-294/11 Elsacom |
| R39 | **The client claim-status portal** — plain-language stages ONLY. `BA_fleet_fuel.md` §3.D's *"client-facing translation (`CLIENT_STAGES`, `/claim-status`): internal 1A..5 codes map to plain-language stages **prep -> ready -> filed -> awaiting -> refunded** (plus \"needs attention\")"* (restated at §4.5 and in R39), harvested VERBATIM as `client_status.CLIENT_STAGES` — six slugs in the spec's own order, no seventh, no synonym. The per-code assignment is an explicit INTERPRETATION (the spec names the stages and the codes but never pairs them) recorded in the module docstring and asserted as a literal map: 1A -> needs_attention, 1B/1C -> prep, 1E -> ready, 2/2A -> filed, 2B/3B/3C/3D -> needs_attention, 3 -> awaiting, 3A/4/4A/5 -> refunded. **R39's acceptance line is entirely an ABSENCE** (*"a client-role session cannot see a status code or a fee anywhere"*) and is enforced STRUCTURALLY, each scan with a seeded-violation self-test: no leaf string of a real response EQUALS any internal code (equality, not containment — `"2"` is a code and `"2026-Q2"` is a period the client needs; the vocabulary is IMPORTED from `status.py` so a code added there is covered), no field name in the service dataclasses or the wire schemas carries code/fee/action vocabulary, the service is AST-asserted never to read `fee_pct`/`fee_min`/`fee_eur`, no server-owned string reads as an instruction, and the SPA renders no control at all. The plain-language labels are SERVER-owned and cross the wire (a spec walks every file under `frontend/src/` proving the six sentences live nowhere in it), so the SPA cannot re-word the vocabulary. Dispatch is on the ENGINE STATUS FIRST, then the code — which makes the surface immune to `lock.withdraw_claim` leaving a stale `status_code` against §3.D D7 (a G2.7 gap recorded, not fixed here). Read-only, org-scoped, module-gated fail-CLOSED, forks no query (`claim.list_claims` + `status.derive_stage` + `freeze.preview_vat_base`, proven by an AST scan for any rival `select()`); `vat_eur` is `Decimal | None` so a cross-currency draft (§4.14) states no figure rather than a false `0.00`. **Route consumer:** `GET /api/v1/transport/claim-status?year=` on the EXISTING structural `VAT_READ` — the permission `Role.READ_ONLY` (§1.2's *"the client, read-only base"*) already holds, chosen over `TRANSPORT_READ` on WO-79's own reservation wording because this returns claim ROWS, not portfolio aggregates; no permission member invented. **UI consumer:** `/claim-status` (`frontend/src/pages/ClaimStatus.tsx`, `frontend/e2e/claim-status.spec.ts`) — the six stages with the server's own label and sentence, the claims table, and a `null` amount rendered as a dash rather than €0.00. NET EUR. | `app/services/transport/client_status.py` (`CLIENT_STAGES`, `STAGE_BY_CODE`, `STAGE_LABELS`, `client_claim_status`), `app/api/routes/transport/claim_status.py`, `app/schemas/transport_client_status.py` | `backend/tests/transport/test_wo93_client_status.py`, `backend/tests/transport/test_wo93_client_surface.py`, `backend/tests/transport/test_wo93_claim_status_routes.py`, `frontend/e2e/claim-status.spec.ts` | `BA_fleet_fuel.md` §1.2, §2.4, §3.D, §4.5, R39; Dir. 2008/9/EC (the claim lifecycle being translated) |
-->
