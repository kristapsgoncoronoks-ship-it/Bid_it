# Reference-integration plan — the eight-repository knowledge archive → InvoiceIQ

Received 2026-09-07: `InvoiceIQ_All_Reference_Investigations_20260907.zip` (archived
deduplicated at `docs/reference-investigations/`, index in `DUPLICATES.md`). Owner's order:
*"You must take ideas from all of these. Our system must include all of them in one
sophisticated service for clients … Involve all agents what are mentioned in files."*

This document is the single register for that order. It follows the archive's own rules:

- **Native implementation, not copying.** UNDERSTAND → EXTRACT PRINCIPLE → DESIGN → IMPLEMENT
  NATIVELY (`AI-ENGINEERING-POLICY.md`). Every archive patch was written against an older
  base (`d30f90b` / `584fb21` / `525470a`) and only one (`flask-route-topology-guard.patch`)
  still applies with `git apply --check`; the rest are re-implemented from their intent.
- **Status vocabulary** (`00-MASTER/README-FIRST.md`): PATCH READY → applied locally →
  targeted suites green → seeded violations red → full regressions green → CI green on the
  pushed head → **DONE** only then. Nothing in the archive was DONE on arrival (every cycle
  recorded `403 Resource not accessible by integration` on push).
- **Provenance fields** on every row: pattern ID · source repository · reference evidence ·
  InvoiceIQ evidence · Lead decision · validation status. Append only; PAT/NO IDs are kept.
- **Anti-cargo-cult**: a reference mechanism is evidence to learn from, not a default to copy;
  measurement before infrastructure; keep the repository's existing severity, never inflate.
- **Owner-only decisions** are recorded in `docs/DECISIONS-NEEDED.md`, never decided in code.

## 1. Roles the archive names — and how they are staffed here

| Role in the archive | Responsibility (from the files) | Staffed as |
|---|---|---|
| Lead Developer | final decision on every debate; implementation orders (objective / lesson / current / target / files / do-not-change / tests / acceptance / rollback); "APPROVE FOR BRANCH; NOT DONE" until CI | the implementing session (this plan's author); writes the verdict line per batch |
| Reference Repository Engineer (lens) | "principle transferred, not the reference's implementation" | post-implementation review agent per batch |
| InvoiceIQ System Architect | "same service/API/DB; no new subsystem" | post-implementation review agent per batch |
| Security Engineer | tenant/RLS/permission boundaries intact; "PASS WITH REQUIRED REGRESSION" when RLS tests must run | post-implementation review agent per batch; RLS suite on real Postgres |
| QA / Test Engineer | full repository tests, seeded-violation proofs, Postgres/RLS job tests | the pipeline discipline (targeted → seeded → full regression in a worktree → CI) |
| Adversarial Engineer | added complexity, hidden coupling, "never infer quality from the reference's popularity" | post-implementation review agent per batch |
| Product / UX | palette, jobs-operations screen, saved views: measured need first | review of R4 |
| DevOps / SRE | supply chain, workflow permissions, branch protection, deploy | R3 |
| Performance | PERF-DUCK-001 pushdown + re-measurement against datapoints 5–6 | R4 |
| Operators | scale lanes, override the OCR budget by environment, decide per-kind metrics | `OCR_PROCESS_TIMEOUT_SECONDS`, `--kinds` lanes (existing) |
| Owner / product owner | branch protection; the private disclosure channel; `STRIPE_METER_UPLOAD` go-live; payment/tax human approval policy | `DECISIONS-NEEDED.md` §20, §22, §23 |
| Human reviewer of AI-assisted change | can explain the change; no checkbox implies a test that was not run | PR template (R3), `AI-ENGINEERING-POLICY.md` |
| Future agents | append to the pattern library, never rediscover rejected patterns | `docs/reference-investigations/00-MASTER/` is the library; this register is the ledger |

The archive names no "SRE", "DevSecOps" or "Product Owner" verbatim (INVENTORY-learnings §Agents);
the table above maps its review lenses and category tags to the panel that reviews each batch.

## 2. Batches

Order follows the archive's dependency sequence (Stirling: "land heartbeat first; OCR timeout
second; full backend; Postgres/RLS job tests; full CI") and the patches inventory's
recommended application order.

| Batch | Scope | Status |
|---|---|---|
| **R1 — queue / runtime hardening** | STIR-P1-01 lease heartbeat · STIR-P2-01 OCR runtime budget + `processing_timeout` · PAT-004 Retry-After floor (capped) · PAT-028 connect-time DNS pin (CGNAT/NAT64, SNI, no redirects) · PAT-030 job log context · FLASK-P2-02 route-topology guard | **DONE — 2c631d5, CI #564 green, main fast-forwarded (deploy = CI #565)** |
| **R2 — billing durability** | BILL-REL-001 durable Stripe webhook (Job row, no new table; customer-mismatch terminal via `jobs.PermanentJobError`) · BILL-METER-001 frozen usage segment (`reporting_target`, migration `e7f9a1c3d5b8`) · BILL-QA-001 lost-response scenarios | **DONE — a789e98, CI #566 green, CI #567 deployed 2026-09-08 04:37 UTC (migration applied)** |
| **R3 — security governance / supply chain** | SEC-SC-001 action SHA pins (+ `check_github_action_pins.py`, SHAs verified upstream) · SEC-SC-002/003 `security-supply-chain.yml` (actionlint, zizmor, TruffleHog, dependency-review; read-only token; `persist-credentials: false`) · explicit `permissions:` on every workflow · SEC-CTRL-001 control register + gate + generated view · ENG-GOV-001 PR template · FLASK-P2-01 JWT signing-key separation + rotation · SEC-GOV-001 disclosure decision doc (SECURITY.md stays BLOCKED on the owner) · `AI-ENGINEERING-POLICY.md` · TW-P2-02 AI-agent action safety as ADR-0030 · TW-P2-03 migration law merged into `engineering-rules.md` §9 | **implemented; certification in §5c** |
| **R4 — performance / product** | PERF-DUCK-001 AP-inbox pushdown (one projected SELECT; no cache, no index) + PERF-DUCK-002 re-measure — **no change on the dashboard; PERF-018 stays open, its cause found as PERF-019 (the tenant guard's per-statement options)** · TW-P2-01 "Go to page" palette (filtered `navGroups` only; the live shell is outside the VR suite, `/design` untouched) · FLASK-P3-01 inventory → `error` · Billing page "activating…" state after Checkout (R2 review P2) · review fixes: FE-022 focus trap, Portal, Billing settle-on-bought-plan + truthful timeout | **DONE — 078d775, CI #574 green, CI #575 deployed 2026-09-08 12:34 UTC** |
| **R5 — tenant-guard mechanism (PERF-019)** | one `with_loader_criteria` on a shared tenant base (+ the `User` membership predicate) or reachable-entity criteria, whichever the Security lens's constraints (§6 R4) allow; acceptance = absolute serial p95 / req/s at N=8, never the ratio; `test_dashboard_statement_shape_is_bounded` tightens from 110 loader options per SELECT (108 today) to a handful; harness seeds approval steps so PERF-DUCK-001's own effect gets its `--shape` datapoint (P-4) | **implemented (one option per registry, by attribute through the mapper, typed bind parameter; ratchet ≤ 2; throughput ×1.6–2.1; parity 98/98, RLS 5/5 + Postgres mechanism test, seven seeds red); certification in §5e — DONE: CI #577 green on cb8763d, main fast-forwarded (deploy = CI #578); the harness seeding (P-4) landed in R6** |
| **R6 — ops deferrals + billing guard + the harness's own datapoint** | the R2 review's deferred `kind` label on the dead-letter gauge (+ `dead_by_kind` on `/health/queue`) · worker LIVENESS probe (`app.worker_probe` on a heartbeat file the loop touches; compose `healthcheck` + k8s `livenessProbe`) — **deliberately not `/health/queue`** as the R2 deferral had worded it: a backlog is a fleet condition to page on, and restarting every replica on it loses the jobs in flight · BE-022 no second Checkout for a subscriber (409 + the Portal button) · R5 P-4: the harness seeds one pending approval step per submitted invoice — which found **PERF-020** (the gate measured an unanalysed-table plan at 8.08×; the harness now analyses its seed; 1.94× with the inbox's rows) · found: BE-023 (owner, DECISIONS §25) | **DONE — 8e600b0, CI #581 green, CI #582 deployed 2026-09-08 18:45 UTC (carrying R5 as well)** |
| **Deferred / rule-only / rejected** | recorded in §4 with the archive's own decision; nothing silently dropped | — |

Per batch: targeted suites → seeded-violation proof for every control → full backend regression
in a git worktree → Postgres/RLS suite where jobs or tenant tables change → CI on the pushed head
→ review panel (§1) with a Lead verdict → `main` fast-forward (auto-deploys) → runbook rows.

## 3. Register — every order and code-bearing pattern

Legend: **Status** = DONE (CI green on main) · CERTIFYING (implemented, regressions running) ·
QUEUED (batch named) · OWNER (owner decision) · RULE (adopted as a rule, no code) ·
DEFERRED / REJECTED (archive decision kept).

### 3.1 Code and CI orders

| ID (archive) | Source repo · reference evidence | InvoiceIQ evidence (before) | Lead decision | Implementation (native) | Validation | Status |
|---|---|---|---|---|---|---|
| STIR-P1-01 (STIR-G1, STIR-D1, CMP-STIR-002) | Stirling-PDF · `ProcessExecutor` owns the resource for the operation's lifetime | `jobs.py`: `claim` stamps `locked_at`; nothing renewed it; `reclaim_stale` at 300 s handed a live 40-page OCR to a second worker | ADOPT C — P1, land first | `LEASE_HEARTBEAT_SECONDS = 60`; `_renew_lease` (own `SessionLocal`, predicate id + RUNNING + `locked_by == worker`); `_heartbeat_loop` on an `asyncio.Event`; task created in `run_once` after the tenant/job context is set, stopped and awaited in `finally`. The patches inventory's claim that "the heartbeat cannot fire during OCR because nothing offloads to a thread" was checked and is **refuted**: `extraction.py:435` and `email_intake.py:177` run the parse in `run_in_threadpool`, so the loop is free during OCR | 9 SQLite tests (`test_jobs.py`: renewal keeps a job live and out of `reclaim_stale`; a re-taken lease is not re-taken; a terminal job cannot be renewed; heartbeats while the handler is alive; a failing renewal does not fail the job; a HUNG renewal is cancelled after `HEARTBEAT_JOIN_SECONDS` (5 s) so the sequential worker is never held — review finding A-1; the task inherits tenant + job context; constants invariant 2×heartbeat ≤ stale, join < heartbeat), 2 Postgres/RLS tests (`test_stir_p1_lease_heartbeat_pg.py`: renewal on a second connection while the handler's transaction is open; renewal scoped by tenant under FORCE RLS — predicate also carries `org_id` explicitly, another tenant's scope renews nothing) — added to the CI postgres job (QA-001 gate); seeded: heartbeat task removed → red; tenant scope removed → red on Postgres; join unbounded → red | DONE (2c631d5, CI #564) |
| STIR-P2-01 (STIR-G2, STIR-004/005/020, STIR-D2) | Stirling-PDF · `processExecutor.timeoutMinutes` | three `pytesseract` calls with no `timeout=` (`pdf_ocr.py`, `receipt_ocr.py` — missed by the archive patch — and `extraction_provider.ImageProvider`) | ADAPT simple version — P2 | `Settings.ocr_process_timeout_seconds` (120, 0 < x ≤ 300); one bounded seam `pdf_ocr.ocr_image_to_data` used by both `image_to_data` sites; `OcrTimedOut`; `PROCESSING_TIMEOUT` in the closed capture vocabulary (`retry_helps=True`, `user_fixable=True`); providers map it; the three OCR routes answer 503 "OCR timed out" instead of 500 | 8 tests in `test_pdf_ocr.py` incl. the receipt path and all three routes (receipt scan, expenses statement import, reconciliation import); the routes answer the operator sentence `pdf_ocr.OCR_TIMED_OUT_DETAIL` ("Reading this file took longer than the server allows. Try again in a moment, or send a shorter or clearer scan.") and log the budget/page — review finding Product-3; the order named `tests/test_capture_failures.py`, which does not exist; `tests/test_capture_failure_worklist.py` is the equivalent and is green with the new kind; seeded: timeout dropped at either site → red; route mapping dropped → red | DONE (2c631d5, CI #564) |
| KT-001 / PAT-004 (REF-008, REF-013, CMP-005) | Scrapling · `throttle.py` AutoThrottle: a hint never speeds the crawler | `webhooks._http_post` discarded response headers; the queue alone chose 30 s | ADAPT NOW — P2 | `jobs.RetryAfterError(retry_after_seconds)`; `_fail(..., retry_after_seconds)` → `max(local_backoff, hint)`; `webhooks._parse_retry_after` (delay-seconds and HTTP-date; NaN/inf/garbage → None; past → 0); `deliver` raises `RetryAfterError` on a non-2xx with a parseable header | parametrised 5→30 / 120→120; **capped at `RETRY_AFTER_CAP_SECONDS` = 86 400** (review finding S-5: `Retry-After: 1e300` overflowed the datetime before commit and left a phantom RUNNING lease; `315360000` parked a delivery for a decade outside every gauge) — 1e300 / 315 360 000 / cap+1 → one day; non-finite or negative hint refused; last attempt still dead-letters; parser cases; end-to-end 503 + `Retry-After: 900` → `run_after = now + 900 s` (the Scrapling integration test the combined patch had dropped; the order's literal 429 case is covered by the status-agnostic path, not a separate test); seeded: floor removed → red, cap removed → red, header dropped → red | DONE (2c631d5, CI #564) |
| PNGX-P3-01 / PAT-028 (PNGX-009/010, TWENTY-017, SEC-SSRF-001, SEC-009) | Paperless-ngx · `network.py` connect-time validation; Twenty · connection-boundary SSRF | `assert_public_url` resolved before the request, httpx resolved again at connect (TOCTOU); fail-open on DNS error; no CGNAT/NAT64 exclusion | ADAPT NOW — keep P3 severity | `app/core/outbound_http.py`: `is_public_address` (explicit class checks — `is_global` alone lets ff02::1 through — plus 100.64.0.0/10 and 64:ff9b::/96), `resolve_hostname_ips` seam, `pin_request` (URL host → vetted IP, Host header and `sni_hostname` keep the name), `PinnedPublicAsyncHTTPTransport` resolving in `asyncio.to_thread` (blocking-IO rule); `webhooks._http_post` uses it with `follow_redirects=False`; `UnsafeOutboundUrl` at connect → terminal `blocked:` delivery, job succeeds; `_addr_is_public` delegates so registration and connect agree | `test_outbound_http.py` (18 classifications, Host/SNI/port, any-private-answer rejects, literal IP not resolved, IPv6 Host bracketed, NXDOMAIN is a ConnectError, transport refuses before a socket opens); `test_webhooks.py` proves the REAL `_http_post` goes through the pin, does not follow a redirect (one request, the 3xx is the outcome), and that registration and connect share one definition of public; seeded: transport bypassed → red; multicast let through → red; redirects followed → red; old registration definition → red. The explicit CGNAT/NAT64 list is NOT load-bearing on Python 3.11 — 100.64/10 is already `is_global == False`, and 64:ff9b::/96 (`is_global == True`!) is refused by the `is_reserved` class check — kept as version-drift insurance (production image is Python 3.14; classification there is unverified from this host — POSSIBLE). **Deliberate deviation from the order's "do not change registration-time semantics":** `_addr_is_public` now delegates to the same predicate, so registration refuses the wider set (CGNAT, 192.0.0.0/24, 198.18/15, …) — accepting at registration what delivery will block forever is worse | DONE (2c631d5, CI #564) |
| PNGX-P2-02 / PAT-030 (PNGX-007, PNGX-D6) | Paperless-ngx · `logging.py` ContextVar task id | worker log lines carried `request_id: "-"` and nothing else | ADAPT NOW — P2 | `job_id_ctx` / `job_kind_ctx` in `observability.py`, set/reset around dispatch, emitted by the JSON formatter only when set; inherited by the heartbeat task | context visible in the handler and reset after on the success AND failure paths; a real JSON log line inside a handler carries both keys; outside a job the keys are absent; documented for log pipelines in `docs/DEPLOYMENT.md` §5; seeded: context not set → red | DONE (2c631d5, CI #564) |
| FLASK-P2-02 (FLASK-001/023, FLASK-G2) | Flask · `setupmethod` refuses late registration; Blueprint duplicate checks | `router.py` relied on a comment to keep `/issued/recurring` ahead of `/issued/{invoice_id}` | ADOPT PRINCIPLE / IMPLEMENT NATIVELY — P2 | `tests/test_route_topology.py`: walks `app.routes`, reuses Starlette's compiled `path_regex`, zero allowlist; result on the live app: **no shadowed route** | self-test on a seeded fixture app names the pair; a correct-order fixture passes | DONE (2c631d5, CI #564) |
| BILL-REL-001 (LAGO-007/011/024, LAGO-D1/D2) | Lago · verified webhook → DB row → processing job | `routes/billing.py:stripe_webhook` applies inline and answers 200 on any exception (event lost) | ADOPT — P1; ADAPT: existing Job row, no new table; customer-mismatch is terminal | `billing.subscription_event_org_id` (side-effect free) → `enqueue_with_outcome(idempotency_key=event_id)` under the tenant guard → commit → 200 `{received, queued, created}`; enqueue failure → 503; handler `billing.apply_subscription_event` re-resolves the customer and raises the new `jobs.PermanentJobError` (dead-letter on attempt 1) on mismatch; ADR-0013 records the body contract change and the ordering assumption | 6 route/handler tests (`test_billing_stripe.py`) + 2 scenarios (`test_billing_reliability_scenarios.py`: worker dies mid-apply → reclaimed → applied once, ledger row once; triple redelivery → one job, one apply) + `PermanentJobError` test; RLS suite green on Postgres 16 with the change; seeded: enqueue failure answered 200 → red; permanent flag dropped → red; mismatch check removed → red | DONE (a789e98, CI #566/#567) |
| BILL-METER-001 (CMP-LAGO-002, LAGO-D3/D4) | Lago · immutable usage events by semantic id | `billing_usage.report_org_usage` recomputes delta and identifier from the live count — a lost response over-reports | ADOPT — P1 before meter activation | `UsageCounter.reporting_target` (+ migration `e7f9a1c3d5b8`, additive, backfill `= reported`, IMPACT/PREFLIGHT/NORMALIZATION/POSTCONDITION/ROLLBACK stated in the docstring per TW-P2-03); `SELECT … FOR UPDATE` on the counter; freeze + commit before the provider call; identifier from the target; on error `reported < reporting_target` so the same segment replays | lost-response scenario (count 10 → accepted, response lost → 15 → replay 10 under the same identifier → then 5 under a new one); commit-before-call proven by commit ordering (a second-session read cannot prove it on SQLite's shared connection); pre-migration row repaired; migration applied on local Postgres 16, DB-012 parity test green; seeded: commit → flush → red; identifier from live count → red; delta from live count → red. **Finding (pre-existing, not R2): `alembic check` on Postgres reports FK drift for the DB-018 column-list `SET NULL (project_id)` constraints, which SQLAlchemy renders as `SET NULL` — a comparison limitation of alembic's autogenerate, not a schema difference; the repository's own parity gate (`test_migrations.py`, DB-012) compares the real ON DELETE text and is green, and CI's `alembic check` step runs on SQLite where the rendering agrees. Recorded in FINDINGS as DB-019 (P4, informational)** | DONE (a789e98, CI #566/#567) |
| BILL-QA-001 (LAGO-016, LAGO-D7) | Lago · `spec/scenarios` | no lost-response scenarios | ADOPT | `tests/test_billing_reliability_scenarios.py` (webhook accepted → worker death → reclaim → applied once; redelivery ×3 → one job) + the meter lost-response / commit-order / repair scenarios in `test_billing_usage.py`; each asserts effects, identifiers, quantities and durable state | green | DONE (a789e98, CI #566/#567) |
| SEC-SC-001 / PNGX-P2-01 (PSC-007/008, PNGX-018, STIR-017, FLASK-020) | PSC/Paperless/Stirling/Flask · full-SHA pins | 25 mutable `uses:` tags across three workflows | ADOPT — P1 | all 25 `uses:` across the three workflows pinned to commit SHAs verified with `git ls-remote` against the upstream tags on 2026-09-07 (the archive's table matched, byte for byte); `# vN` comments kept for Dependabot; `scripts/check_github_action_pins.py` runs in the CI lint job, in `security-supply-chain.yml`, and as `tests/test_supply_chain_gates.py`; `persist-credentials: false` on every checkout; top-level `permissions: contents: read` on `ci.yml` and `pii-history.yml` (release keeps `packages: write`); `vr-baselines` — the ONE job that writes (it publishes refreshed baselines to the dispatching branch and refuses main) — keeps `contents: write` and its token, allowed by name in the test (the first draft of this batch removed it on a misreading; the review's DevOps lens caught the broken push before it shipped) | gate + 3 repository tests; seeded: one tag unpinned → red, token persisted → red, permissions block removed → red | CERTIFYING (R3) |
| SEC-SC-002 / SEC-SC-003 (PSC-010/011/012) | PSC · actionlint, zizmor, TruffleHog, dependency-review | pip-audit + PII scan only | ADOPT (hybrid) | `.github/workflows/security-supply-chain.yml`: workflow-review (pin check + actionlint + zizmor), secret-scan (TruffleHog, PR diff, verified secrets only), dependency-review (fail on moderate+), security-controls gate; `permissions: contents: read`; every action SHA verified upstream (actionlint v2.2.0, zizmor-action v0.6.3, trufflehog v3.97.1, dependency-review-action v5.0.0); runs on pull_request, push to main and dispatch (the two PR-only jobs skip otherwise) | the register keeps SEC-SC-002/003 `open` until a real PR run proves each job on a seeded case — verification is the run number, not the file | CERTIFYING (R3) |
| PSC-009 / FLASK-019 | PSC/Flask · read-only default permissions | only `release.yml` has `permissions:` | ADOPT | explicit read-only `permissions:` on every workflow, asserted by `test_every_workflow_declares_read_only_token_permissions_at_the_top` and `test_no_job_level_permission_widens_the_default` | tests; zizmor runs in the new workflow | CERTIFYING (R3) |
| SEC-CTRL-001 (PSC-001/002/004/018/021) | PSC · YAML source → generated checklist | no compact register | ADOPT C | `docs/security/security-controls.json` (15 controls: the archive's 14 with statuses updated to THIS tree — SEC-SC-001 and SEC-SSRF-001 `verified`, SEC-GOV-001 `blocked`, OPS-GOV-001 `open` — plus SEC-JWT-001) + `scripts/security_control_gate.py --check/--render` (stable ids, vocabularies, verified ⇒ evidence and EVERY evidence path must exist — an addition over the archive, blocked ⇒ blocker, N/A ⇒ rationale, no open P0, drift = exit 1) + generated `SECURITY-CONTROLS.md`; the gate runs in the CI lint job and the supply-chain workflow; a test asserts the register's claims about the tree and that `SECURITY.md` is still the placeholder while SEC-GOV-001 is blocked | 8 gate tests incl. six refused shapes; seeded: stale view → red, non-existent evidence path → red | CERTIFYING (R3) |
| ENG-GOV-001 (PSC-005/006/019) | PSC · evidence-first PR template | none | ADOPT | `.github/PULL_REQUEST_TEMPLATE.md` (objective, scope, risk rows, evidence, tests ACTUALLY executed, rollback with the §9 migration sections, external/secrets review incl. the control register, AI-assistance disclosure, owner decisions, reviewer acceptance); `engineering-rules.md` §11 points at it | `test_pr_template_asks_for_evidence_executed_tests_and_ai_disclosure` | CERTIFYING (R3) |
| FLASK-P2-01 (FLASK-010, FLASK-G1, FLASK-D1) | Flask · `SECRET_KEY_FALLBACKS` | access tokens and OIDC state sign with `secret_key`, which also derives the local KEK | ADAPT C — P2 | `Settings.jwt_signing_key` + `jwt_signing_key_fallbacks` (≤3, verify-only; env `JWT_SIGNING_KEY_FALLBACKS='["old"]'`), `active_jwt_signing_key` / `jwt_verification_keys`; `security.decode_internal_jwt` (active then fallbacks, last error re-raised, nothing logged); access tokens and OIDC state sign only with the active key; production refuses the dev default as signer or fallback, an active key repeated as fallback, and duplicates; KEK derivation untouched (SEC-KEK-001 stays open, now without the JWT reason to rotate SECRET_KEY); documented in `DEPLOYMENT.md` | 3 rotation tests (revocation gate stays authoritative; retired fallback invalidates; new tokens never sign with SECRET_KEY while a signing key is set) + OIDC state rotation + 6 validator cases; seeded: sign with secret_key → red, fallbacks ignored → red, state verified with secret_key → red, duplicate check removed → red | CERTIFYING (R3) |
| PERF-DUCK-001 / PERF-DUCK-002 (DUCK-001/002/003/021, DUCK-D1/D5) | DuckDB · filter/projection pushdown | `approval_policy.waiting_for` hydrates every pending step + invoice + vendor and reduces in Python (PERF-018) | ADAPT NOW — P2; no cache, no index in the same change | one projected SELECT with current-step, SoD and assignee predicates and `LIMIT` in SQL | one-SELECT execution-shape test; re-measure concurrency vs datapoints 5–6 | **DONE (R4)** — pushdown + shape test (guard off, so the org predicates are the statement's own) + service-level predicate tests; the review's differential probe found HEAD and R4 byte-identical on ten chains. Re-measurement: **no measurable change** (`docs/perf/TENANT-GUARD-2026-09-08.md` §1–1a: three alternating pairs, medians inside the run bands) — the harness seeds no approval steps, so the read returned nothing under either shape. The order's step 1 (EXPLAIN + per-call timing), done late after the panel asked for it, caught the first rewrite costing 5.1 vs 3.8 ms per call in statement CONSTRUCTION (two `aliased()` collections per call) — fixed by building the statement once with bound parameters: 2.9 vs 3.8 ms guarded, 0.8 vs 1.3 unguarded. The profile found **PERF-019** → R5. **R6: the pushdown's own datapoint** — with one pending step per submitted invoice seeded, the dashboard grows 1.94× across 2,000 → 8,000 (ceiling 4.0); the first run read 8.08× on unanalysed tables (PERF-020, `docs/perf/SEED-STATISTICS-2026-09-08.md`) |
| TW-P2-01 (TWENTY-005, TWENTY-D3) | Twenty · permission-aware command menu | no palette; `Layout.filterNav` is the one filter | ADAPT NOW — P2 | `CommandPalette.tsx` over the filtered `navGroups`; Ctrl/⌘+K; no API; `quickNavigation` prop off in the design showcase | owner reaches `/invoices`; a role without Upload cannot find it; VR baseline reviewed | **DONE (R4)** — "Go to page" (one name: button, dialog, combobox), keyboard-only e2e (`keyboard.type` after Ctrl+K — the first draft passed only because `fill()` re-focused the input the trap had taken: FE-022, fixed at the root), platform item and suspended-workspace e2e. VR: the live shell is outside the visual suite and `/design` passes no `quickNavigation`, so the CI baselines are unchanged — nothing to review |
| FLASK-P3-01 (FLASK-013, FLASK-D3) | Flask · `filterwarnings = error` | `pytest.ini` ignores every DeprecationWarning | ADAPT, do not flip blindly | inventory run with `-W default::DeprecationWarning`, classify, exact filters, then `error` | inventory file committed under `docs/perf/` | **DONE (R4)** — inventory over 3,266 tests + import time: ONE deprecation (a test's own Pydantic instance access), fixed; `pytest.ini` → `error::DeprecationWarning` + `error::PendingDeprecationWarning` (a reasoned per-source `ignore` is the rule for a future upstream one); `docs/architecture/DEPRECATION-WARNINGS-2026-09-08.md` (not `docs/perf/` — it is a quality record, not a baseline) with the 3.11-only and subprocess caveats |
| R2-DEF-01 (R2 review deferral) | Lago review · alert routing needs the KIND of the dead job | `invoiceiq_jobs{status="dead"}` said that something dead-lettered, not what; `/health/queue` had `dead` only | ADOPT — ops group | `queue_health.snapshot` → `dead_by_kind` (one grouped query, only when `dead > 0`); `/health/queue` body carries it; gauge `invoiceiq_jobs_dead{kind}` with every previously seen kind re-published as 0 when absent (a labelled gauge otherwise keeps its last value) — kinds are the registered handler names, never tenant data | 4 tests (`test_queue_health.py`): breakdown, empty breakdown without a query, probe body, stale-label reset via the real registry; seeded: breakdown skipped → red, reset removed → red | **DONE (R6)** |
| R2-DEF-02 (R2 review deferral, reworded) | Lago/Stirling · the worker's liveness in the container probes | the worker inherited the backend image's HTTP healthcheck (a port it never serves) — Hostinger disabled it; k8s had no probe | ADAPT: the deferral said "`/health/queue` in the compose/k8s probes" — rejected as worded: a queue backlog is a FLEET condition an uptime check pages on, and a liveness probe on it would restart every replica on a backlog (losing the jobs in flight, adding no capacity). Liveness = THIS loop turns | `app/worker_probe.py` (import-light: no app, no SQLAlchemy — it runs every minute in a second process); the loop touches `WORKER_LIVENESS_PATH` every tick, after every job, and on every lease heartbeat inside a long job (`jobs.run_once(on_liveness_tick=…)`); `WORKER_LIVENESS_MAX_AGE_SECONDS` 180 > 2 × the 60 s lease heartbeat; compose `healthcheck` (base + Hostinger; prod inherits) and k8s `livenessProbe` (60 s × 5 > 180 s; `/tmp` emptyDir under the read-only root) | `test_worker_probe.py` (probe semantics incl. clock step, real subprocess exit codes, import-light guard, defaults == Settings and > 2× heartbeat, the loop touches even with the DB down, the lease heartbeat carries the tick, a failing tick never fails the job) + 2 wiring tests in `test_ops_deploy_hygiene.py`; seeded: touch removed → red, tick not carried → red, stale accepted → red, app import → red, compose disabled → red, k8s probe removed → red | **DONE (R6)** |
| BE-022 (R4 review, Product U-2) | Stripe · one subscription per customer | `start_checkout` had no existing-subscription guard; the SPA's disabled button expired at 90 s | ADOPT — P2 | 409 before the provider is reached when `provider.kind == "subscription"` and the org holds a non-canceled `stripe_subscription_id`; EveryPay (redirect, no subscription object) untouched; SPA: "Change to <plan> via Manage billing" opens the Portal, suspended copy points there | 3 route tests + 4 e2e (`billing-portal-change.spec.ts`); seeded: guard removed → red, kind ignored → EveryPay test red, SPA offers Checkout → e2e red. Owner: Portal plan-switch setting + BE-023 (DECISIONS §25) | **DONE (R6)** |
| R5 P-4 (Performance lens) | DuckDB · a pushdown needs its own datapoint | the harness seeded no approval steps; the AP inbox reduced an empty set at every scale | ADOPT | `_seed_scale` adds one generic pending `ApprovalStep` per submitted invoice (collected and added after the invoices flush — the composite FK is not ordered by the unit of work without a relationship), `submitted_at` set, `submitted_by` None (SoD must not hide the rows from the measuring user); `_analyze_after_seed` (PERF-020) | `test_perf_harness_seed.py` (SQLite: one step per submitted invoice and `waiting_for` sees them, LIMIT honoured; Postgres: the seeded tables carry a fresh `last_analyze`) — the Postgres test joined CI's `postgres` job; seeded: steps removed → red, analyse removed → red | **DONE (R6)** |

### 3.2 Documents and policies

| ID | Source | Decision | Placement | Status |
|---|---|---|---|---|
| KT-002 `AI-ENGINEERING-POLICY.md` | Scrapling `AI_POLICY.md` | ADOPT | `docs/architecture/AI-ENGINEERING-POLICY.md` (Accepted), linked from `engineering-rules.md` §11 and the PR template | CERTIFYING (R3) |
| TW-P2-02 AI-agent action safety (12 controls + minimum validation list) | Twenty agent roles | ADOPT AS ARCHITECTURE LAW; no write agent until then | `docs/architecture/adr/0030-ai-agent-action-safety.md` (indexed; README/ARCHITECTURE counts 29 → 30) | CERTIFYING (R3) |
| TW-P2-03 migration law | Twenty plan/apply | ADAPT as policy, reject engine | merged into `engineering-rules.md` §9: IMPACT / PREFLIGHT / REFUSAL / NORMALIZATION / POSTCONDITION / ROLLBACK, refusal-path test, one writer, AI-generated migrations reviewed identically (the R2 migration already carries the sections) | CERTIFYING (R3) |
| SEC-GOV-001 disclosure decision | PSC | BLOCKED on owner | `docs/security/VULNERABILITY-DISCLOSURE-DECISION.md` + `DECISIONS-NEEDED.md` §22; `SECURITY.md` unchanged (a test refuses a replacement while the register says blocked) | DONE (doc) / OWNER |
| KT-003 pattern library | Scrapling | ADOPT | `docs/reference-investigations/00-MASTER/` is the library; append-only | DONE (archived this commit) |

### 3.3 Owner decisions (never decided in code)

| Item | Where |
|---|---|
| Branch protection on `main` (required checks pii-scan, lint, backend, postgres, frontend, frontend-e2e, docker-build; not deploy; block force-push/delete; no second approval while solo) — PNGX-P1-01 / OPS-GOV-001, P1 | `DECISIONS-NEEDED.md` §20 (already listed) and §23 |
| Private vulnerability-disclosure channel (GitHub PVR preferred, or a monitored mailbox) — SEC-GOV-001, P1 | `DECISIONS-NEEDED.md` §22 |
| `STRIPE_METER_UPLOAD` go-live (BILL-METER-001 is a hard precondition) | `DECISIONS-NEEDED.md` §18 (billing) |
| GitHub integration write permission (the archive's 403) — not needed here; this session pushes | none |

## 4. Rule-only, deferred and rejected patterns (archive decisions kept)

Adopted as rules (no code): S3 idempotency identity from semantic inputs; S9 bound task creation
at the first real fan-out; P11/P12 secondary indexes enforce tenant filters and stay rebuildable;
D3 batch processing; D17 performance rewrites never bypass RLS; T4 one migration writer;
T23 documented dependency overrides; F14 fail-closed security capabilities; STIR-020 resource
policy lives in `Settings`; LAGO-018 NOT VALID deadline; L20/D16 agent contracts link to
`engineering-rules.md`.

Deferred with a trigger: KT-004 extraction replay corpus (P2, after PII quarantine design);
KT-005 AP confirmed-output drift (design after data-grain review); KT-006–011, PNGX-P2-03 jobs
operations screen (product decision), PNGX-P3-02/03/04, PNGX-P4-01/02, TW-P3-01/02/03,
TW-P4-01/02, PSC CI-PATH-001, JOB-OBS-001, DB-MIG-VALIDATE-001, PERF-DUCK-003, QA-DUCK-001,
SEC-DUCK-001, STIR-P3-01, STIR-P4-01/02/03, FLASK-P3-02, FLASK-P4-01/03.

Rejected (do not re-propose): Scrapling REJECT-001..010; Paperless NOT-adopt 1–10; Twenty
NOT-adopt 1–12; PSC NOT-adopt 1–12; Lago NOT-adopt 1–15; DuckDB NOT-copy 1–15;
Stirling NOT-adopt 1–15; Flask NOT-copy 1–15 (full lists in the cumulative learnings file).

## 5. Batch R1 — certification record

| Step | Result |
|---|---|
| Targeted suites (after the review fixes) | `test_jobs.py` 37 · `test_webhooks.py` 16 · `test_outbound_http.py` 27 · `test_route_topology.py` 3 · `test_pdf_ocr.py` 10 · capture worklist, bank statement, AR legal, OpenAPI truth, QA-001 gate — **126 passed / 0 failed** in one run; blocking-IO gate, automation webhooks green earlier |
| Postgres / RLS (local Postgres 16, `RLS_TEST_DATABASE_URL`) | heartbeat PG tests 2 / 0 (re-run after the `org_id` predicate) with BE-003 and RLS connection reuse (5 / 0); ARCH-013 advisory lock green in the first round |
| Seeded violations | 14 of 15 caught: heartbeat removed, tenant scope removed (Postgres), join unbounded, Retry-After floor removed, cap removed, header dropped, raise dropped, log context not set, transport bypassed, redirects followed, old registration definition, multicast let through, OCR timeout dropped ×2, statement-route 503 dropped. Not load-bearing: the explicit CGNAT/NAT64 list (see the PAT-028 row) — recorded, not claimed as a control |
| ruff / mypy | clean (393 files) |
| Full backend regression (worktree, final tree after the review fixes) | **3198 passed / 19 skipped / 0 failed (54:35)** — 3217 collected |
| CI | **#564 at 2c631d5 SUCCESS, all seven active jobs** (19:52–20:16 UTC; backend 23:54; postgres job ran the two heartbeat PG tests; frontend-e2e 3:42; deploy skipped on the branch). `main` fast-forwarded to 2c631d5 → **CI #565 (push) SUCCESS, all nine jobs incl. deploy — production 2c631d5 since 21:02 UTC** (backend Healthy 16 s after start; no migration) |
| Review panel | recorded in §6 after the run |

## 5b. Batch R2 — certification record

| Step | Result |
|---|---|
| Targeted suites (after the review fixes) | `test_billing_stripe.py` 22 · `test_billing_usage.py` 14 · `test_billing_reliability_scenarios.py` 5 · `test_jobs.py` 38 · `test_migrations.py` (DB-012 parity) · `test_docs_truth.py` (README 132 revisions) · `test_openapi_truth.py` · `test_wo_ad_retention_ladder.py` — all green (104 in the first round, 47 + 9 after the fixes) |
| Postgres (local 16, `RLS_TEST_DATABASE_URL`) | migration `e7f9a1c3d5b8` applied; `test_bill_meter_segment_pg.py` (a second connection reads the committed segment and takes `FOR UPDATE NOWAIT` during the provider call on the freeze AND replay paths), `test_rls.py`, heartbeat, BE-003, usage-counter concurrency — 9 / 0 on a quiet database (one earlier `test_rls.py` failure was residue from a killed concurrent run: a leftover `switched@x.io` user; removed) |
| Seeded violations | 9 of 9 caught: enqueue failure answered 200; permanent flag dropped; mismatch check removed; supersession check removed; empty event id accepted; segment flushed not committed; identifier from live count; delta from live count; post-ack advance made a blind write |
| ruff / mypy | clean (393 files) |
| `alembic check` | green on SQLite (CI's mode); on Postgres it reports the pre-existing DB-018 column-list rendering difference — DB-019, informational |
| Full backend regression (worktree, final tree) | **3213 passed / 20 skipped / 0 failed (54:56)** — 3233 collected |
| CI | **#566 at a789e98 SUCCESS, all seven active jobs; #567 (main, push) SUCCESS, all nine incl. deploy — production a789e98 since 2026-09-08 04:37 UTC, backend Healthy 21 s after start with the migration applied** |

## 5c. Batch R3 — certification record

| Step | Result |
|---|---|
| Upstream verification of every pinned SHA | 12 tags (8 in the existing workflows, 4 in the new one) checked with `git ls-remote` on 2026-09-07; all lightweight tags, all equal to the archive's table |
| Gates on the tree | `check_github_action_pins.py` PASS · `security_control_gate.py --check` PASS (15 controls, view in sync) · **zizmor 1.30.0 (the pinned version) offline over `.github/`: exit 0, no findings** (the first draft exited 13 on four `dependabot-cooldown` findings — fixed with `cooldown: default-days: 7`) · actionlint 1.7.12 clean (review DevOps lens) · all workflows and compose files parse |
| Targeted suites | `test_supply_chain_gates.py` 17 · `test_sessions.py` (+4) · `test_sso_oidc.py` · `test_config_guards_authz.py` (+7) · `test_ops_deploy_hygiene.py` (+1) · `test_foundation.py` · `test_security_hardening.py` · `test_docs_truth.py` — **114 / 0** after the review fixes |
| Seeded violations | 13 of 13 caught: one action unpinned; token persisted; permissions block removed; generated view stale; verified control with a non-existent evidence path; token signed with SECRET_KEY; fallbacks ignored on verify; OIDC state verified with SECRET_KEY only; duplicate fallbacks accepted in production; session not bound to the token subject (SEC-012); compose file not forwarding the signing-key variables; the writing job's `contents: write` removed; expired token reported as a signature failure |
| ruff / mypy | clean (393 files) |
| Full backend regression (worktree, final tree) | **3244 passed / 20 skipped / 0 failed (54:04)** — 3264 collected |
| CI | recorded in the dashboard when read; the new workflow's PR-only jobs (secret-scan, dependency-review) run on the first real pull request — SEC-SC-002/003 stay `open` in the register until then |

## 5d. Batch R4 — certification record

| Step | Result |
|---|---|
| Targeted suites | `test_dashboard.py` 12 (shape with the guard off, service-level predicates, two-step chain, statement-shape ratchet: ≤ 20 SELECTs / ≤ 110 loader options — 20 / 108 today) · `test_p2_batch4_perf.py` 6 · `test_docs_truth.py` + `test_supply_chain_gates.py` 24 · frontend `nav.spec.ts` 16 (keyboard-only palette, platform item, served-permission negative, suspended), `billing-activating.spec.ts` 5 (bought-plan settle, already-applied, failed read, 90 s timeout via `page.clock`, ordinary visit), `masters.spec.ts` 6 (form-modal typing) |
| Seeded violations | **16 of 16 red** — backend: NOT EXISTS removed (3 red), SoD predicate removed, LIMIT out of SQL, vendor join without its org predicate (caught after the shape test gained the assertion), the pre-fix Pydantic instance access under `error::DeprecationWarning`; frontend: palette indexing the unfiltered nav, palette offered while suspended, activating not disabling subscribe, no poll while activating, Modal ignoring `initialFocusRef`, Portal detaching synchronously, trap re-running per render (caught by the form-modal typing test), settle on first-read change instead of the bought plan, timeout returning to idle, bought plan re-enabled after timeout, identity not re-read |
| Gates | `tsc`, `check-e2e` 51, `check-controls` 268 / 0 debt, `check-labels` 140, `check-pending` 185, `check-locale` 149, `check-query-errors` 20 (ratchet unchanged), build 430.6 kB raw / 125.7 kB gzip (budget 460 / 135); ruff, `ruff format`, mypy 393 files; action pins PASS; control register PASS |
| Full frontend e2e | **481 passed / 0 failed (5.8 m)** on the final tree |
| Deprecation inventory | 1 warning in 3,266 tests + none at import (55:44 run with the ignore overridden) → `error` for both categories; `docs/architecture/DEPRECATION-WARNINGS-2026-09-08.md` |
| PERF-DUCK-002 | first pair + three alternating pairs + per-call timing + EXPLAIN ANALYZE (`docs/perf/TENANT-GUARD-2026-09-08.md`); the guard-bound measurement → PERF-019 |
| VR | not applicable to the live shell (outside the visual suite); `/design` untouched — CI's `frontend-e2e` VR step is the evidence |
| Full backend regression (worktree, final tree) | **3248 passed / 20 skipped / 0 failed / 0 warnings (57:26)** — 3268 collected, `error::DeprecationWarning` in force |
| CI | **#574 at 078d775 SUCCESS — all seven active jobs (frontend-e2e 481 + VR green, postgres, backend, lint, docker-build, pii-scan, frontend); main fast-forwarded to 078d775; #575 (main, push) SUCCESS, all nine jobs incl. deploy — production 078d775 since 2026-09-08 12:34 UTC** |

## 5e. Batch R5 — certification record

| Step | Result |
|---|---|
| Targeted suites | `test_tenant_guard_mechanism.py` 14 on SQLite + the Postgres-engine test (20 with the RLS suites on Postgres 16) · `test_tenancy_parity.py` **98 / 0** (incl. the self-test with layer 2 neutralised) · `test_rls.py` + `test_rls_connection_reuse.py` **5 / 0 on Postgres 16**, now cleaning up their rows · `test_cross_tenant_isolation.py`, `test_tenancy.py`, `test_recycle_bin_*.py`, `test_tenant_registration.py`, `test_dashboard.py` — 69 / 0 |
| Seeded violations | **7 of 7 red on the FINAL tree** (`r5-seeds-perf-final.log`, tree hashes recorded, `set -e`): the tenant parameter hidden from the lambda analysis (the baked-in leak — org B answered 401 / saw nothing of its own); `include_aliases` off; one registered class silently unscoped; `User` scoped by its active-org pointer instead of membership (caught once the test held a member of two workspaces); an aliased `User` checked by class identity (the panel's S1 defect); the guard impossible to neutralise (parity self-test red); `propagate_to_loaders` off (cross-org lazy and selectin loads) |
| Perf | three runs on the FINAL tree vs the R4 final tree's three, same machine: throughput ×1.6–2.1 on every scenario, dashboard conc p50 451 → 187 ms / 15.1 → 31.1 req/s, zero errors (`docs/perf/TENANT-GUARD-2026-09-08.md` §4); shape ratchet ≤ 2 options per SELECT (was 108) |
| ruff / mypy | clean |
| Full backend regression (worktree, final tree) | **3262 passed / 21 skipped / 0 failed / 0 warnings (58:09)** — 3283 collected (the Postgres-engine mechanism test skips under SQLite and runs in CI's `postgres` job) |
| CI | **#577 on cb8763d — all active jobs SUCCESS (13:06–13:45 UTC)**; main fast-forwarded to cb8763d, deploy run #578 |

## 5f. Batch R6 — certification record

| Step | Result |
|---|---|
| Targeted suites | `test_billing_stripe.py` · `test_queue_health.py` · `test_worker_probe.py` · `test_perf_harness_seed.py` (incl. the Postgres-engine analyse proof on local Postgres 16) · `test_ops_deploy_hygiene.py` · `test_jobs.py` · `test_perf_shape.py` — **101 / 0** (1 skip: the perf gate without its URL); e2e `billing-portal-change` 4 / 0, `billing-activating` 5 / 0, `suspended-workspace` 2 / 0, `billing-downgrade-confirm`, `fe-resilience` (one flake on FE-002 sign-in return, unrelated to the tree; 14 / 14 on re-run ×2) |
| Frontend gates | tsc, check-e2e (52 specs clean), labels, controls, pending, locale, query-errors (20 pages, ratchet unchanged), build + bundle 430.6 kB / 125.7 kB gz (budget 460 / 135) |
| Perf | `--shape --scale 2000`: **dashboard 1.94×** with the inbox's rows (the first run read 8.08× — PERF-020); three concurrency runs, zero errors (`SEED-STATISTICS-2026-09-08.md`) |
| Seeded violations | **24 of 24 red on the FINAL tree** (`r6-seeds.log`, `-part2`, `-part3`, `-part4`; tree hash logged, every restore checked identical; part 4 re-seeds the two guards after the `active_provider_kind` refactor and adds the read-constructs-the-provider control that the first regression had found): the Checkout guard removed (3 red); the helper ignoring the provider kind (red only once the redirect-provider test also read `has_subscription` — the first run of that seed stayed green and the test was strengthened, then re-run red); the in-app free switch allowed again (BE-023); the in-flight marker unchecked (2 red); the provider's `expired` event ignored; a cancel keeping the subscription id; `has_subscription` conflating providers; `dead_by_kind` never computed (3 red) and computed on every snapshot (the statement count); the stale label not reset; the loop never touching the liveness file; the lease heartbeat not carrying the tick (3 red); the probe accepting a stale file; the probe importing the application; the Hostinger healthcheck disabled again; the k8s pool without its probe; a k8s LANE without its probe; the harness seeding no approval steps; the harness not analysing after the seed (the Postgres proof red); the page offering a second Checkout to a subscriber (4 e2e red); the page re-enabling the other paid plans after the timeout (e2e red) |
| Full frontend e2e | **487 passed / 0 failed (5.6 m)** on the final tree; plus the `vat-admin` countries locator scoped after CI #578's strict-mode flake (QA-012), 12 / 12 on a 3× repeat |
| ruff / mypy | clean (394 files) |
| Full backend regression (worktree, final tree) | first run **3 failed / 3285 passed / 22 skipped (50:00)** — two OCR tests at the 120 s Tesseract budget and `get_billing` raising `BillingError` on a host without the optional `stripe` package. The OCR pair was environmental: a command backgrounded earlier had left a shell spinning `rev` on one core for 75 minutes; with it killed the pair runs in 11 s. The third was real: the new `has_subscription` constructed the provider (which imports the Stripe SDK) on a READ — fixed with `active_provider_kind()` (the injected provider's kind, else the kind the settings imply; nothing constructed), also used by the in-app plan change; 94 / 0 on the five billing suites. Second run on the final tree: **3288 passed / 22 skipped / 0 failed / 0 warnings (43:57)** — 3310 collected (the two Postgres-engine tests skip under SQLite and run in CI's `postgres` job) |
| CI | **#581 on 8e600b0 — all active jobs SUCCESS (17:27–17:59 UTC)**; main fast-forwarded to 8e600b0; **CI #582 (push) SUCCESS, all nine jobs incl. deploy — production 8e600b0 since 2026-09-08 18:45 UTC** (deploy over SSH 1:08; carries R5 too, whose own deploy run #578 had been skipped by the e2e flake) |

## 6. Review panel verdicts

Filled per batch after implementation (Reference lens · Architect · Security · QA · Adversarial ·
Product/UX · DevOps · Performance · Lead).

### R1 — panel run 2026-09-07 on the uncommitted tree (three review agents, read-only), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Reference Repository Engineer | PASS WITH REQUIRED FOLLOW-UP → follow-ups done | provenance text corrected (NAT64 refused by `is_reserved`, not `is_global`); the widened registration-time address set named as a deliberate deviation; added tests: terminal job cannot be renewed, context reset on the failure path; `test_capture_failures.py` → `test_capture_failure_worklist.py` substitution recorded |
| InvoiceIQ System Architect | PASS | no new table, dependency, process or parallel abstraction; layering core ← services intact; heartbeat uses the existing `SessionLocal` per renewal; OCR budget in `Settings`. Noted, not acted on: the timeout classifier exists twice (`pdf_ocr.ocr_image_to_data`, `ImageProvider`), `webhooks.py`'s lazy `import httpx` now redundant |
| Security Engineer | PASS WITH REQUIRED FOLLOW-UP → fixed | **S-5 CONFIRMED** unbounded Retry-After (overflow → phantom RUNNING lease; decade-long park) → `RETRY_AFTER_CAP_SECONDS` + non-finite refused + 3 tests; S-1 hardening `Job.org_id` in the renewal predicate; S-2 classification probed on 3.11/3.12/3.13 (all listed classes refused; `::ffff:8.8.8.8` and 192.0.0.9/.10 accepted by IANA design); S-8 POSSIBLE: production image is Python 3.14, classification unverified there |
| Adversarial Engineer | PASS WITH REQUIRED FOLLOW-UP → fixed | **A-1 CONFIRMED** `await heartbeat_task` unbounded on a hung renewal → `HEARTBEAT_JOIN_SECONDS` = 5 s then cancel, test with a renewal that never returns; A-5 claims not test-enforced → tests for `follow_redirects=False`, registration/connect agreement, the two statement routes' 503, and the constants invariant; A-6 vacuous sleeps removed; A-7 benign completion race now logged at info with a neutral message; A-4 `HTTPS_PROXY` bypass documented in `DEPLOYMENT.md` |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → fixed | the two untested 503 branches now tested; seeded-violation record judged credible for all nine original controls; flake probe 5/5 green on the heartbeat tests; count bookkeeping corrected in §5 |
| Performance | PASS | ≤ 1 heartbeat per process (sequential worker), one UPDATE/min only for jobs over a minute; `to_thread(getaddrinfo)` cost-neutral (httpx resolved in a thread anyway, now connects to a literal); no perf-harness path touched; POSSIBLE: httpx now in the API startup import graph (startup-heap gate still green) |
| DevOps / SRE | PASS WITH REQUIRED FOLLOW-UP → fixed | `DEPLOYMENT.md`: `job_id`/`job_kind` log fields, two connections per worker in the connections alert, worker grace ≥ OCR budget, 503-in-5xx-rate note, proxy note; SIGTERM path confirmed (in-flight job completes with the lease renewed); CI postgres list correct |
| Product / UX | PASS WITH REQUIRED FOLLOW-UP → fixed | the 503 detail was stack vocabulary ("OCR timed out: OCR exceeded 120 seconds") shown verbatim by three screens → one operator sentence, exception text to the log; remediation no longer promises "this reference" the card does not show; worklist prose confirmed operator-grade and Retry offered (`retry_helps=True`) |
| **Lead Developer** | **DONE** — full regression 3198 / 19 / 0, CI #564 green on 2c631d5, main fast-forwarded | |

### R2 — panel run 2026-09-07 on the uncommitted tree (two review agents, eight lenses), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Reference Repository Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **R-1 LIKELY** durable retry re-orders events for one subscription (a failed `updated{active,pro}` retried after `deleted{canceled}` applied) → a job is SUPERSEDED when a newer job for the same subscription already succeeded (queue-table lookup; the reducer stays untouched per the order's do-not-change list) + scenario test; R-4 "ack then local commit fails" scenario added; adaptations A (terminal mismatch) and B (`applied` dropped) judged correct |
| InvoiceIQ System Architect | PASS | `PermanentJobError` is the right seam (an outcome property, not a kind property; precedent `RetryAfterError`); migration additive, chain linear, `server_default` matches the model; mid-loop commit does not disturb `_complete`/`_fail` (`expire_on_commit=False`); A-3 rollback precondition written into the migration docstring; A-5 comment on the repair branch |
| Security Engineer | PASS | a signed event cannot reach an arbitrary tenant (`stripe_customer_id` is unique and written only from our own `Customer.create`); enqueue under the tenant guard passes FORCE RLS (probed on Postgres); 503 body constant; **S-4** a verified body without an `id` (reachable only with the signing secret) would have made a job per delivery → refused as a harmless 200, test added; a dead-letter's `last_error` shows the tenant its own former customer id only |
| Adversarial Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **A1 CONFIRMED (reproduced)** the single-worker backoff case of R-1 → fixed by the supersession check (the probe scenario is now the committed test `test_a_stale_retry_does_not_undo_a_newer_event_for_the_same_subscription`); **A3** the post-ack advance was a blind write → compare-and-set (`UPDATE … WHERE reporting_target = target AND reported < target`), test; A2 worker down for days → `/health/queue` documented as a REQUIRED uptime check; A4/A5 verified safe (mid-loop commit vs rollback; RLS GUC at the INSERT) |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → done | tests assert state, not call counts; the sibling-helper import has precedent; the commit-order proof got its Postgres twin (`test_bill_meter_segment_pg.py`, in the CI postgres list, QA-001 gate green); Q4 two-events-one-customer gap closed by the A1 test; README 132 revisions verified |
| Performance | PASS WITH REQUIRED FOLLOW-UP → done | **P1 CONFIRMED** the replay path held `FOR UPDATE` across the Stripe call (up to 80 s), stalling that tenant's upload counter (`access.record_usage` writes the same row) → the commit now runs before EVERY provider call; proven by `FOR UPDATE NOWAIT` from a second connection on Postgres; webhook route cost neutral; perf-harness endpoints untouched |
| DevOps / SRE | PASS WITH REQUIRED FOLLOW-UP → partly done | worker lanes absorb the new kind (D1); migration/deploy ordering safe, old code on the new column handled by the repair branch (D2); D3 `/health/queue` documented as required (done); a `kind` label on the dead-letter gauge deferred to the ops group (it changes the Prometheus series shape; recorded below) |
| Product / UX | PASS WITH REQUIRED FOLLOW-UP → partly done | no `applied` consumer; P3 the three stale doc sentences (data-flows, architecture/deployment, overview) rewritten; **P2 open → R4:** after Checkout the Billing page shows the old plan until the worker applies the event and the "Subscribe" button stays enabled — add an "activating…" state with a refetch until the plan changes (recorded in ADR-0013 and §2 R4) |
| **Lead Developer** | **DONE** — full regression 3213 / 20 / 0, CI #566 green on a789e98, main fast-forwarded, CI #567 deployed | |

Deferred from the R2 review, with owners: `kind` label on the dead-letter gauge and `/health/queue` in the compose/k8s probes (ops group, with OPS-003/007/009/011/013); Billing page "activating…" state (R4 product).

### R3 — panel run 2026-09-08 on the uncommitted tree (two review agents, seven lenses; the first attempt died on the API session limit and was re-run), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Reference Repository Engineer | PASS WITH REQUIRED FOLLOW-UP → done | principles transferred natively; JWT do-not-change list honoured (HS256, 24 h, jti gate, KEK derivation untouched); the one order violated — PSC "do not change CI job semantics while pinning" — was the VR-baseline job (below), restored |
| Security Engineer | PASS WITH REQUIRED FOLLOW-UP → done | all 12 pinned SHAs re-verified upstream; `contents: read` suffices for dependency-review, zizmor (`advanced-security: false`) and actionlint; JWT: no downgrade path, active-then-fallback order, nothing logged, 15 malformed-token probes all `JWTError`; IdP JWKS path untouched; register claims for SEC-JWT-001 / SEC-SC-001 / SEC-SSRF-001 true of the code |
| DevOps / SRE | **FAIL → fixed** | (1) `security-supply-chain.yml` would have been red on its first run — zizmor exit 13 on `dependabot.yml` (no `cooldown`) → `cooldown: default-days: 7` ×4, zizmor exit 0 locally; (2) **the VR-baseline job DOES `git push` to the dispatching branch** — the batch's first draft removed its `contents: write` and token on a misreading and five places said "never commits" → re-granted narrowly, allowed by name in the tests, every sentence corrected; (3) the documented JWT rotation was inert on the documented deploy — the compose files pass explicit `environment:` maps → both variables forwarded in all three compose files (`${JWT_SIGNING_KEY:-}`, `${JWT_SIGNING_KEY_FALLBACKS:-[]}`), `.env.hostinger.example`, hygiene test |
| InvoiceIQ System Architect | PASS WITH REQUIRED FOLLOW-UP → done | register is the right single source (posture snapshot vs the audit's chronological ledger); `importlib` loading has precedent (`test_pii_scan.py`); pydantic enforces `max_length=3` on the list; A-2 an expired token with a fallback configured was reported as "signature failed" → `ExpiredSignatureError` decisive, test; A-4 test globs aligned to `*.y*ml` |
| Adversarial Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **B-1 CONFIRMED (reproduced on the live app): the jti gate never checked that the session belongs to the token's subject** — `{sub: victim, jti: attacker's own live session}` signed with the key was served as the victim; pre-existing, but R3's rotation argument leaned on it → `_authenticate` refuses a session whose `user_id` is not the subject, probe committed as a red test; recorded as **SEC-012** (P1, DONE); residuals stated: a SHA swap that keeps `# vN` is caught only by review and zizmor's impostor-commit audit; an existing-but-irrelevant evidence path is caught only by review |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **Q-1 CONFIRMED** first enablement of `JWT_SIGNING_KEY` logs everyone out unless `SECRET_KEY` is listed as a fallback — sentence in `DEPLOYMENT.md`, `config.py` and the env example + test; Q-3 verification-order test; seeded list judged credible |
| Product / UX | PASS WITH REQUIRED FOLLOW-UP → done | P-1 the six reviewer self-attestation boxes collapsed to one prose line; P-3 "session row stays authoritative" copy now true after B-1; P-4 "25 actions" → 33 `uses:` lines; disclosure decision doc judged a clear minimal path |
| **Lead Developer** | **APPROVE FOR BRANCH — DONE when the full regression and CI on the pushed head read green; SEC-SC-002/003 flip to `verified` only with real PR run numbers** | |

### R4 — panel run 2026-09-08 on the uncommitted tree (two review agents, eight lenses), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Reference Repository Engineer | PASS WITH REQUIRED FOLLOW-UP → done | pushdown is a native transfer (no cache, no index); R-2 PERF-DUCK-002 step 1 (EXPLAIN) recorded as skipped and why; R-3 before/after quoted as medians of three alternating runs, not one run each; R-4 the Twenty order's keyboard item was NOT met until A-1 was fixed; R-5 "VR baseline reviewed" had no evidence — the live shell is outside the VR suite, stated as such |
| InvoiceIQ System Architect | PASS WITH REQUIRED FOLLOW-UP → done | AR-1 differential probe: HEAD vs R4 `waiting_for` byte-identical on ten chains (the one divergence, a cross-org vendor name on the unguarded path, is now hidden by the ON-clause org predicate); AR-2 the org predicate inside the `NOT EXISTS` is the only layer-2 scope there — comment says so; AR-4 docstring: parity for non-platform-admin callers; AR-5 the shape test runs with the guard off so its predicates are the statement's own; **AR-7 Billing settle condition wrong for real paths** (EveryPay always, a fast Stripe worker, any reload) → fixed: the bought plan key is the target, the query is dropped on settle; AR-8 PERF-019 acceptance stated as absolute p95 / req/s |
| Security Engineer | PASS WITH REQUIRED FOLLOW-UP → done | palette reaches nothing the sidebar hides (same array, no fetch, unmounted while suspended); polling adds no new request; pushdown narrows visibility; **S-4 layer 2 does not scope correlated subqueries** (captured guarded SQL) — documented in `tenant.py`, the perf note and the PERF-019 brief; S-5 constraints + the exact tests a cheaper guard must pass, in the R5 brief; S-3 plain `invoices.vendor_id` FK recorded as DB-020; S-6 empty state moved out of the listbox |
| Adversarial Engineer | **FAIL → fixed** | **A-1 CONFIRMED (jsdom + live reproduction): every keystroke in the palette moved focus to the Close button** — root cause `useFocusTrap` depending on the inline `onEscape` closure (pre-existing for every form modal: FE-022) → the callback is read through a ref, the trap depends on `active` alone; the Modal takes `initialFocusRef`; the Portal no longer detaches its node on StrictMode's simulated unmount (the second cause, found by the live probe); keyboard-only e2e + a form-modal typing e2e; A-4 Billing already-applied case → fixed with a `planAfter = 0` e2e; A-5 timeout race → the timeout now ends in a `role="alert"` and keeps the bought plan's button disabled |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → done | Q-1 keyboard seed (`keyboard.type`); Q-2 `planAfter = 0`; Q-3 timeout via `page.clock`, failed-read case, checkout counter re-asserted after settle; Q-4 service-level predicate tests (assigned-to-another with approve-any, decided earlier step, ordering, LIMIT); Q-5 unconditional SoD assert; Q-6 platform-admin palette case; Q-7 inventory caveats (3.11-only, subprocess blind spot) recorded |
| Performance Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **P-2 CONFIRMED: PERF-019 is the absolute cost, not the ratio** — with the guard attaching nothing the dashboard still reads 12.5× because the ratio is a shape property (twenty statements on one loop); PERF-018/019 rows reworded, KPI = serial p95 + req/s; P-1 the "43 %" is profiler-weighted — the bound (40–50 %) is the number; P-3 three alternating before/after runs, medians quoted; P-4 the pushdown's benefit is unmeasured, not absent (harness seeds no steps) — dataset specified, own row; P-5 no index missing at realistic scale, trigger condition recorded; P-6 statement-count + loader-option-count test added (20 SELECTs / 108 options today, the red→green for R5); P-7 the bound removed the soft-delete options too — said so |
| DevOps / SRE | PASS WITH REQUIRED FOLLOW-UP → done | no deploy/ops change; new spec in `test:e2e` and the coverage gate; VR gate unaffected; D-5 `BILLING_SUCCESS_URL` must keep `?checkout=success` — sentence in `DEPLOYMENT.md`, `architecture/deployment.md`, `foundation.md`; D-6 support runbook line ("paid but old plan" → `/health/queue` + the webhook job) |
| Product / UX | **FAIL (Billing) → fixed**; palette PASS WITH REQUIRED FOLLOW-UP → done | U-1 the common path (webhook beats the redirect) showed "activating…" for 90 s over a page already on the new plan → target-plan comparison + query cleanup; U-2 the 91 s end state re-enabled "Subscribe" silently → persistent alert, bought plan stays disabled, backend existing-subscription guard recorded as BE-022; U-3 keyboard; U-4 one name "Go to page…", navigation-flavoured empty state, no route-path hint; U-6 combobox semantics (`aria-activedescendant`); U-8 "Checkout complete" (true for delayed-notification methods too); U-9 identity re-read on settle |
| **Lead Developer** | **APPROVE FOR BRANCH — DONE when the final full regression and CI on the pushed head read green** | |

### R5 — panel run 2026-09-08 on the uncommitted tree (two review agents, six lenses), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Security Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **S1 CONFIRMED: `aliased(User)` received the active-org POINTER predicate instead of membership** — the callable is handed the `AliasedClass`, so `model is User` was False; fixed by resolving the class through `inspect(cls).mapper` and building the predicate on the alias's own columns; aliased-`User` and join-on-alias assertions added. S2 the option's cache key is structural (code object + `DeferredLambdaElement` + the typed bind's structure), the org value an extracted bind — a direct equal-keys/different-values test added. S3 coverage identical to the old mechanism on eighteen statement shapes on Postgres; the uncovered set (WHERE-only tables, identity-map hits, refresh, Core/text, writes) is now stated precisely in the ADR, `tenant.py` and `approval_policy.py`. S4 the bind name collided silently → reserved `_tenant_guard_org` + a grep tripwire |
| Adversarial Engineer | PASS | no cross-tenant rows and no per-tenant cache growth on any shape (third tenant after warm-up, `None` between scoped calls, `include_deleted` alternation, three alternating passes); all three docstring failure claims reproduced |
| InvoiceIQ System Architect | PASS WITH REQUIRED FOLLOW-UP → done | A1 `hasattr` replaced by the mapper's column set (a non-column `org_id` attribute could have produced an unmapped comparison); registry wording made consistent (on/off + cache key; the per-class decision is by attribute); A3 the ADR addendum's "unchanged in WHAT it enforces", "lazy loads stay covered" and "correlated subqueries" sentences rewritten with the measured rule; exact pin stated; A5 tripwires added (direct cache-key test, aliased User, `_all_mappers` reach, reserved bind name, compile-level reach rule); A6 `test_rls.py` now deletes its rows; A7 the `AND true` collateral recorded |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **Q-4 the users test's raw INSERT was SQLite-only** (`is_expense_approver = 0` fails on Postgres) → ORM `Membership(...)`; a Postgres-engine test of the mechanism added (`RLS_TEST_DATABASE_URL`, cleans up) and the file joined CI's `postgres` job; **Q-5 the seed log predated the final tree** → seeds and the three perf runs re-run on the final tree with `set -e` (§5e cites that log); Q-1 the docstrings credited the cache-size assertion — corrected: the row assertions and the direct key test are the proof; Q-3 the coverage test uses public API (`str(select(cls).options(opt))`); Q-6 joinedload ON clause, selectinload, third tenant and `include_deleted` alternation added |
| Performance Engineer | PASS WITH REQUIRED FOLLOW-UP → done | gain attributable (non-overlapping three-run bands on every scenario; medians recomputed and correct); nothing slower — callable resolution 0 calls in steady state, `_gen_cache_key` 31 per statement (was ~330), guard ≈ 0.9 ms/request; P-2 `explore_by_vendor` (×1.60) added to §4; P-4 the next cost named (twenty sequential round trips — PERF-018's own remedy); P-5 the unbacked "nothing measurable" sentence replaced by the panel's profile figures; CI's informational step unchanged (datapoint 7 recorded when read) |
| Reference Repository Engineer | PASS WITH REQUIRED FOLLOW-UP → done | no control weakened (registration test + coverage test + ADR); **R-4 no rollback path** → written into the ADR addendum and the perf note (revert the four-file commit and restart; RLS bounds a leak meanwhile; no runtime switch by design); R-2/R-3 the ADR's two imprecise sentences rewritten (fixture-vs-assertion; the cache proof was SQLite-only — now also on Postgres) |
| **Lead Developer** | **APPROVE FOR BRANCH — DONE when the final full regression and CI on the pushed head read green** | |


### R6 — panel run 2026-09-08 on the uncommitted tree (two review agents, seven lenses), fixes applied before certification

| Lens | Verdict | Findings acted on |
|---|---|---|
| Security Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **S1 CONFIRMED: a Stripe subscriber could still set the FREE plan in-app** — entitlements gone, charge running, reverted by the next renewal webhook (BE-023) → `change_plan` 409s a subscriber choosing free and the page's Free card cancels through the Portal (a business-behaviour change under the autonomy rule, flagged, DECISIONS §25); **S2 LIKELY: the `status != "canceled"` clause was dead** (a canceled org is a 401 on this surface) and a reactivated workspace would have been 409 forever → the webhook clears `stripe_subscription_id` on cancel (support keeps `stripe_customer_id`), guard folded into `_holds_live_subscription`; S3 the Stripe status matrix checked (past_due/unpaid/incomplete/paused → Portal; `paused` by `pause_collection` recorded in §25 as a runbook case); S5 kinds are a closed set (route allow-list + constants), no tenant data, `/health/queue` is public — its PUBLIC_ROUTES reason now says "by job kind"; S6 the probe widens nothing |
| Adversarial Engineer | PASS WITH REQUIRED FOLLOW-UP → done | **A1 CONFIRMED: the window BEFORE the webhook lands** — until the worker applies `checkout.session.completed` the org holds no subscription id and the guard cannot fire; the page re-enabled every paid button but the bought one after 90 s → every started Checkout is persisted as a `BillingPayment` in state `initial`, a second one is refused while an unexpired one exists, the Stripe session is created with the same 30 min expiry, the provider's `completed`/`expired` events settle/abandon the marker (a new `checkout_session_id` on the reduced event; the applier's no-op branch extended), and the page keeps EVERY paid button off after the timeout; A4 `has_subscription` conflated providers (a Stripe deployment with a legacy EveryPay token was sent to a Portal it had no account at) → the active provider's object only; **A6 LIKELY: a Postgres that accepts connections and never answers parks the loop and restarts every replica** — accepted and documented as the probe's honest behaviour (churn, not loss); a global `command_timeout` recorded as DB-021 to be measured first; A8 the recovered-kind 0 holds per process only (no multiprocess registry; the worker's own registry is never scraped) → OPS-014, alerting routed off `/health/queue`'s body; A5/A7/A9 confirmed correct (OCR is threadpooled so the 60 s lease tick keeps the file fresh; read-only FS, disk full, clock steps; seed FK ordering) |
| DevOps / SRE | PASS WITH REQUIRED FOLLOW-UP → done | **D1 CONFIRMED: `36-worker-lanes.yaml` — applied INSTEAD of `35-worker.yaml` — had no probe on either lane, the OCR lane included, and the test read only `35-*`** → probes on both lanes; the test iterates every worker Deployment under `deploy/k8s`; D2–D6 confirmed (compose comment honest about `unhealthy`; 60 × 5 > 180 s; `/tmp` emptyDir writable under the read-only root; `python -m app.worker_probe` resolves via `WORKDIR /app`; env parsing equivalent — the `.env`-vs-environ split documented: process environment only); D7 seven doc edits applied (health/probes/metrics/env-table bullets, Hostinger row, PUBLIC_ROUTES reason) |
| QA / Test Engineer | PASS WITH REQUIRED FOLLOW-UP → done | every new test's red seed named and confirmed load-bearing except **Q-1 the "no query" half of `test_nothing_dead_…`, which counted nothing** → a `before_cursor_execute` counter (2 statements, 3 with a dead job); **Q-2 CONFIRMED false design claim: a handler that awaits forever does NOT fail the probe** (the lease heartbeat keeps ticking for it; no per-job deadline) → the docstring, DEPLOYMENT.md and the data-flows failure table say what is and is not detected, `test_a_handler_that_awaits_forever_keeps_the_probe_green_by_design` pins the boundary, BE-024 opened; Q-6 the gauge test's last assertion asserted its own write → asserted before and after; Q-7 a wrong comment on the k8s timing product corrected; e2e locators exact, first-anchor rule kept |
| Product / UX | PASS WITH REQUIRED FOLLOW-UP → done | P-1 the Free card for a subscriber (see S1) → "Cancel subscription via Manage billing"; **P-2 the R18 confirm dialog said "Switch to X? … switching will disable it" while confirming only opened the Portal** → "Change to X in Manage billing?" / "Continue to Manage billing" / "if you complete the change there, it will be disabled when the new plan is applied" (and "Subscribe to X?" on the Checkout path, which had the same drift); P-3 the page never said WHY → a subscriber's blurb explains that changes happen in Manage billing so a second subscription is never started; P-4 the dangling "DECISIONS §25" reference → §25 written; P-5 "or the plan lapsed" dropped for a workspace that provably holds a subscription |
| Performance Engineer | PASS | `dead_by_kind` runs only when `dead > 0` (now proven by the statement count) on `ix_jobs_ready`'s status prefix; gauge cardinality bounded by the kinds; the probe ≈ 30–60 ms a minute; **no new index needed for the inbox statement** — the 8.08× reading was the unanalysed-table plan (PERF-020), `ix_approval_steps_invoice` and `uq_approval_steps_invoice_seq` serve it once statistics exist; seeding still chunked |
| InvoiceIQ System Architect | PASS WITH REQUIRED FOLLOW-UP → done | layering clean (route → service; `jobs` receives a plain callable); `worker_probe.py` at `app/` beside the other entrypoints is the convention; A-3 "heartbeat" meant two things (lease vs liveness) → renamed before the env names ship: `WORKER_LIVENESS_PATH` / `WORKER_LIVENESS_MAX_AGE_SECONDS`, `/tmp/invoiceiq-worker-liveness`, `_touch_liveness`, `on_liveness_tick`; A-4 the `.env`-vs-environ split documented; A-5 the false claim (see Q-2) reworded; the still-stale doc lines (source trees, curl example, domain-modules, data-flows, TENANT-GUARD §0) refreshed |
| **Lead Developer** | **APPROVE FOR BRANCH — DONE when the final full regression, the seeds on the final tree and CI on the pushed head read green** | |
