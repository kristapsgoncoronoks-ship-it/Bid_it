[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    slow: tests that shell out (e.g. run Alembic in a subprocess); still run in CI
filterwarnings =
    ignore::DeprecationWarning
