# Developer + CI tooling, pinned for reproducibility. Runtime deps (incl. the
# pytest that ships in the app image for the in-container smoke) live in
# requirements.txt. Install both locally:  pip install -r requirements.txt -r requirements-dev.txt
ruff==0.16.2
mypy==2.3.0
pre-commit==4.6.2
