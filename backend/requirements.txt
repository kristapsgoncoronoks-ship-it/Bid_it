fastapi==0.140.13
uvicorn[standard]==0.51.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
aiosqlite==0.22.1
alembic==1.18.5
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
# Password hashing calls bcrypt directly (see app/core/security.py). passlib was
# dropped: 1.7.4 is unmaintained and incompatible with bcrypt >= 5 (its backend
# probe trips bcrypt 5's hard 72-byte limit). bcrypt's "$2b$" output is unchanged,
# so hashes created under the old passlib path still verify.
bcrypt==5.0.0
python-multipart==0.0.32
email-validator==2.3.0

# Object storage for document bytes (ADR-0008). Imported lazily and only when
# STORAGE_BACKEND=s3; the default local/memory backends need nothing extra.
boto3==1.43.57

# PDF ingestion + OCR. Text-layer extraction needs only pdfplumber; the OCR
# fallback also needs the system `tesseract-ocr` binary on PATH (see Dockerfile).
pdfplumber==0.11.10
pypdf==5.1.0
pypdfium2==5.12.1
pytesseract==0.3.13
pillow==12.3.0
reportlab==4.2.5
# Structured e-invoice XML (UBL 2.1 / UN-CEFACT CII, incl. Factur-X/ZUGFeRD).
# defusedxml hardens parsing of attacker-supplied XML (XXE / billion-laughs).
defusedxml==0.7.1

# General report-to-Excel writer (board I1.5) — Explore-cut .xlsx export.
# Pure Python, no compiled/system dependency.
openpyxl==3.1.5

# Billing (ADR-0013). Imported LAZILY only when a Stripe secret key is set;
# without it the app runs on the NullProvider and nothing charges. Uncomment (or
# `pip install stripe`) on any deployment that turns billing on.
# stripe==11.4.1

# Malware scanning of uploads/attachments (OPTIONAL). filesec imports `clamd`
# lazily and only when clamav_enabled=true; without it, type validation + EICAR
# detection still apply. Install alongside a running clamd daemon to enable AV.
# clamd==1.0.2

# dev / test
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
prometheus-client==0.26.0
